
# 상속 

여러 클래스를 합성하는 방법들
1. 1개 상속 + n개 포함
2. 1개 상속 + n개 인터페이스
3. n개 인터페이스
4. n개 인터페이스 + n개 포함
5. 1개 상속 + n개 인터페이스 + n개 포함
또는 인터페이스 대신 추상 클래스
> 상속, 포함, 추상클래스, 인터페이스 들의 설계 철학을 이해하여 적재적소에 잘 혼용하여 사용할 필요가 있다.



## 공변 반환 타입 (Covariant Return Type)

### 공변(Covariant)의 의미

Co-(함께) + variant(변하는) = **두 가지가 같은 방향으로 변한다.**

```
클래스 계층:  Animal → Dog  (아래로 내려감)
반환 타입도:  Animal → Dog  (같은 방향으로 내려감) → 공변
```

반대 개념은 **반공변(Contravariant)**: 계층은 내려가는데 타입은 거꾸로 올라가는 것. 자바의 `? super T`가 반공변의 예.

### JDK5 이전: 반환 타입이 반드시 같아야 했음

java

```java
class Animal {
    Animal create() { return new Animal(); }
}
class Dog extends Animal {
    Animal create() { return new Dog(); }  // 반환 타입이 부모와 똑같아야 했음
}

// 호출하는 쪽에서 매번 캐스팅 필요
Dog dog = (Dog) dogInstance.create();  // ❌ 불편하고 런타임 실패 위험
```

### JDK5 이후: 자손 타입으로 반환 타입 변경 가능

java

```java
class Dog extends Animal {
    Dog create() { return new Dog(); }  // ✅ 반환 타입을 자손으로 변경 가능
}

// 캐스팅 없이 바로 사용
Dog dog = dogInstance.create();  // ✅ 컴파일 타임에 타입 보장
```

### 왜 자손으로만 가능한가

java

```java
class Dog extends Animal {
    Object create() { ... }  // ❌ 상위 타입으로 올리는 건 불가
    Animal create() { ... }  // ✅ 같은 타입 (기존 규칙)
    Dog create() { ... }     // ✅ 하위 타입으로 내리는 건 가능 (공변)
}
```

부모가 "Animal을 반환한다"고 약속했는데, Dog는 Animal의 하위 타입이므로 그 약속을 지키면서 더 구체적인 정보를 추가 제공하는 것 → 허용. Object를 반환하면 부모의 약속을 어기는 것 → 불가.


## 오버라이딩 시 접근 제어자는 좁혀질 수 없다

### 규칙

오버라이딩할 때 접근 제어자는 **부모보다 좁은 범위로 변경할 수 없다.**

java

```java
class Animal {
    public void sound() { }     // public
}
class Dog extends Animal {
    private void sound() { }    // ❌ 더 좁게 → 불가
    protected void sound() { }  // ❌ 더 좁게 → 불가
    public void sound() { }     // ✅ 같은 범위 → 가능
}

class Cat extends Animal {
    public void sound() { }     // ✅ 같은 범위 → 가능
}
```

넓히는 건 괜찮다:

java

```java
class Animal {
    protected void sound() { }  // protected
}
class Dog extends Animal {
    public void sound() { }     // ✅ 더 넓게 → 가능
}
```

### "더 좁히면 더 안전한 거 아닌가?"라는 직관이 틀리는 이유

핵심은 **"누구의 입장에서 안전한가"**. 상속 관계에서는 부모 타입으로 자식 객체를 다루는 **외부 코드**가 존재한다.

java

```java
Animal animal = new Dog(); // 부모 타입으로 자식 객체 참조
animal.sound();            // 부모 기준으로 public → 호출 가능하다고 판단
```

만약 자식이 접근 범위를 좁힐 수 있다면:

java

```java
class Dog extends Animal {
    private void sound() { }  // 만약 이게 허용된다면...
}

Animal animal = new Dog();
animal.sound(); // 💥 Animal 기준으로는 public이라 호출했는데
                //    실제 실행은 Dog의 private 메소드 → 예측 불가능한 상황
```

```
부모: "이 메소드는 public이야, 누구나 써도 돼" (약속)
자식: "나는 private으로 바꿀게, 아무도 못 써" (약속 파기)
외부: ???
```

자식이 접근 범위를 좁히는 순간 **외부 코드가 부모를 믿고 호출했는데 자식이 그 약속을 파기하는** 상황이 생긴다. 이게 오히려 더 위험하다.

반대로 넓히는 건 부모의 약속을 지키면서 더 관대해진 것이므로 외부 코드가 깨질 일이 없다.

한 줄 요약: **부모가 외부에 한 약속(접근 범위)을 자식이 일방적으로 파기하면, 부모 타입으로 자식을 다루는 외부 코드가 예측 불가능한 상황에 빠지기 때문이다**

> 부모가 자식보다 더 넓게 커버한다는 직관 때문에 헷갈리는 것. 오히려 부모가 자식보다 더 적게 커버한다. (상속 받을때 부모를 자식이 extends 함) 따라서 부모의 메소드 접근 범위보다 자식의 접근 범위가 더 넓어야 함


# Collection
java.util 패키지
: 다수의 데이터를 쉽게 처리하는 방법 제공 -> DB처럼 **CRUD** 기능 중요

3대 주요 인터페이스
![[Pasted image 20260727090659.png]]

Thread Safe?  Thread Safe하지 않은 현재 버전은 뭘 주의해야하지? 
![[Pasted image 20260727091520.png]]

Collection은 배열을 기반으로 만들어졌는데 배열은 생성시 크기가 정해지는 반면 Collection은 그렇지 않다. how?
강사님이 역으로 클래스들을 거슬러 올라가며 참고하는 것 따라해보자.

컬렉션 프레임워크에 저장할 수 있는 데이터는 오로지 객체(Object) 뿐이다 즉, int형이나 double형 같은 자바의 primitive 타입은 적재를 못한다는 말이다. 따라서 primitive 타입을 wrapper 타입으로 변환하여 Integer 객체나 Double 객체로 박싱(Boxing)하여 저장하여야 한다

 Map은 iterable 인터페이스를 상속받지 않기 때문이 ~~iterator()~~와 ~~spliterator()~~는 Map 컬렉션에 구현이 되어 있지 않다. 따라서 직접적으로 Map 컬렉션을 순회할수는 없고 스트림(Stream)을 사용하거나 간접적으로 키를 Collection으로 반환하여 루프문으로 순회하는 식을 이용한다.


collection 메소드
retainAll : 교집합 동작
removeAll : 차집합 동작

Collection 인터페이스의 메서드를 보면 요소(객체)에 대한 추가, 삭제, 탐색은 다형성 기능으로 사용이 가능하지만, 데이터를 get 하는 메서드는 보이지 않는다. 왜냐하면 각 컬렉션 자료형 마다 구현하는 자료 구조가 제각각 이기 때문에 최상위 타입으로 조회하기 까다롭기 때문이다.


PPT 9~10 번 참고 및 이해!!

ppt 11
ArrayList의 장/단점
- 배열 기반이라 배열의 장/단점 그대로 가져감(배열의 장단점 숙지)

단점
- **비 순차적** 데이터의 추가,삭제에 많은 시간이 걸림. (왜 비 순차적인지 이해)

 ArrayList의 메소드 add의 입장에서는 인자로 어떤 형태의 값이 올지 알 수 없다. 그렇기 때문에 모든 데이터 타입의 조상인 Object 형식으로 데이터를 받고 있다
 
ppt12
LinkedList는 배열을 쓰는게 아님
- 각 요소가 다음 요소의 링크 정보를 가지며 메모리에 연속적으로 구성될 필요가 없음
1억 번째 요소를 만나려면? -> 1억번의 연산이 필요...
-> Doubly-linked list로 해결 가능 ( 앞에서 또는 뒤에서 데이터 찾아가도록 최적화 됨)
: 그래도 중간에 있는 요소 찾기 힘듬 (순서대로 찾기 힘들다 )

- 데이터 삭제 및 추가 -> 연결만 바꾸면 되니까 효율적. but 여전히 원하는 요소에 찾아가야함

ppt13
ListIterator 쓸때와 안쓸때 차이 이해


ppt14
ArrayList vs LinkedList 정확하게 이해 (바로 바로 떠오를 정도로)

상위 타입을 가지고 놀면 차후 타입을 바꿀때 용이하다.
ex : ArrayList -> LinkedList로 변경할때

> 언제 어떤 자료구조를 써야 좋고 나쁜지 이해하는게 중요


ppt15
자료 삭제시 index 밀리는거 주의
ppt16
forEach 사용시 Collection 크기가 불변해야함

ppt17
이해가 안되었음

ppt19
set 관련 클래스 관계도 참고

ppt20
해시 일종의 키값 (고정된 길이)

ppt21
해시는 물리적인 메모리 용량때문에 압축할 수 밖에 없음
set에서 Sandre Dee가 중복으로 추가되려고 하면 이걸 어떻게 처리하는가? -> 해당하는 해시값 날릴 수 없음 왜? 여러 데이터가 묶여있을 수 있으니까. -> 해시로 접근 후 equals로 엮여있는 데이터 비교
equals는 해시와 재정의하는게 국룰이다... 무슨 의미인지 다시 이해 필요

ppt22
update가 없는 이유는? 위치를 지정하는 것이 없어서.

ppt23
set인데 같은 전화번호 왜 중복 추가 가능하지?
equals 재정의 필요 (비즈니스적 중복 처리하기 위해)

같다? 같은 객체다(같은 reference) , 내용이 같다 (equals + 같은 hash 값)

ppt25
key는 중복 허용 x, value 중복 가능

관련클래스 관계도 확인

ppt27
add와 put이 같기 때문에 데이터 넣을때 덮어쓰지 않도록 주의
putIfAbsent 사용


ppt29
불변 Collection 생성

ppt31
특정 기준에 따라 **요소의 크기를 비교**하여 **내림차순 또는 오름 차순**으로 배치 하는 것
**기준**과 **방향**!!

이 ppt는 다 보는게 좋을 듯
ppt32
Comparable의 조상들만 정렬 가능

ppt33
this.number.compareTo(o.number)를 this,num - o.num 하면 안돼?
this.num = MAX_VALUE , o.num = -1 일 경우 오버플로우 날 수 있잖아.

ppt34
comparator vs comparable

ppt35
1회용인데 class 만들기 부담스러워,,;;
anonymous inner class
Comparator는 interface라 new 못함 -> 왜? 구현부가 없으니까 -> 구현부 정의해주면 inner class 사용 가능

이것도 과해 -> 람다 표현식



클래스에 final 붙은건 뭐지

아래 코드 메소드 숙지
```
this.movieSet.stream().filter(m -> m instanceof SeriesMovie).collect(Collectors.toSet())
```

https://www.youtube.com/live/xrQDFp7vvwg




ppt5
타겟 타입은 abstract 메서드가 반드시 하나만 존재해야함(default, static등은 무관)

boolean equals(Object obj); // 이건 Object가 재정의 해주기 때문에 괜찮

- abstract method가 2개 이상 존재하는 경우 여전히 익명의 inner class 사용

@FunctionalInterface

ppt8
ppt9
너무 무리해서 줄이려고 할 필욘 없지만 읽으면 이해할 수는 있어야함

ppt12
ppt13

ppt18
ppt19

강의에서 작성한 예시 무조건 다시 확인 필요


ppt22
배열 및 Collection의 요소를 하나씩 참조해서 처리하는 목적
- 람다와 내부 반복자를 이용해 요소를 다루는 코드를 간결화

ppt24
ppt25
ppt28

ppt38
이거 대신 ppt39 주로 활용해라


과제 요구사항별로 새로 만들어 제출해보기


mm 월말평가 공지 확인




### 2-1. 자식 → 부모 → 자식으로 캐스팅해도 정보 손실이 없는 이유

java

```java
class Animal {
    void eat() { System.out.println("먹는다"); }
}

class Dog extends Animal {
    void bark() { System.out.println("멍멍"); }
    int loyalty = 10;
}
```

java

```java
Dog dog = new Dog();          // 힙에 Dog 객체 생성 (딱 1개)
Animal animal = dog;          // 업캐스팅 (자식 -> 부모)

// animal.bark(); ❌ 컴파일 에러 (Animal 타입엔 bark()가 없음)

Dog dog2 = (Dog) animal;      // 다운캐스팅 (부모 -> 자식)
dog2.bark();                  // ✅ 정상 동작, "멍멍" 출력
```

**핵심 원리**: 캐스팅은 객체 자체를 바꾸는 게 아니라, 그 객체를 **"어떤 타입의 창문으로 바라보는지"**만 바꾸는 것입니다.

- `Dog` 객체는 힙 메모리에 딱 하나만 생성되어 있고, 캐스팅 과정 내내 그대로 유지됩니다.
- `Animal animal = dog`는 새 객체를 만드는 게 아니라, 같은 객체를 가리키는 **참조 변수의 타입만** `Animal`로 바뀌는 것입니다.
- `animal` 타입일 때는 컴파일러가 `Animal`에 정의된 멤버만 호출하도록 제한할 뿐, `bark()`나 `loyalty`가 객체에서 사라진 것이 아닙니다.
- 다시 `Dog`로 캐스팅하면 그 제한이 풀리면서 원래대로 다 쓸 수 있습니다.

> int → double → int 변환처럼 비트 표현 자체가 바뀌며 값이 손실되는 것과는 완전히 다른 개념입니다. 참조 타입 캐스팅에는 그런 변환 과정 자체가 없습니다.

### 2-2. 객체는 저장될 때 실제 타입 정보도 함께 저장된다

힙에 객체가 생성될 때, 필드값뿐 아니라 **"이 객체의 진짜 타입이 뭔지"에 대한 정보도 객체 안에 함께 저장**됩니다.

```
힙(Heap) 메모리                          메서드 영역
┌─────────────────────┐                ┌──────────────────────┐
│  Dog 객체            │                │  Dog.class 메타데이터  │
│  ┌────────────────┐ │   클래스 포인터   │  실제 타입: Dog        │
│  │ 객체 헤더        │─┼───────────────>│  메서드 목록(vtable)   │
│  │ (클래스 포인터)  │ │                │                       │
│  └────────────────┘ │                └──────────────────────┘
│  ┌────────────────┐ │
│  │ 인스턴스 필드    │ │
│  │ loyalty = 10   │ │
│  └────────────────┘ │
└─────────────────────┘
```

- 모든 객체는 앞부분에 **객체 헤더(Object Header)** 를 가지며, 여기에 담긴 클래스 포인터가 메서드 영역의 `Dog` 클래스 메타데이터(실제 타입 정보 + 메서드 테이블)를 가리킵니다.
- 참조 변수의 타입을 바꾸는 것은 **컴파일 시점**에 어떤 멤버를 호출할 수 있는지를 제한하는 것일 뿐, 객체의 헤더나 클래스 포인터는 전혀 건드리지 않습니다.
- `(Dog) animal`처럼 다운캐스팅할 때, JVM은 이 클래스 포인터를 보고 "이 객체가 진짜 `Dog`가 맞는지"를 **런타임**에 확인합니다. 맞으면 성공, 아니면 `ClassCastException`이 발생합니다.

### 2-3. 이 구조 덕분에 가능한 것들

1. **`instanceof` 검사** — 런타임에 실제 타입을 조회해서 판단
2. **오버라이딩된 메서드의 동적 바인딩** — 참조 변수 타입이 아니라 객체의 **실제 타입**을 기준으로 오버라이딩된 메서드가 호출됨
3. **`getClass()`** — 어떤 참조 타입으로 캐스팅해서 보든 `dog.getClass()`는 항상 `Dog.class` 반환

### 2-4. 주의할 점 — 다운캐스팅과 필드 섀도잉

**다운캐스팅은 실제 타입이 맞아야 함**

java

```java
Animal animal2 = new Animal();  // 진짜 Animal 객체
Dog dog3 = (Dog) animal2;       // 💥 ClassCastException!
```

`animal2`가 가리키는 실제 객체가 `Dog`가 아니라 순수 `Animal`이라면 런타임에 예외가 발생합니다. 보통 `instanceof`로 먼저 확인 후 캐스팅합니다.

java

```java
if (animal instanceof Dog d) {  // Java 16+ 패턴 매칭
    d.bark();
}
```

**멤버 변수(필드)는 메서드와 다르게 동작함**

java

```java
class Parent { String name = "parent"; }
class Child extends Parent { String name = "child"; }

Child c = new Child();
Parent p = c;
System.out.println(p.name);   // "parent" 출력!
System.out.println(c.name);   // "child" 출력
```

- **메서드**는 오버라이딩되면 항상 실제 객체(런타임 타입)의 메서드가 호출됨 (동적 바인딩)
- **필드**는 참조 변수의 타입(컴파일 타임 타입)을 기준으로 어떤 필드에 접근할지 결정됨 (정적 바인딩)

객체 안에는 사실 두 필드가 별개로 존재하며, 자식 필드가 부모 필드를 덮어쓰는 것이 아닙니다. 그래서 다시 `Child`로 캐스팅하면 `c.name`은 여전히 "child"로 정상 출력됩니다.



배열은 공변, 제네릭은 무공변
Dog가 Animal의 하위타입이면 Dog[]도 Animal의 하위타입으로 취급함
```java
Dog[] dogs = new Dog[3];
Animal[] animals = dogs;   // ✅ 컴파일 OK! Dog[]를 Animal[] 변수에 대입 가능
```
그러나 제네릭은 무공변
```java
List<Dog> dogList = new ArrayList<>();
List<Animal> animalList = dogList;   // ❌ 컴파일 에러!
```



Inner Class
보통 두 클래스가 서로 긴밀하게 관계가 있거나, 하나의 클래스 또는 메소드에서만 사용되는 클래스일 때 이용되는 기법
1. 클래스를 논리적으로 그룹화
   클래스가 여러 클래스와 관계를 맺지 않고 **하나의 특정 클래스와만 관계**를 맺는다면, 외부에 클래스를 새로 작성하는 것이 아닌 내부 클래스로 작성할 수 있다.
2. 더욱 타이트한 캡슐화의 적용
   private를 사용함으로 외부에서의 접근을 차단하면서. 내부 클래스에서 외부 클래스의 멤버들을 제약 없이 쉽게 접근할 수 있다.
   Q : 내부 클래스의 private 멤버를 어떻게 이 내부 클래스를 감싸는 외부 클래스에서 사용 가능한가?
   A : 자바에서 private는 클래스 몸통 단위가 아니라 최상위 클래스 단위로 적용이 된다.
   자바 설계자들이 의도적으로 만든 규칙이에요. 내부 클래스는 원래 "외부 클래스와 긴밀하게 협력하는 하나의 논리적 단위"로 설계된 것
3. 가독성이 좋고 유지 관리가 쉬운 코드
instance class
주의. 인스턴스 클래스 내부에는 instance 멤버만 선언할 수 있다. (static 멤버는 선언 불가)
외부 클래스의 멤버로 취급되기 때문에 외부 클래스의 객체 먼저 생성한 후 내부 클래스의 객체를 생성이 가능하다

static class
주의 . static이라고 해서 new 생성자 초기화를 못 하는것이 아니다.
static 클래스 내부에는 instance 멤버와 static 멤버 모두 선언 할 수 있다.
일반적인 static 메서드와 동일하게 외부 클래스의 인스턴스 멤버에는 접근이 불가하고, 정적(static) 멤버에만 접근할 수 있다. 
스태틱 내부 클래스의 인스턴스는 외부 클래스를 먼저 생성하지 않아도 된다

local class
접근제한자와 static을 붙일 수 없다.
메소드 내부에서만 사용되므로 접근을 제한할 필요가 없고, 원래 메소드 내에는 static 멤버를 선언할 수 없기 때문
메서드내의 로컬 클래스에서 메서드의 지역 변수에 접근해서 값을 사용하려고 할때 반드시 final 상수화 된 지역 변수만 사용이 가능하다. -> 지역 변수에명시적으로 `final`을 안 붙여도, 그 지역변수가 재할당되지 않는 한(effectively final) 마치 `final`인 것처럼 취급하고 강제한다 다만 로컬 클래스에서 값이 바뀌는 라인이 있으면 더 이상 final 상수가 아닌 변수로 취급되기에 접근이 불가능해진다.


anonymous class
익명 클래스는 생성자가 존재하지 않는다
클래스의 선언과 동시에 객체를 생성
익명 클래스는 기존에 존재하는 클래스를 메서드 내에서 일회용으로 클래스 내부 구성을 선언하여 필요한 메서드를 재정의 하여 사용하는 기법이다

각 inner class 종류는 언제 사용이 되는거지?



**Enum** is short for "enumerations", which means "specifically listed".


for-each는 내부적으로 `Iterator`를 사용하는데, 반복 도중에 리스트의 **구조(크기)를 변경**하면 `ConcurrentModificationException`이 터집니다.


FileOutputStream("person.json");
- "person.json" 파일을 열고 byte 단위로만 쓸 수 있는 통로를 만든다.
FileInputStream
- 파일에서 바이트를 읽어오는 가장 안쪽 통로

OutputStreamWriter(byte);
- 문자를 byte로 인코딩
InputStreamReader
- 읽어온 바이트를 다시 문자로 디코딩

BufferedWriter
- 한글자씩 시스템 왔다갔다 하지 않고 한번에 읽어 버퍼에 모아두고 어느정도 차거나, flush,close 될때 한번에 쓰기 실행/
BufferedReader
- 한글자씩 읽지 않고 버퍼 단위로 미리 읽어와서 속도 향상, 




멀티쓰레드의 장단점
단점 : 
context switching 비용 발생
관리가 어려움
동기화 문제
데드락

Runnable : Thread가 어떤 작업을 할지 적혀있는 거

start() , run()


run은 실제로 준비된 스레드 실행시킴
run은 JVM이 동작시킴. 언제 동작시켜? 그건 OS 스케쥴러가 알아서
main()에서 호출하는 run 메소드는 thread를 동작시키는게 아니라 단순히 메서드를 실행하는거


만약 main 메소드에서 run메소드 호출하면? 
새로운 스택 안만들고 같은 스택에서 run 메소드 실행함. (순차적으로 진행되기 때문에 싱글스레드처럼 동작함)


쓰레드 상태와 생명주기
쓰레드에 우선순위를 줄 수는 있지만 실제로 보장되지는 않음

쓰레드의 상태
NEW
스레드 객체 생성 후 아직 start 호출되지 않은상태
RUNNABLE
JVM 선택에 의해 실행 가능한 상태
TERMINATE
run메소드의 종료로 소명된 상태
WAITING
sleep,wait,join 등에 의해 정해진 시간 없이 대기중인 상태
TIMED_WAITING
sleep,wait,join 등에 의해 정해진 시간 동안 대기중인 상태
BLOCKED
사용하려는 객체의 모니터 락이 풀릴때 까지 기다리는 상태


쓰레드의 메소드
sleep()
이 메소드를 호출한 쓰레드는 waiting pool에서 대기
정해진 시간이 있으니까 TIMED_WAITING 상태
1초씩 프린트 찍어도 왜 정확하지 않을까?
context switching

join()
다른 스레드의 작업이 종료될 때 까지 대기 풀에서 대기
쓰레드.join()을 호출하면 이를 호출한 쓰레드가 join붙은 쓰레드 끝날때까지 대기

yield()
실행 중 동일한 우선순위를 가진 다른 Thread에게 실행을 양보하는거

interrupt()
waiting pool에서 대기중인 thread를 깨워서 runnable로 이동시킴

thread의 종료
기본적으론 run() 메소드가 끝나면 자동 종료
looping중인 thread를 종료하려면 interrupt상황 파악해서 안정적인 종료 유도
한번 쓰레드 종료되면 다시 start할 수 없음 그냥 버려지는거


Daemon Thread
- 보조 쓰레드 (ex : garbage collector, 파일 자동 저장 등등)
일반 쓰레드가 종료되는 순간 강제적으로 자동 종료

일반적으로 무한 루프를 이용해 run 이후 대기 상태있다가 조건에 따라 실행

멀티쓰레드의 공유와 동기화 문제
lock을 이용한 동시 접근 방지

synchronized 키워드
작업과 관련된 공유 영역에 lock 점검 필요 표시
mutex, 한번에 하나의 스레드만 동작할 수 있게 함

쓰레드의 비 효율성
쓰레드가 어차피 동작 못할 상황이면 아예 들어가지도 못하게 wait() 호출
이후 조건이 다시 되면 notifiy() 또는 notifiyAll() 호출

wait(), notifiy(), notifyAll()
Object에 정의된 메서드들로 동기화블록(synchronized) 내에서만 사용 가능


Thread Pool
병렬작업 증가로 1회성 스레드 증가 -> 스레드 생성, 스케쥴링 비용 증가 -> 애플리케이션 성능 저하
Thread Pool
- 작업 처리에 사용되는 스레드의 개수 제한 해두고 작업 큐에 들어오는 작업들 하나씩 스레드가 처리
- 작업 처리가 끝난 스레드는 다시 작업 큐에서 새로운 작업을 가져와 처리
즉, **작업**을 만들고 스레드 풀의 스레드들이 처리하는것


runnable, callable

completableFuture

virtual thread

스레드는 본래 OS의 자산이고 자바에선 OS의 스레드와 1:1 통신하는게 Thread 객체임
즉, 스레드 주도권은 OS에게 있고 자바에서 Thread로 무언가 작업을 하려면 OS와 통신 필요

virtual thread와 기존 thread의 각각 장단점 비교


강사가 강의할때 어떤 기술이 어떤 배경으로 등장하였는지를 **직접 느끼게** 한 후 설명을 하네





하네스 엔지니어링 수업 프로젝트 복기
instruction : 각 에이전트별 역할 분담 및 역할 제한
skill : 반복되는것 넣어놓고 재사용하겠다


파일 입출력

기본 상대경로는 java program을 실행하는 위치 (현재 실행중인 파일의 루트가 아님)
그래서 경로를 헷갈리지 않게 잘 잡아줘야한다.
절대경로
상대경로 
- 특정 클래스 기준으로 파일을 찾을 때
new File(B_UseFileText.class.getResource("...").getFile(); // 이 방법이 좋음
chap13 코드 이해 필요

** Serialization ** // 시험에 무조건 나옴
implements serializeale
객체를 파일 등에 저장하거나 네트워크로 전송하기 위해 연속적인 데이터로 변환하는것
transient 직렬화 제외
static 도 직렬화 제외. 왜? 객체를 직렬화 하는 것이기 때문에

직접 파일을 입/출력해보면서 실습해보자

예전에나 데이터 보낼때 직렬화 해서 보냈지 요새는 JSON으로 함

SerialVersinoUID
: 클래스의 변경 여부를 파악하기 위한 유일키
직렬화 할때와 역직렬화 할때의 UID가 다르면 에러남
직렬화 UID 설정 안했으면 컴파일러가 자동 생성함. 변경 있을 때 마다 UID 바꿔줌
serialVersionUID를 명시적으로 설정해주면 UID 고정된 상태로 사용 가능함 (클래스에 변경 있어도 UID 안바뀜)

어쨌든 JSON으로 하는게 더 좋음

Scanner와 BufferedReader
Scanner - 자동 형 변환 지원, 간편하지만 속도 느림
BufferedReader - 직접 스트림 구성해야하는 등 번거롭지만 속도가 빠름


알고리즘 학습법


문제를 바라보는 관점
문제의 흐름을 먼저 떠올리고 그에 맞는 알고리즘과 자료구조를 연결하는 능력

1. 
지문이 긴 문제 -> 필요한 정보로만 다시 정의
처음부터 모든 지문을 읽지 말고 빠르게 흝어보며 풀이에 필요한 정보를 찾아보기
문제 흝어볼 때 문제를 구성하는 데이터와 조건 중심으로 아래 내용 확인해보자
- 어떤 데이터가 주어지는가?
- 각 데이터는 무엇을 의미하는가?
- 값 사이에는 어떤 관계가 있는가?
- 어떤 조건에서 상태가 변하는가?
- 어떤 연산이 반복되는가?
- 최종적으로 구해야 하는 결과값은 무엇인가?
위 정보들로 문제의 구조 재정의하자.
-> 이로부터 그래프인지, 배열인지, 좌표 문제인지 구분할 수도 있고 어떤 값을 저장하거나 상태로 관리해야 하는지도 대략 유추 가능
> 문제의 뼈대 세우기

그 다음 두번째 읽을때 세부 조건과 놓친 조건 채워 넣기
스토리와 핵심 조건을 먼저 분리하고, 풀이에 필요한 구조를 세운 다음 다시 원문으로 돌아가 검증하는 과정에 가깝습니다.

2. 시뮬레이션을 통해 풀이 흐름을 파악
문제를 단순한 구조로 정의한 후 시뮬레이션을 통해 예제를 직접 손으로 굴려볼 차례
정답 자체가 아니라 정답이 만들어지는 과정에 집중하는 것
머리 말고 직접 종이에 적어보는 것 추천

예시
그래프 문제라면 : 어느 정점에서 출발해 어떤 순서로 정점을 방문하는 지 확인
재귀 문제라면 : 현재 값이 다음 호출의 매개변수로 어떻게 전달 되는지, 어떤 조건에서 멈추고 결과가 어떤 순서로 돌아오는지 직접 적어보기

만약 풀이가 잘 안보이면 그냥 AI한테 바로 물어보는게 효율적. 다만 바로 정답 코드를 주기 보단, 앞서 정리한 내용을 바탕으로 막힌 지점을 설명하는것이 좋다.
예시
이거 처음에는 그리디인가 싶었는데, 예제를 굴려보니까 DP 같아. 이전 값을 재사용해야 하는 건 알겠는데...... 그래서 뭘 저장해야 하지? 어떤 값을 관리해야 할지 잘 모르겠다. 힌트 조금만 줘봐.
-> 이후 복기, 내가 어떤 조건에서 해당 접근을 떠올렸는지, 어떤 값을 관리해야했는지, 처음 생각한 흐름과 무엇이 달랐는지 문서화 : 그래야 이 접근이 나온 이유를 기억할 수 있고, 조건이 조금 달라진 문제를 만났을 때도 활용 가능

3. 흐름을 구현할 수 있는 재료를 수집한다

현재 어떤 값을 알고 있어야 하는가?
다음 동작을 결정하기 위해 어떤 상태가 필요한가?
처리 순서를 보장해야 하는가?
같은 계산이 반복되는가?
한 번 확인한 대상을 다시 방문해야 하는가?
지금 처리하지 못한 값을 나중에 다시 확인해야 하는가?

위와 같은 질문에 답하다보면 어떤 상태를 관리해야 하고 어떤 연산이 필요한지 조금씩 보이기 시작함
그럼 그 흐름을 실제로 구현할 수 있는 알고리즘과 자료구조로 연결해야한다.

예시
먼저 들어온 값을 순서대로 처리해야 한다면 Queue, 가장 작은 값을 반복해서 꺼내야 한다면 PriorityQueue, 같은 계산이 반복된다면 DP를 생각해볼 수 있습니다.

note
문제를 읽자마자 알고리즘의 이름부터 떠올리는 것이 아니라 풀이의 흐름에서 필요한 상태와 연산을 찾고 이를 제공하는 알고리즘과 자료구조로 연결해야 한다.

비슷한 형태의 문제라도 조건이나 값의 범위, 관리해야 하는 상태에 따라 필요한 재료가 달라질 수 있습니다.

따라서 알고리즘을 먼저 정해두고 문제를 끼워 맞추기보다는, 흐름에서 필요한 동작을 먼저 찾는 편이 더 안정적입니다.


정답 풀이에 적혀 있으니까 그대로 사용하는 것이 아니라, 해당 재료가 어떤 상태를 관리하고 어떤 연산을 처리했는지를 풀이의 흐름과 함께 살펴보는 것이죠.

이를 위해 하나의 문제를 두고 다음과 같은 질문을 던져봅니다.

"이 재료 대신 다른 재료를 사용하면 어떻게 될까?"

"현재 흐름을 다른 방식으로 구현할 수는 없을까?"

예를 들어 Set으로 관리한 상태를 배열로 바꾸거나, 재귀 탐색을 반복문과 스택으로 구현해보면서 기존 풀이와 무엇이 달라지는지 비교해볼 수 있습니다.

이때 반드시 더 좋은 풀이를 만들어야 하는 것은 아닙니다. 직접 바꿔보니 코드가 더 복잡해지거나 성능이 나빠질 수도 있고, 오히려 기존 풀이가 가장 단순하고 적절했다는 사실을 확인할 수도 있습니다.

중요한 것은 결과보다 그 과정에서 차이가 발생한 이유를 살펴보는 것입니다.

처음에는 다른 방법을 떠올리는 것 자체가 어려울 수 있습니다. 그럴 때는 다른 사람의 풀이 또는 AI의 답변을 참고하면서, 내가 사용한 재료와 무엇이 다른지부터 비교해봐도 좋습니다. 그리고 단순히 새로운 풀이를 확인하는 데서 끝내지 않고, 왜 그런 재료를 선택했는지까지 살펴보는 것입니다.

4. 시간 복잡도와 공간 복잡도를 계산한다.
note.
현재 풀이가 제한 조건을 충분히 통과한다면, 약간의 성능 차이보다는 자신의 사고 과정과 풀이의 흐름이 명확하게 드러나는 코드를 작성하는 편이 좋다고 생각합니다. 성능을 높이기 위해 코드가 지나치게 복잡해지면 디버깅이 어려워지고, 풀이의 흐름을 이해하거나 설명하기도 힘들어지기 때문입니다.
풀이를 완성한 뒤 시간적인 여유가 있다면, 자신의 사고 과정과 풀이의 흐름이 더 명확하게 드러나도록 코드를 정리해보는 것도 좋은 것 같습니다.

5. 정리한 흐름을 코드로 구현

진짜 문제는 수정이 계속 쌓이면서 코드가 난잡해지고, 처음 생각했던 흐름조차 설명하기 어려워지는 경우입니다.

하나를 고치기 위해 새로운 조건을 추가하고, 그 조건 때문에 또 다른 예외 처리가 필요해진다면 처음 설계부터 잘못되었을 가능성이 큽니다.
이럴 때는 기존 코드를 억지로 살리려고 하지 말고, 과감하게 초기화한 뒤 처음부터 다시 작성하는 편이 좋습니다.
코드를 구현할 때는 용도별로 함수를 분리하는 것을 추천합니다.

https://dwinging.github.io/Algorithm_webbook
chapter 2,3으로 실제 적용 예시 확인

1. 문제를 읽었는데 무슨 말인지 모르겠다?
   문해력의 문제. 책 많이 읽고 문제 많이 읽어봐라
2. 문제는 이해했는데 뭘 써야할지 모르겠다
    이론이 명확히 이해되지 않은 것 (자료구조, 알고리즘에 대해서 설명할 수 있을 정도로 명확하게 이해해야 한다)
3. 문제도 이해 했고 뭘 써야할지도 알겠는데 어떻게 구현해야할지 모르겠다.
    문제를 많이 풀어봐라



I/O Stream
stream은 단방향으로만 통신 가능하고 하나의 스트림으로 입력과 출력 같이 처리 할 수 없음
데이터의 타입에 따라 stream naming이 다르다.
byte : XXStream
char : XXer

read(byte[])는 **"배열을 초기화하고 다시 채우는 함수"가 아니라 "배열의 앞부분 n칸만 덮어쓰는 함수"**입니다. 그래서 반환값(readCount)이 곧 "이번에 진짜로 신뢰할 수 있는 데이터의 범위"를 알려주는 유일한 정보이고, 이 값 없이 배열 전체를 쓰면 이전 루프의 잔재가 섞여 나올 위험이 있는 겁니다.

FileInputStream을 만들면 자바 내부적으로 운영체제의 파일 핸들을 하나 붙잡고 있는 셈. 이걸 다 쓰고도 닫지 않으면 다른 프로그램이 그 파일을 못 열 수도 있고 운영체제가 너무 많은 파일 핸들을 쥐고 있어서 Too many open files 에러가 날 수도 있다.
다 썼으면 close() 해줘라

그런데 자바 7이후부턴 try-with-resources가 도입되어서 try()괄호 안에 Stream 생성하면 블록이 끝날 때 자바가 자동으로 close 호출해준다.
```
try(FileInputStream fis = new FileInputStream("data.txt")){
    int data;
    while((data = fis.read()) != -1 ){
        System.out.println((char) byte);
    }
catch(IOException e){
    e.printStackTrace();
}

```

UTF-8은 한 글자당 3바이트,  read(buffer)로 읽으면 한 글자가 잘려서 버퍼에 담길 수가 있음. 그러면 깨짐
그래서 문자 읽을 땐 Reader 사용

```
private void read3(){
    char[] buffer = new char[10]; // buffer의 타입이 byte가 아니라 char임을 주의
    try(Reader input = new CharArrayReader(data2.toCharArray())){
        int read = -1;
        while((read = input.read(buffer)) > 0){
            System.out.println("읽은 개수: %d, 문자열 %s%n",read, new String(buffer,0,read));
        }
    }catch(IOException e){
        e.printStackTrace();
    }
}
```
그런데 char는 2byte이고 UTF-8의 한글은 3byte인데 여전히 짤리는거 아니야?
InputStreamReader는 파일에서 바이트를 읽어올 때, 그것을 곧바로 char 배열에 던져 넣지 않고 내부에 작은 임시 저장 공간(디코더 버퍼)를 따로 가지고 있어서 아직 글자가 완성이 안되었다고 싶으면 계속 들고 있다가 완전한 글자가 될 때만 그걸 char로 변환해준다. 즉, buffer에 값이 들어가는 시점에 이미 인코딩,디코딩이 마무리 된 상태.

프로그래머스 기초문제 이번주 안에 최대한 많이 풀기 (자바)


20260804

AI 수업

파이썬 기본 문법

데이터 사진 20장 (jpg,png 50mb 이하) 제출 필

파이썬 lab의 참고,실습 내용도 확인

도커로 어떻게 로커 파일 띄운거지?


numpy, pandas의 메소드들을 외우는게 중요한게 아니라 "어떤 데이터"가 필요한가 "어떻게 보여져야 하는가" 에 대해서 알아야 한다. 메소드는 언제든지 확인할 수 있음



2026.08.05

income : 라벨(반응/목표) -> Y로 표기


아래 개념을 명확하게 알아야 한다

가설 공간
:
관계를 표현할 수 있는 모든 후보 함수들의 모음
피쳐 공간과 라벨 공간위에서 정의된 함수들의 집합

모델
: 
가설 공간에 속한 특정 함수

학습
: 
주어진 데이터에서 정답을 가장 잘 맞출 수 있도록 모델의 규칙을 조금씩 조정해가는 과정

학습과정
1. 가설 공간에서 하나의 함수를 선택
2. 그 함수로 데이터의 모든 예시를 예측
3. 손실함수로 틀린 정도 계산
4. 더 적게 틀리도록 함수의 파라미터 조정
5. 반복하여 최적의 모델 완성


오차범위를 줄이는게 최종 목적이 아니라, 이후에 새로운 데이터가 왔을때도 오차범위를 최소화 할 수 있는게 더 좋음 (참 함수에 근접하도록) (ppt 37)


측정오차 : 피처 X와 무관함. 관계없음


왜 f( )를 학습하는가?
잘 학습된 f가 있으면 새로운 입력 X= x에서 반응/목표 Y를 예측할 수 있음

중요 특성 파악
피쳐들  X 중에서 어떤 특성이 Y를 설명하는데 중요하고, 어떤 것은 덜 중요(무관)한지 알 수 있음

지도학습이란?

회귀
output이 숫자인걸 regression이라고 함
분류
output이 범주인걸 classification이라고 함

손실함수란?
모델의 예측이 정답에서 얼마나 벗어났는지를 숫자로 나타내는 함수


ppt67
실제값 - 예측값의 오차와 참함수에서 있는 앱실론과는 다른것


결정계수
설명력


분류정확도
Label이 범주로 되어있기 때문에 MSE나 R^2를 사용하기 어렵다

indicator 함수

정확도가 99%이면 항상 좋은 모델일까?
- 불균형 데이터(양성 1%, 음성 99%)에서는 전부 음성이라고 찍어도 정확도가 99%일 수있음

즉, 정확도는 단순하게 전체 중 맞춘 비율만 계산함
따라서 정확도만 보지 말고 다른 지표(혼동행렬)도 함께 봐야 안전하다.

혼동행렬
예측-실제 값 사이의 관계를 행렬 형태로 표현
TP : 둘 다 양성
TN : 실제 음성, 예측도 음성
FP(falsly positive) : 실제 음성, 예측은 양성 (오탐)
FN(falsyl negative) : 실제 양성, 예측은 음성 (누락)

정밀도
- 양성이라 판정한 것 중 진짜 양성의 비율   = TP/(TP + FP)
재현율
- 진짜 양성 가운데 잡아낸 예측 양성 비율 = TP/(TP+FN)

둘 다 높으면 좋음
F1-score
정밀도와 재현율의 조화 평균
클 수록 좋음


교차 엔트로피
손실 = $-log(정답에 준 확률)$

- 정답에 준 확률이 높을 수록 손실이 작아짐
- 정답에 준 확률이 낮으면 손실이 급격히 커짐

학습의 목적
:
훈련 데이터에서 성능이 아무리 좋아도, 새로운 데이터에서 성능이 떨어지면 실전에 사용할 수 없음
- 일반화 오류의 최소화 지향
즉, 새로운 데이터에서 틀리는 것을 최대한 줄여야한다.
> 맞춘다기 보단 틀리는걸 줄인다... 

시험 볼 때 맞춘다는 관점 말고 틀리지 않는 다는 관점?

overfitting
- 훈련 데이터에서 **우연한 패턴/잡음까지 외워버려서** 훈련에는 잘 맞지만 테스트에선 성능이 나빠지는 현상
- 현상 : 훈련 오류 급격히 낮음, 테스트 오류 높음/요동

오버피팅이 왜 안좋나?
- 표본(sample)의존 불안정 : 훈련 데이터는 모집단의 일부 표본이라 잡음이 있음/. 이것만 과하게 맞추어 학습하면 샘플 몇개만 바뀌어도 예측이 크게 흔들림
- 일반화 실패 : 보지 못한 데이터(테스트) 오류가 커짐


오버피팅에 대한 오해 ( 중요 )
오버피팅 $!=$ 분표 변화로 인한 에러 증가

현상을 보고 오버피팅이라고 하면 안됨
훈련 데이터 분포와 실제 테스트 분포가 다름. 분포 변화로 인한 에러 증가는 모델이 과적합하지 않아도 발생 가능


오버 피팅 vs 언더 피팅
오버 피팅 : 모델이 너무 복잡 -> 잡음까지 학습
언더 피팅 : 모델이 단순하거나 학습이 완료되지 않음 -> 중요한 패턴을 놓침

해결 실말
- 더 많은 데이터, 테스트 데이터를 활용한

훈련 오류 vs 테스트 오류
훈련 오류 : 모델을 학습시킨 같은 데이터에 다시 적용해 계산한 오류
테스트 오류 : 학습에 쓰지 않은 새 관측치에 대해 모델을 적용했을 때의 평균 예ㅊ측 오류

훈련 오류는 계속 감소
테스트 오류는 U자형

목적 : 테스트 오류의 U자형의 바닥이 되도록 하는 적절한 모델 찾기
- 과적합 피하기 ( 너무 많이 답을 외우지도 않게 )
- 너무 대충 공부하지 도 않게 (과소 적합)


테스트 성능 평가

테스트 예측 오류 계산
- 이상적 케이스 : 충분히 큰 별도 테스트 데이터셋 -> 현실에서 구하기 힘듬
- 현실에서는 테스트만을 위한 데이터를 갖기에 데이터가 부족할 수 있음
그러면 어떻게 해야할까

대안
재표본화 (resampling)
:
데이터를 나눠 여러 번 "훈련->평가"를 반복해 테스트 오류를 가늠

hold-out(검증셋) 방법 
- 가용 샘플들을 훈련셋과 검증셋으로 나눔
훈련셋으로 모델 적합, 검증셋으로 예측 후 검증 오류 계산
검증 오류는 보통 정량 반응은 (MSE) , 범

데이터 순서 무작위 셔플링 후 두 부분으로 분할
학습은 훈련셋에서, 성능평가는 검증셋에서 수행


K겹 교차검증
- 테스트 오류 추정의 표준적 접근

- 데이터를 먼저 셔플링
- 총 n개의 데이터를 **겹치지 않는** K개 그룹으로 분할
- 각 그룹이 번갈아 검증셋, 나머지는 훈련셋

10fold crossvariation이면 충분하다.

비지도학습
:
레이블(정답) 없이 데이터의 구조,패턴 집단(잠재 서브그룹)을 찾아내는 학습
대표과제
: 군집화, 차원 축소 , 밀도추정/이상치 탐지 등

출력: 정답 예측이 아니라 구조/요약/표현

지도학습 vs 비지도학습

클러스터링
:
데이터 안에서 하위집단(클러스터)를 찾는 기법들을 총칭

집단 내부는 서로 유사, 집단간은 상이하도록 데이터 분할

두 가지의 대표적인 클러스터링 방법
K평균 : K(클러스터 수)를 미리 정해 분할
계층적 군집 : K를 사전에 고정하지 않음

분류 vs 클러스터
??
분류는 정답이 있음
클러스터는 정답이 없음

k평균
- 각 관측치는 적어도 하나의 군집에 속함 ( 하나라도 빼먹으면 안됨 )
- 하나의 데이터가 여러 그룹에 동시에 속할 수 없음

좋은 군집화란?
- 클러스터 내부 변동이 작은 분할
목표 : 클러스터 거리,차이의 합이 최소가 되도록 데이터를 나눈것

1. 초기화 : 무작위로 1~K 클러스터를 임시 부여
2. 반복(할당이 더 이상 바뀌지 않을 때 까지)
	1. 각 클러스터의 중심 계산
	2. 각 관측치를 가장 가까운 중심의 클러스터에 재할당


계층적군집
K-means는 클러스터 수 K를 미리 지정해야 하는 단점이 있음
그 단점을 극복하기 위한 방법이 계층적군집

K 를 고정하지 않고 전체 구조를 덴드로그램으로 제공
덴드로그램에서 수평선 높이(거리)를 기준으로 가위질하며 K개의 군집을 얻음

계층적군집은
클러스터링의 뭉침 과정
첫 단계에선 각각의 포인트들이 하나의 클러스터임

계층적 군집의 링크 유형
- 최소 거리 링크 : 쌍별 거리 중 최소값을 군집 간 거리로
- 최대 거리 링크 : 쌍별 거리 중 최대값을 군집 간 거리로
- 평균 거리 링크 : 쌍별 거리의 평균을 군집 간 거리로

클러스터링시 주의점
스케일링 : 표준화(평균0, 표준편차 1로 입력 변수 변환)가 필요한가?
-> yes 변수 단위 차이 영향 큼

몇 개의 클러스터가 적합한지?
-> K-means 계층적 모두 어려운 문제, 합의된 정답 없음

그래서 단일 시도가 아닌 여러번 시도 권장됨...





최소 제곱법
잔차 제곱의 합의 최소인 선을 찾는것

계수를 측정하기 위한 공식
잔체 제곱을 최소화 할 수 있는 공식
기울기,절편 공식 외울 필요 없음. 

std.Error
데이터가 바뀌면 기울기 혹은 절편이 바뀔 수 있는 정도.
t-statistic
$Coefficient/Std.Error$
결국 중요하게 보면 될 건 Coefficient, p-Value

p-value
$B_1=0$일 때 관측값만큼( 그이상)극단적일 확률
.. 뭔 말이지?
p-value가 낮을 수록 coefficient 값이 유의미하다... 음.. 뭔 말이지 모르겠다.
선형성이 크다 작다는 p-value만 보면된다, coefficient만 보면 안된다?..

다중선형회귀
공식
$Y=B_0X_0 + B_1X_1$ ... 각각의 독립변수(feature)에 해당하는 기울기가 있따.
데이터와 가장 가까운 평면을 찾는 문제

.. 이거 인강 다시보기하면서 봐야 이해할 듯

다중선형회귀 계수 추정 유도(행렬표현)
수학은 더 깊은 이해를 위한 것.. 공식 이해하려고 해봐라

다중선형회귀 결과를 해석할 줄 알아야한다. (상관관계가 유의미한지 아닌지, coefficient, p-value)

$R^2$ 높을 수록, 1에 가까울 수록 좋음

선형회귀 주의사항

선형회귀를 통해 대답할 수 있는 질문들
- 이상적 상황 : 변수들이 연관(correlation)되지 않고, 독립적일 때 -> 계수 해석 명확
- 문제 상황 : 변수들이 연관되어 있다면 -> 계수 추정이 불안정해지고 해석에 혼동이 발생할 수 있음
- 주의 : 데이터의 상관관계를 인과관계라고 오해하면 안됨... (ex :아이스크림 소비량 vs 상어에 물리는 사건)

위까지 공부한 내용 요약
선형회귀 개요
- 지도학습의 가장 기초적인 방법
- 입력과 출력의 선형 관계를 학습
단순선형회귀
- 모형 : $Y = B_0 + B_1X + e$
- 의미 : $B_0$ : 절편, $B_1$ : 기울기, $e$ : 측정오차
- 학습 방법 : 최소제곱법 (RSS 최소화)
- 사례 : TV 광고비와 매출 관계


분류란?
실제 범주를 구하는것 보다 범주에 속할 확률 구하는게 유용할떄가 있음

분류 문제에 선형회귀를 써도 될까?
| 선형회귀는 분류 문제에 사용하기에 부적절함 : 이진 분류 문제
선형회귀는 예측 확률이 0보다 작거나 1보다 크게 예측이 될 수 있음.. 말이 안되기 때문에 부적절함
-> 그래서 로지스틱 회귀를 사용

시그모이드 함수는 무조건 0~1 사이에 있음

시그모이드 함수의 z에  $z = B_0 + B_1x$식을 대입하면 .. (이 식만 이해하면 됨)
| 단순 시그모이드에 선형함수로 덮어서 함수를 만든거

선형회귀와 비슷하다.

로지스틱 회귀 모형 vs 선형 모형 (이해하길 추천)






6시

7시~8시 이동

8시~9시 0.5


6시 끝
6~7시30분 운동
7시30분~8시30분 이동
8시30분 ~ 9시30분 저녁 (요리하려니까 시간 아깝네)

9시30분~12시 (복습) -2.5





>[!note]+
>싸피 프로젝트는 못 씀.
>이력서에 넣을 프로젝트 1~2개는 있었다. 
>코테 준비는 되어있었다. 
>싸피 하면서 배운것 : 비슷한 사람들과 모여서 배우고 도움을 줄 수 있었던것이 좋았다.
>기술적인 이해도 향상한 것(같은 것을 또 보면서) _ 내실을 다졌다


토스 선배와의 대화

토스에서 AI 관련
- 개인적으로 코덱스 많이 쓴다고 함
- 대부분의 모델 다 사용 가능
- AI로 코드작성 -> AI로 코드 리뷰(skill 사용) -> PR 올림 -> 사내 봇이 검토
- AI가 구현을 다 해주는데 그래도 뭘 하는지 알고 쓰는게 좋다.
- AI가 짠 코드 검토를 할 때 실력 차이가 나타난다.

취업 준비 관련
- AI 잘 쓴다는 어필하면 좋지만 의존 한다는 안됨
- 코테는 싸피에서 하는 수준만 해도 대부분 충족 ( 단, 서비스 기업은 열심히 하는게 좋다)
- 기술 서적을 많이 본 이유 (실제 프로젝트를 하면서 문제 해결하면서 지식을 익히는게 제일 좋음 그런데 그런 기회를 스스로 만들기 어려워서 보완 차원으로 읽음)
- 이 쯤 이면 취업할 만 한 것 같은데? 생각이 들 때 취직이 되었다.
- 취업 준비는 단순히 수단으로 바라봄. 설득력 있어보이는 것들 준비함
- 삼성 면접 떨어진 것 복기해보니까 너무 희망하는 포지션을 단언한것이 패착. 그 회사에서 그 직무를 안 뽑을 수도 있음
- 별거 없어보이는 프로젝트로도 잘 취업할 수 있음(설득력이 중요). 왜 그 기술을 썼고 다른 대안은 없었고 등등

싸피 생활 관련
- 스터디 많이 하면 좋다. (강제성 차원에서, 질적으로는 AI와 깊은 대화를 하면서 지식을 익히는게 좋음)
- 1학기때 코테 열심히 해라

