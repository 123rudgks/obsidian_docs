
## 1. Pre-training vs Post-training

### 1.1 거대 언어 모델 학습 패러다임

**정의** 최근 거대 언어 모델(LLM)을 학습하기 위해서는 크게 **두 가지 단계**로 나눠진다.

- **Pre-training (사전 학습)**: 방대한 텍스트 데이터를 통해 언어와 지식을 배우는 단계
- **Post-training (사후 학습)**: 사람이 원하는 방식으로 대화하고, 안전하고, 유용하게 만드는 단계

각 단계는 학습 목표(objective) 자체가 근본적으로 다르며, 이 차이가 왜 두 단계가 모두 필요한지를 설명한다 (아래 1.2, 1.3 참고).

_출처: 4-1_LangChain 서비스 개발, p.8_

### 1.2 Pre-training (사전 학습)

**정의** 방대한 텍스트 데이터를 이용한 **Self-supervised learning**을 통해 언어 패턴, 지식 등을 배우는 과정.

**학습 목표** **다음 단어 예측 (Next Token Prediction)** — 주어진 이전 토큰들을 바탕으로 다음에 올 토큰의 확률을 가장 높게 만들도록 파라미터를 업데이트.

**메커니즘**

- 입력 토큰 시퀀스(예: `Iroh, goes, to, make, tasty, tea`)가 **Decoder (Transformer, LSTM 등)**에 순차적으로 들어감
- 각 시점에서 모델은 다음에 올 토큰의 확률 분포를 출력
- 정답 레이블은 사람이 별도로 만든 것이 아니라 **원본 텍스트 자체의 다음 단어** — 그래서 "self-supervised"라 부름. 레이블링 비용 없이 인터넷 텍스트 전체를 학습 데이터로 쓸 수 있는 것이 pre-training이 이 정도 규모로 가능한 이유

**예시**

- "내일은 비가 ___" → "온다"의 확률을 가장 높게 만들도록 파라미터 업데이트
- 슬라이드 예시: `Iroh → goes → to → make → tasty → tea → [END]` 순서로 각 시점마다 다음 토큰을 예측하도록 학습

**Pre-training만 거친 모델의 한계 (배경/문제의식)**

- Pre-training은 "다음에 통계적으로 나올 법한 토큰"을 예측하도록 학습되었을 뿐, "사용자의 질문에 답하는 것" 자체를 학습 목표로 삼지 않음
- 예시 (GPT-3, PROMPT: "달 착륙을 6살 어린이에게 몇 문장으로 설명해줘.") → 모델은 질문에 답하는 대신 **비슷한 형식의 새로운 질문들을 나열**함:
    - "중력 이론을 6살 어린이에게 설명해줘."
    - "상대성이론을 6살 어린이에게 몇 문장으로 설명해줘."
    - "빅뱅 이론을 6살 어린이에게 설명해줘."
    - "진화론을 6살 어린이에게 설명해줘."
- 사전학습 데이터(인터넷 텍스트) 안에는 이런 식의 질문 리스트가 실제로 자주 존재하기 때문에, 모델 입장에서는 "질문에 답하기"보다 "비슷한 질문을 이어서 나열하기"가 통계적으로 더 그럴듯한 다음 토큰이었던 것 — 즉 **학습 목표(다음 토큰 확률 최대화)와 사용자가 원하는 것(질문에 대한 유용한 답변) 사이의 간극**이 바로 post-training이 필요한 근본 이유

_출처: 4-1_LangChain 서비스 개발, p.9-10_

### 1.3 Post-training (사후 학습)

**정의** **유저의 의도를 파악하고** 원하는 답변을 모델이 응답하도록 pre-training 이후 추가로 진행하는 학습 단계.

**목표** Pre-training이 만든 "다음 토큰을 잘 예측하는 모델"을, 실제로 **대화하고, 안전하고, 유용하게** 만드는 것 — 즉 순수 언어모델을 사람이 쓸 수 있는 어시스턴트로 바꾸는 단계.

**대표 기법**

|기법|설명|
|---|---|
|**Instruction-tuning**|지시-응답 쌍으로 지도학습 (2장에서 상세히 다룸)|
|**RLHF** (Reinforcement Learning from Human Feedback)|사람의 선호 피드백을 보상 신호로 강화학습 (3장에서 상세히 다룸)|
|**DPO** (Direct Preference Optimization)|별도 보상모델 없이 선호 데이터로 직접 최적화|
|**RLVR** (Reinforcement Learning with Verifiable Reward)|정답 검증이 가능한 보상(예: 수학/코드 정답 여부)으로 강화학습|

**예시 (동일 프롬프트, post-training 이후)**

- PROMPT: "달 착륙을 6살 어린이에게 몇 문장으로 설명해줘."
- COMPLETION (Human 선호 방식 응답): "거대한 로켓이 지구에서 우주비행사들을 태우고 달로 날아갔어요. 우주비행사들은 우주선을 달에 착륙시키고 달 표면을 걸어다니며 탐험했어요. 그리고 나서 그들은 지구로 안전하게 돌아왔고, 모든 사람들에게 보여주기 위해 달 돌을 가져왔어요."
- 1.2의 pre-training 예시와 비교하면, 질문을 나열하는 대신 **실제로 질문 의도에 맞는 답변**을 생성함 — post-training의 효과가 명확히 드러나는 대조 예시

_출처: 4-1_LangChain 서비스 개발, p.11_

## 2. Instruction-tuning

_(추후 슬라이드 학습 후 추가 예정)_

## 3. Reinforcement Learning from Human Feedback (RLHF)

### 3.1 Instruction-tuning의 한계 (SFT/모방학습의 근본적 한계)

**표면적 문제** 정답 데이터를 수집하는 것이 비싸다는 한계 외에도 다음 세 가지 문제가 있다.

- **문제 1**: 개방형/창의적 생성과 같은 태스크에는 정답이 존재하지 않는다 (예: "개와 개가 기르는 메뚜기에 대한 이야기를 만들어줘" — 정답이 하나로 정해질 수 없음)
- **문제 2**: 언어 모델링은 모든 토큰 레벨의 오류를 동일하게 취급하지만, 어떤 오류는 다른 오류보다 훨씬 심각하게 작용한다
- **문제 3**: 사람이 만든 답변(정답 레이블) 자체가 최적이 아닐 수 있다

**LM objective와 인간 선호 사이의 불일치** Instruction-tuning을 하더라도, 언어모델의 학습 목표(다음 토큰 예측 확률 최대화)와 "인간의 선호를 만족시키는 것"은 여전히 다른 목표다. 다음 토큰 예측이라는 objective 자체가 통계적으로 그럴듯한 토큰을 고르는 것이지, "사람이 실제로 가장 선호하는 응답"을 고르도록 직접 최적화된 게 아니기 때문이다.

**정리 메모 (직접 도출한 통찰)** Instruction-tuning은 결국 지도학습(Supervised Learning) — 정답 데이터를 보고 그대로 흉내 내는 모방학습(imitation learning)이다. 이는 어린아이가 부모의 행동을 그대로 따라 하는 것과 비슷하다. 하지만 아이가 자라면서는 "이건 해도 되고, 저건 하면 안 된다"는 걸 스스로 판단하는 법을 배워야 하듯, 언어모델도 단순 모방을 넘어 "여러 응답 중 무엇이 더 나은가"에 대한 피드백을 통해 배워야 한다 — 이것이 RLHF가 필요한 근본적인 이유다. 또한 instruction-tuning은 고정된 정답 패턴을 기계적으로 학습하기 때문에, 학습 데이터 분포를 벗어난 상황에서 유연하게 대응하기 어렵다는 한계도 있다.

_출처: 4-1_LangChain 서비스 개발, p.30-31_

### 3.2 인간의 선호도(Human Preference)란 무엇인가

**단답형 vs 주관식**

- 단답형 질문: "상대성 이론을 발표한 과학자는 누구인가?" → "알베르트 아인슈타인" (정답이 명확함)
- 주관식 질문: "아인슈타인이 오늘날 살아있다면, 현대 과학기술에 어떤 기여를 했을까?" (정답이 하나로 정해지지 않음)

**선호도의 주관성 (예시)** 동일한 주관식 질문에 대해 여러 최신 모델의 답변을 비교하면:

- **Claude Sonnet4**: 양자컴퓨팅/양자정보학, 양자암호학·양자통신 이론 발전, 중력파 검출기술 개발 참여 등을 언급
- **Copilot**: 상대성 이론의 재정립, GPS·우주탐사 기여, 다중우주 이론이나 암흑물질 연구에 대한 통찰 가능성 등을 언급
- **Gemini 2.5 flash**: 통일장 이론 완성 시도, 양자역학과의 조화(양자컴퓨터·양자통신 발전) 등을 언급
- **GPT-5**: 양자정보과학·AI·우주론 등 첨단 분야 관여, 블랙홀·중력파 연구 기여, 데이터·수학적 모델링 결합 등을 언급

네 모델 모두 그럴듯하지만 서로 다른 답을 낸다 — 이는 개방형 질문에서 "선호도"라는 것 자체가 굉장히 주관적이라는 것을 보여준다. 정답이 하나가 아니기 때문에, "어떤 응답이 더 나은가"를 판단하는 기준을 모델링하는 것 자체가 RLHF의 핵심 과제가 된다.

**Human-in-the-loop ML (직관)** Labeled Data → Training Model → Selecting Confusing Data(모델이 헷갈려하는 데이터 선별) → Human Annotator(사람이 라벨링) → 다시 Labeled Data로 순환하는 능동학습(active learning) 사이클. 사람이 모든 데이터에 라벨을 다는 대신, 모델이 확신하지 못하는 데이터를 우선적으로 사람에게 검토받아 라벨링 효율을 높이는 방식이며, RLHF에서 인간 피드백을 수집하는 방식의 기본 직관이 된다.

_출처: 4-1_LangChain 서비스 개발, p.32-36_

### 3.3 인간의 선호를 반영한 최적화 — 보상 함수(Reward Function)

**정의** 지시문 x와 언어모델의 응답 y가 있을 때, 해당 응답에 대한 인간의 보상(human reward)을 나타내는 함수 **R(x,y) ∈ ℝ**를 상상한다 (점수가 높을수록 좋음).

**예시** (샌프란시스코 지진 뉴스 요약 태스크)

|요약|보상 R(x,y)|
|---|---|
|y₁ (규모 4.2 지진, 강타, 불안정한 물체 넘어질 수 있음)|8.0|
|y₂ (지진 발생, 재산 피해 경미, 부상자 없음)|1.2|

**목표** 언어모델의 응답 중 **기대 보상(expected reward)을 최대화**하는 것:

$$E_{{\hat{y}\sim p_\theta(y|x)}}[R(x,\hat{y})]$$

_출처: 4-1_LangChain 서비스 개발, p.37_

### 3.4 RLHF 파이프라인 개요

전체 파이프라인은 크게 3단계로 이뤄진다.

|단계|내용|
|---|---|
|**Step 1: Instruction-tuning**|프롬프트(질문, 명령 등)를 데이터셋에서 샘플링 → 사람 주석자가 모델이 내야 하는 이상적인 답변을 직접 작성 → 이 정답 데이터로 GPT-3 같은 기본 모델을 SFT(지도 파인튜닝)|
|**Step 2: 비교 데이터 수집 및 보상 모델 학습**|동일한 질문에 대해 (Step 1에서 만든) SFT 모델이 여러 개의 답변(A, B, C, D 등)을 생성 → 사람 주석자가 가장 좋은 답변부터 가장 나쁜 답변까지 순위를 매김 → 이 순위 데이터를 학습해 각 응답을 채점할 수 있는 **Reward Model(RM)**을 훈련|
|**Step 3: 강화학습(PPO)으로 정책 최적화**|새로운 프롬프트 입력 → 정책 모델(Policy Model, = SFT 모델)이 답변 생성 → Reward Model이 그 답변의 점수를 계산 → **PPO(Proximal Policy Optimization)**로 정책(정책 모델의 파라미터)을 업데이트 → 이 과정을 반복|

**단순화된 순환 구조**로도 표현 가능하다: 입력 → LLM이 출력 A/B/C 생성 → 사람이 평가(B>A>C 같은 비교 데이터) → 이 (Input, Output) 비교 데이터로 Reward Model 학습 → Reward Model이 주는 보상을 이용해 강화학습으로 LLM을 다시 업데이트.

_출처: 4-1_LangChain 서비스 개발, p.38-39_

### 3.5 보상 모델(Reward Model) 학습 — 비교 기반 라벨링과 Bradley-Terry

**왜 절대 점수가 아니라 비교(순위)를 쓰는가**

- **문제**: 인간의 판단은 일관성이 떨어지고, 사람마다 채점 기준이 어긋날 수 있다 (한 사람이 준 절대 점수 "8.0"과 다른 사람이 준 "8.0"이 실제로 같은 품질을 의미하지 않을 수 있음)
- **해결**: 직접 점수를 매기지 않고, 두 응답 중 **어느 쪽이 더 나은지 비교**하는 방식을 사용한다 (상대적 비교가 절대 점수보다 더 일관성 있는 라벨이 됨)
- 예시: y₁(지진 강타), y₃(베이 지역 날씨는 좋지만 지진·산불 취약), y₂(지진 발생, 재산 피해 경미) → 사람이 보기엔 **y₂ > y₁ > y₃** 순으로 선호 → 이 비교 결과가 Reward Model 학습 데이터가 됨

**Bradley-Terry paired comparison model** 비교 데이터(pairwise labels: y₂>y₁, y₁>y₃, y₂>y₃)로 Reward Model $RM_\phi$를 학습하는 손실 함수:

$$J_{RM}(\phi) = -\mathbb{E}_{(x,y^w,y^l)\sim D}[\log \sigma(RM_\phi(x,y^w) - RM_\phi(x,y^l))]$$

여기서 $y^w$(winning sample, 이긴 응답)는 $y^l$(losing sample, 진 응답)보다 높은 점수를 받도록 학습된다. 위 예시라면 손실은 y₂>y₁, y₁>y₃, y₂>y₃ 각 쌍에 대한 로그시그모이드 항의 합으로 구성된다.

**정리 메모 (직접 도출한 통찰)** 직관적으로는, Bradley-Terry 방식은 "이런 응답은 해도 된다(winning sample → 점수↑)"와 "이런 응답은 하면 안 된다(losing sample → 점수↓)"를 상대적으로 학습시키는 방식이라고 이해할 수 있다.

_출처: 4-1_LangChain 서비스 개발, p.41-42_

### 3.6 정책 최적화 — 강화학습(RL)의 등장과 PPO

**RL의 역사적 배경 (3개의 시대)**

- **①고전적 RL 연구 시대**: 강화학습 분야는 오랫동안 관련 문제들을 연구해왔음 _(이 시기의 구체적 내용은 강의 음성을 놓쳐서 아직 정리하지 못함 — 추후 보강 필요)_
- **②딥러닝+게임 플레이 시대 (2013년 전후)**: 딥러닝이 게임 플레이에 적용되며 RL 연구가 다시 급증 (예: AlphaGo)
- **③LLM 적용 시대 (2019년~)**: 언어모델에 RL을 적용하려는 관심은 비교적 최근인 2019년부터 이루어짐 [Ziegler et al., 2019 / Stiennon et al., 2020 / Ouyang et al., 2022]

**왜 최근에야 가능해졌는가** RL을 언어모델에 적용하는 것은 원래 어렵게 여겨졌으나, 언어모델에 적용될 수 있는 RL 알고리즘(대표적으로 **PPO**, Schulman et al., 2017)이 최근 나오면서 실질적으로 가능해졌다.

_출처: 4-1_LangChain 서비스 개발, p.43_

### 3.7 RLHF의 효과: 모방을 넘어서는 이유

**RLHF는 pre-training과 instruction-tuning보다 더 나은 성능 향상을 제공한다**

Model size(1.3B~12.9B)를 늘려가며 Fraction preferred to reference summaries(참조 요약 대비 선호되는 비율)를 비교한 그래프:

- **Human feedback (RLHF)**, $p^{RL}(y|x)$: 약 0.6 → 0.7까지 상승
- **Supervised learning (Instruction-tuned)**, $p^{IFT}(y|x)$: 약 0.38 → 0.44 수준에 머무름
- **Pretrain only**, $p^{PT}(y|x)$: 약 0.22 → 0.31 수준에 머무름

핵심은 **모델 크기를 아무리 키워도 Pretrain-only와 Supervised(IFT)는 RLHF 수준을 따라잡지 못한다**는 점이다 — 즉 이는 스케일링만으로 해결되는 문제가 아니라, 학습 방식 자체의 차이에서 오는 격차다.

**정리 메모 (직접 도출한 통찰)** 모델도 인간도, 무언가를 흉내 내는 것만으로는 그 한계를 넘어서기 어렵다. 한계를 넘어서려면 스스로의 경험을 이용해야 한다. SFT/Instruction-tuning은 결국 사람이 미리 만들어둔 정답을 모방하는 것(모방의 한계에 갇힘)인 반면, RLHF는 모델이 스스로 생성한 응답에 대한 피드백(=모델 자신의 경험)을 학습에 활용하기 때문에, 모방만으로는 도달할 수 없는 성능까지 갈 수 있다.

_출처: 4-1_LangChain 서비스 개발, p.44_

### 3.8 실전 주의점: 보상 모델의 신뢰도

**주의점**: 강화학습(3단계)으로 넘어가기 전에, Reward Model이 잘 작동하는지 먼저 검증해야 한다.

Validation accuracy(검증 정확도) vs Model size(reward model 크기) 그래프:

- 학습 데이터 양(8k/16k/32k/64k example)이 많을수록, 그리고 모델 크기가 클수록 validation accuracy가 상승
- 그래프상 **Human baseline**(단일 인간 평가자 수준)과 **Ensemble of humans**(여러 인간 평가자 앙상블 수준)이 점선으로 표시되어 있으며, 충분히 많은 데이터로 학습한 큰 크기의 Reward Model은 대략 Human baseline 수준에 근접한다

**배경 논리 (왜 중요한가)** 3단계 PPO 최적화는 전적으로 Reward Model이 매긴 점수를 신뢰하고 진행된다. 따라서 Reward Model이 부정확하면 그 오차가 그대로 정책 최적화에 전파되어, 실제로는 좋지 않은 응답이 높은 점수를 받도록 모델이 학습되는 문제(reward hacking)로 이어질 수 있다. 그래서 본 강화학습을 시작하기 전에 Reward Model 자체의 신뢰도를 먼저 검증하는 것이 중요하다.

_출처: 4-1_LangChain 서비스 개발, p.45_

### 3.9 사례: ChatGPT = Instruction-tuning + RLHF

ChatGPT는 RLHF를 통한 발전에 힘입어, 거대언어모델 중 처음으로 상업적으로 널리 활용된 사례다.

**OpenAI 공식 설명 요약** (ChatGPT: Optimizing Language Models for Dialogue) InstructGPT와 동일한 RLHF 방법론을 사용하되, 데이터 수집 방식에 약간의 차이가 있다: 사람 AI 트레이너가 유저 역할과 AI 어시스턴트 역할을 모두 연기하며 대화 데이터를 생성하고, 이때 모델이 제안한 응답(model-written suggestions)을 참고해 응답을 작성할 수 있도록 함 — 이렇게 만든 새 대화 데이터셋을 대화 형식으로 변환한 InstructGPT 데이터셋과 혼합하여 학습.

_출처: 4-1_LangChain 서비스 개발, p.46; OpenAI, "ChatGPT: Optimizing Language Models for Dialogue" (https://openai.com/index/chatgpt/)_

## 4. What's Next?

### 4.1 RLHF의 한계 — 리워드 해킹과 환각

**문제의식** 인간의 선호도는 일관성이 부족하다 (3.5에서 다룬 문제와 동일한 근본 원인). 이 불완전한 선호 데이터로 학습된 리워드 모델을 강화학습에서 최적화하다 보면 **리워드 해킹(Reward Hacking)**이 자주 발생한다: 챗봇들이 정답 여부와 관계없이 **생산적이고 도움이 되어 보이는** 답을 생성하도록 학습되어버리는 것이다. 이러한 결과들은 **환각(Hallucination)** 문제를 발생시킨다.

**실제 사례**

- Google의 AI 챗봇(Bard)이 답변에서 실수를 저지르면서 주가가 하루 만에 1,000억 달러 하락 (2023년 2월)
- Bing AI가 "누가 슈퍼볼에서 이겼나?"라는 질문에 최근 경기 결과를 잘못 생성(환각)해 답변한 사례

_출처: 4-1_LangChain 서비스 개발, p.49_

### 4.2 리워드 모델 과최적화 (Reward Model Over-optimization)

**그래프 설명 (Reward model over-optimization)**

- x축: **KL from supervised baseline** — 정책(policy)이 원래 SFT 모델로부터 얼마나 멀어졌는지 (RL로 최적화를 많이 할수록 커짐)
- y축: Fraction preferred to ref (기준 응답 대비 선호되는 비율)
- **RM prediction (점선)**: 리워드 모델이 매긴 점수는 KL이 커질수록(=더 많이 최적화할수록) 계속 상승
- **Actual preference (실선, 실제 인간 평가)**: 처음엔 함께 상승하지만, 특정 지점(그래프상 KL 10~25 부근)을 정점으로 이후 급격히 하락

**결론**: 인간의 선호도를 학습한 리워드 모델은 (그 자체가 불완전한 근사치이기 때문에) 더 일관성이 부족하다!

**배경 논리 (Goodhart's Law)** 리워드 모델은 실제 인간 선호를 근사한 불완전한 대리 지표(proxy)일 뿐이다. 정책을 이 대리 지표에 너무 강하게 최적화하면, 모델이 리워드 모델의 허점(blind spot)을 파고드는 출력을 찾아내게 된다 — 리워드 모델 점수는 계속 오르지만 실제 품질은 오히려 나빠지는 역설이 발생한다("측정이 목표가 되는 순간, 그 측정은 더 이상 좋은 지표가 아니다"). 이것이 3.8에서 언급한 "본격적인 RL 최적화 전에 리워드 모델의 신뢰도를 먼저 확인해야 한다"는 경고의 실증적 근거다.

_출처: 4-1_LangChain 서비스 개발, p.50_

### 4.3 DPO — RLHF에서 'RL'을 제거하자

**정의** **DPO (Direct Preference Optimization)**; Rafailov et al., 2023

**배경/문제의식** 기존 RLHF 파이프라인은 (1) 별도의 리워드 모델을 학습하고 → (2) 그 리워드 모델을 이용해 RL(PPO)로 정책을 반복 최적화하는 2단계 구조다. 이 구조는 복잡하고, 학습이 불안정하며(4.2의 과최적화 문제 포함), 계산 비용도 크다.

**해결 아이디어** RLHF의 목적함수(KL 정규화가 포함된 보상 최대화)를 수학적으로 재정리하면, 별도의 리워드 모델 학습이나 RL 루프 없이도 **선호 데이터(preference data)에서 언어모델을 곧바로 maximum likelihood로 직접 최적화**할 수 있다는 것을 보인 것이 DPO다.

**비교**

||RLHF|DPO|
|---|---|---|
|절차|preference data → (maximum likelihood로) reward model 학습 → reward model이 준 label을 이용해 강화학습(RL)으로 LM policy 반복 업데이트(sample completions ↔ label rewards 순환)|preference data → (maximum likelihood로) final LM 직접 최적화|
|리워드 모델|별도로 필요|불필요|
|강화학습(RL)|필요 (PPO 등)|**불필요**|

_출처: 4-1_LangChain 서비스 개발, p.51_

### 4.4 RLVR — 정답이 명확한 문제는 검증 가능한 보상을 사용하자

**정의** **RLVR (Reinforcement Learning with Verifiable Reward)**; Lambert et al., 2024 / Deepseek-AI, 2025. 대표적인 성공 사례: **DeepSeek-R1**

**배경/문제의식** RLHF에서 수학 문제처럼 답이 객관적으로 명확한(검증 가능한) 문제까지 굳이 노이즈가 있는 학습된 리워드 모델(4.1~4.2에서 다룬 한계 포함)에 의존할 필요가 없다.

**해결 아이디어** 정답 여부를 그대로 보상으로 사용하는 **검증 가능한 보상(Verifiable Reward)**을 정의한다:

$$r = \begin{cases} \gamma & \text{if correct} \ 0 & \text{otherwise} \end{cases}$$

**메커니즘**

- Training data에서 프롬프트 $s_i$를 정책 $\pi_\theta(\cdot)$에 입력
- 정책이 응답(completion) $a_i$를 생성
- 정답 여부를 검증해 스칼라 보상 $r_i$를 계산 (Scalar Reward)
- 정책 파라미터를 업데이트: $\theta_{t+1} = \theta_t + \alpha\nabla_\theta J(\pi_\theta)$ (Policy Update)

_출처: 4-1_LangChain 서비스 개발, p.52_

### 4.5 정리 — DPO와 RLVR의 방향성 차이 (직접 도출한 통찰)

DPO와 RLVR은 같은 근본 문제(RLHF의 불완전한 리워드 신호 + 불안정한 최적화)를 서로 다른 방식으로 공략한다.

- **DPO**: 최적화 절차 자체를 단순화한다 — 리워드 모델과 RL(PPO) 루프를 통째로 제거하고, 선호 데이터로 LM을 직접 최적화
- **RLVR**: 보상의 출처를 바꾼다 — 학습된 근사 리워드 모델 대신, 정답을 객관적으로 검증할 수 있는 태스크에 한해 검증 가능한(ground-truth) 리워드를 사용해 애초에 리워드 해킹의 여지를 줄임

두 기법은 서로 배타적이지 않으며, 적용 대상(범용 선호 정렬 vs 정답이 명확한 태스크)이 다르다고 이해하는 것이 정확하다.

## 5. Basic Concepts of Retrieval-augmented LM

### 5.1 RAG 구성 요소 개관

**정의** Retrieval-augmented LM은 크게 네 가지 요소로 구성된다: **Datastore, Query, Index, Language Model**

**전체 흐름** `Datastore` (원시 텍스트 코퍼스) → 이를 바탕으로 `Index` 구축 → 사용자의 `Query`가 Index를 거쳐 관련 정보를 검색 → 검색된 정보(예시 조각: "FRIEND/MOBI...")를 언어모델의 원래 `Input`과 결합해 함께 활용

_출처: 4-1_LangChain 서비스 개발, p.70_

### 5.2 Datastore

**정의** 가공되지 않은 대규모 텍스트 코퍼스.

**특징**

- 최소 수십억에서 수조 단위의 토큰으로 구성
- 라벨링된 데이터셋이 아님
- 지식베이스(Knowledge base)와 같은 구조화된 데이터가 아님

**배경 논리** 이 정도 규모(수조 토큰)의 데이터를 사람이 일일이 정제·구조화하는 것은 현실적으로 불가능하다. 그래서 RAG는 원시(raw) 텍스트에서 바로 작동하는 인덱싱/검색 기법을 사용하는 것을 전제로 한다 — 이 점이 Datastore가 지식베이스(Knowledge base)와 근본적으로 다른 지점이다.

_출처: 4-1_LangChain 서비스 개발, p.71_

### 5.3 쿼리(Query)

**정의** 검색 질의(Retrieval input).

**핵심 포인트** 언어모델의 질의(input)와 같아야 하는 것은 아니다 — 즉 사용자가 LM에 던진 원래 질문 그대로가 검색 엔진에 들어가는 게 아니라, 검색에 더 적합한 형태로 변형·재구성될 수 있다 (예: 키워드 위주로 재작성 등). 핵심은 **Query ≠ LM Input일 수 있다**는 개념이다.

_출처: 4-1_LangChain 서비스 개발, p.72_

### 5.4 인덱스(Index)

**정의** 문서나 단락과 같은 검색 가능한 항목들을 체계적으로 정리하여 더 쉽게 찾을 수 있도록 하는 것.

**역할** 각 정보 검색(Information Retrieval) 메서드는 인덱싱 과정에서 구축된 인덱스를 활용해, 쿼리와 관련 있는 정보를 식별한다 — Datastore를 매번 처음부터 훑는 대신, 미리 구축해둔 인덱스를 통해 빠르게 관련 정보를 찾을 수 있다.

_출처: 4-1_LangChain 서비스 개발, p.73_

### 5.5 검색(Retrieval)

**정의** Datastore에 있는 수많은 정보 중에서, 주어진 쿼리(query)와 가장 관련성이 높은 정보를 찾아내는 과정.

_출처: 4-1_LangChain 서비스 개발, p.74_

### 5.6 Retrieval-augmented Generation (RAG) — 정의

**정의** 사용자의 질문에 답하기 위해, Datastore에서 관련 정보를 검색(Retrieval)해와서, 이를 언어모델이 **생성(Generation)** 단계에 함께 활용하는 방법.

5.1~5.5에서 다룬 Datastore, Query, Index, Retrieval이 모두 결합되어 이 하나의 개념(RAG)을 이룬다.

_출처: 4-1_LangChain 서비스 개발, p.75_

## 6. Information Retrieval

_(추후 슬라이드 학습 후 추가 예정)_

## 7. Retrieval-augmented LM

_(추후 슬라이드 학습 후 추가 예정)_