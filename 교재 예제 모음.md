
다음 코드의 실행 결과는?
```java
public static void main(String[] args){
	int i1 = Integer.MAX_VALUE;
	int i2 = i1 + 1;
	System.out.println(i2); // -2147483648
}
```
> 정수 계산 시 overflow 주의. 저장할 값의 크기를 고려해서 int 또는 long 등 타입 선택


문자형은 숫자? 내부적으로 ASCII/Unicode에 매핑된 결과. 아래 실행 결과는?
```java
	char c = '0';
		System.out.println(c==0);
		System.out.println(c==48);
		System.out.println('A' + 32 == 'a');
```

>[!success]- 
>ASCII에서 0과 A , a는 기억해놓자. 0: 48, A: 65, a:97
>```java
>false
>true
>true
>```


다음 코드의 실행 결과는?
```java
public static void main(String[] args){
	int i1 = Integer.MAX_VALUE; // 2147483647
	int i2 = i1 + 1;
	System.out.println(i2);
	
	long l1 = i1 + 1;
	System.out.println(l1);
	
	long l2 = (long)(i1 + 1);
	System.out.println(l2);
	
	long l3 = (long) i1 + 1;
	System.out.println(l3);
	
	int i3 = 1000000 * 1000000 / 1000000;
	int i4 = 1000000 / 1000000 * 1000000;
	System.out.println(i3);
	System.out.println(i4);
	
}
```
>[!success]-
>```javaa
>-2147483648 // overflow 발생
-2147483648 // overflow 발생
-2147483648 // overflow 발생
2147483648 // overflow 발생 X
-727 // overflow 발생
1000000 // overflow 발생 X
>```
>1. `long l1 = i1 + 1` 에서 `i1 + 1` 은 여전히 int 끼리의 연산
>2. `(long)(i1+1)`에서 `(i1+1)`에서 여전히 int끼리 연산을 하고 그 결과에 `(long)`이 씌워진것
>3. `(long) i1 + 1`에서 i1이 long으로 캐스팅되고 long + int 연산이므로 long으로 자동 변환이 됨
>4. 연산은 왼쪽에서 오른쪽으로 진행하므로 `1000000*1000000`에서 overflow 발생한 후 `/1000000`연산이 됨
>5. `1000000/1000000`에서 1로 연산이 되고 `1000000`과 곱해짐. int 범위 벗어나지 않아 overflow 발생 안함


다음 코드의 실행 결과는?
```java
public static void main(String[] args){
	byte b1 = 10;
	byte b2 = 20;
	byte b3 = b1 + b2;
	
	int i1 = 10;
	long l1 = 20;
	int i2 = i1 + l1;
	
	float f1 = 10.0;
	float f2 = f1 + 20.0;
}
```
>[!success]+
>```java
>// error: incompatible types: possible lossy conversion from int to byte
>// error: incompatible types: possible lossy conversion from long to int
>// error: incompatible types: possible lossy conversion from double to float
>```


다음 코드의 실행 결과는?
```java
int num = 3;

switch(num){
	case 1:
		System.out.println(num);
	case 2:
		System.out.println(num);
	case 3:
		System.out.println(num);
	case 4:
		System.out.println(num);
	case 5:
		break;
	case 6:
		break;
	default:
		System.out.println(num);
}
```
>[!check]+
>```java
>3
>3
>```


월별 날의 수를 출력하려면 아래와 같다. 이를 Switch expressions로 바꿔보아라
```java
int month = 3;
int day = -1;
switch(month){
	case 2:
		day = 29; break;
	case 4:
	case 6:
	case 9:
	case 11:
		day = 30; break;
	default:
		day = 31;
}
```
>[!check]+
>```java
>int month = 3;
>int day = switch(month){
>	case 2 -> 29;
>	case 4,6,9,11 -> 30;
>	default -> {
>		yield 31;
>	}
>}
>```
>만약, block을 구성해야하는 경우 yield 키워드 사용


반복문은 언제나 4가지 요소가 있다. 뭘까?
>[!check]+
>1. 변수 초기화
>2. 반복 조건
>3. 실행문
>4. 증감식


아래 배열의 내용 출력을 Arrays.toString()을 사용하여 한번에 나오도록 수정해봐
```java
int [] points = new int[3];
System.out.printf("배열의 크기 : %d%n",points.length);

points[0] = 1;
points[1] = 'A'; // 묵시적 형변환 char -> int
// points[2] = 1.5; // double -> int 형변환 불가

System.out.printf("0 번째 요소: %d%n",points[0]);
System.out.printf("1 번째 요소: %d%n",points[1]);
System.out.printf("2 번째 요소: %d%n",points[2]);

```
>[!check]+
>```java
>System.out.printf(Arrays.toString(points)); // [1, 65, 0]
>System.out.printf(points.toString(); // [D@46a49e] 같은거 나옴
>```


String "1234567890"의 각 문자를 char []과 int[]에 저장하고 출력하시오.
```java
// 아래에 코드 작성
// 정답 확인 후 초기화 해라
```
>[!check]+
>```java
>String org = "123456789";
>char[] chars = new char[org.length()];
>int[] nums = new int[org.length()];
>for(int i =0; i< chars.length; i++){
>	chars[i] = org.charAt(i);
>	nums[i] = chars[i] - '0';
>}
>for(int i = 0 ; i < chars.length; i++){
>	System.out.printf("char %c, num: %d%n", chars[i], nums[i]);
>}
>// API 활용
>chars = org.toCharArray();
>System.out.println(Arrays.toString(chars));
>```
>



배열의 선언과 생성을 따로 처리할 경우 초기화 주의. 아래 에러 코드를 옳바르게 수정하라
```java
int[] points;
points={1,3,5,6,8}; // 컴파일 오류
```
>[!check]+
>```java
>int[] points;
>points = new int[]{1,3,5,6,8} // 선언할 때는 배열의 크기를 알 수 없을 때
>```


배열을 arrayCopy와 copyOf API를 사용하여 복사해보자
```java
int[] src = {1,2,3,4,5};
```
>[!check]+
>```java
>int dest = new int[5];
>System.arraycopy(src, 0, dest,0,5); // 원본, 시작위치, 대상, 시작위치, 복사길이
int copy_src = Arrays.copyOf(src, 5); // 원본, 복사할 길이
>```
>`Arrays.copyOf`의 길이 부분이 원본보다 클 경우 나머지는 0으로 채움
>특정 범위만 복사하고 싶으면 `Arrays.copyOfRange(원본, start, end)`


다음 배열에 새로운 원소를 추가하려고 할 때 적절하지 못한 코드는?
```java
int[] scores = {90, 80, 100};

scores[3] = 95; // #1

scores = new int[] {90, 80, 100, 95}; // #2

scores = {90, 80, 100, 95}; // #3

scores = Arrays.copyOf(scores,5);
scores[3] = 95; // #4

int[] scores2 = new int[4];
System.arraycopy(scores,0,scores2,scores.length);
scores2[3] = 95; // #5

```
>[!check]+
>#1, #3


다음은 4x3의 2차원 배열을 만드는 방법이다. 올바르지 않은 것은?
```java
int intArray[][] = new int [4][3]; // A
int [] intArray2[] = new int[][3]; // B
int [][] intArray3 = new int[4][]; // C
int[][] intArray4 = new int[4]{1,2,3}; // D
int [][] intArray5 = new int[][]{{1,2,3},{1,2,3},{1,2,3},{1,2,3}}; // E
int[][] intArray6 = {{1,2,3},{1,2,3},{1,2,3},{1,2,3}} // F
```
>[!check]+
> B , D
> 의외로 E가 되네?
> 


객체지향 언어의 특징 4가지
OOP is A P.I.E
>[!check]+
>Abstraction : 추상화
>Polymorphism : 다형성
>Inheritance : 상속
>Encapsulation : 데이터 은닉과 보호


JVM의 메모리 구조. 각 정보가 들어있는 메모리를 작성해라
- 타입 정보
- 메서드들의 실행 공간
- Field 정보
- Method 정보
- 객체를 저장하기 위한 영역
- 상수 풀
>[!check]+
> 타입 정보 : Method Area
> 메서드들의 실행 공간 : Stack
> Field 정보 : Method Area
> Method 정보 : Method Area
> 객체를 저장하기 위한 영역 : Heap
> 상수 풀 : Method Area


다음 설명에 해당하는 메모리 영역 매칭해라
thread 별로 별도 관리
thread에 의해 공유
>[!check]+
>thread 별로 별도 관리 : Stack
>thread에 의해 공유 : Heap


객체는 프로그래머가 직접 메모리에서 없앨 수 있다. O,X
>[!check]+
>X : Garbage Collector가 없앰


다음을 손 컴파일링하고 메모리 동작을 그려보세요
```java
 public class SmartPhone{
	 static double osVer = 1.0;
	 String number;
	 void call(String to){
		 String msg = "띠";
		 for(int i=0; i<3; i++){
			 System.out.println(msg);
		 }
	 }
 }
 
 public class SmartPhoneTest{
	 public static void main(String[] args){
		 SmartPhone sphone = new SmartPhone();
		 sphone.number = "010";
		 System.out.println(sphone.osVer+" : "+sphone.number);
		 sphone.call("011");
		 
		 SmartPhone sphone2 = new SmartPhone();
		 sphone2.number = "010-111-1111";
		 System.out.println(sphone2.osVer+" : "+sphone2.number);
		 
		 SmartPhone.osVer = 2.0;
		 System.out.println(sphone.osVer+" : "+sphone.number);
	 }
 }
 
```

### Answer
	**1. 프로그램 시작 — Method Area**
	
	```
	Method Area
	├── SmartPhone 클래스 정보, 메소드 바이트코드
	├── SmartPhoneTest 클래스 정보, 메소드 바이트코드
	└── static double osVer = 1.0
	```
	
	---
	
	**2. `main()` 호출 — Stack**
	
	```
	Stack
	└── main 프레임
	     └── args
	```
	
	---
	
	**3. `new SmartPhone()` — Heap**
	
	```
	Stack                    Heap
	└── main 프레임          └── SmartPhone 객체
	     ├── args                 └── number = "010"
	     └── sphone ──────────────▶
	```
	
	---
	
	**4. `sphone.call("011")` 호출 — Stack**
	
	```
	Stack
	├── call 프레임     ← 새로 쌓임
	│    ├── to = "011"
	│    ├── msg = "띠"
	│    └── i = 0,1,2
	└── main 프레임
	```
	
	call() 종료되면 call 프레임은 Stack에서 제거됨.
	
	---
	
	**5. `new SmartPhone()` 두 번째 — Heap**
	
	```
	Stack                    Heap
	└── main 프레임          ├── SmartPhone 객체1 (number = "010")
	     ├── sphone  ────────▶
	     └── sphone2 ────────▶ SmartPhone 객체2 (number = "010-111-1111")
	```
	
	---
	
	**6. `SmartPhone.osVer = 2.0` — Method Area**
	
	```
	Method Area
	└── static double osVer = 2.0  ← 변경됨
	```
	
	sphone, sphone2 둘 다 같은 osVer를 공유하므로 둘 다 2.0으로 보임.
	
	---
	
	**7. `main()` 종료**
	
	```
	Stack → main 프레임 제거 → 비워짐
	Heap  → GC가 객체 정리
	Method Area → 프로그램 종료 시 정리
	```




##### 클래스 구성 요소의 로딩과 초기화 순서
Static 요소의 로딩
Instance 요소의 로딩
>[!check]+
>Static 요소의 로딩
>1. Static 변수 로딩 -> 기본 초기화
>2. Static 메소드 로딩
>3. Static initializer 실행
>4. Static Method 중 main 실행
>
>Instance 요소의 로딩
>1. 멤버 변수 로딩 -> 기본 초기화
>2. Instance Initializer 실행
>3. 생성자 실행


Static 영역에서 this를 쓸 수 있을 까?
>[!check]+
>못 쓴다, this는 객체의 주소를 참조함. static은 객체 생성전에도 이미 메모리에 할당 되어있다. (Method Area)



메서드 오버라이딩의 조건 5개
>[!check]+
>1. 메서드 이름이 같아야 한다.
>2. 매개변수의 개수, 타입, 순서가 같아야 한다.
>3. 리턴 타입이 같아야 한다.
>4. 접근 제한자는 부모보다 범위가 넓거나 같아야 한다.
>5. 조상보다 더 큰 예외를 둘 수가 없다.


상속 관계에 있을 때 자식 클래스의 타입으로 조상 클래스 객체를 참조할 수 있을까?
>[!check]+
>**조상**클래스 타입으로 자식 클래스 객체 참조 가능함. 부모가 가진게 더 적기 때문에 부모 타입으로 형변환 하여도 지장이 없음. 반면 자식 클래스로 부모 객체를 형변환 하면 객체에 없는 멤버를 사용할 수 있으니까 안됨


아래 코드의 프린트 내용을 작성하시오
```java
class Animal {
    String type = "동물";

    public String sound() {
        return "...";
    }

    public String getType() {   // getter도 결국 메서드!
        return type;
    }
}

class Dog extends Animal {
    String type = "개";   // 필드 숨김 (hiding) - Animal의 type과는 별개로 존재

    @Override
    public String sound() {
        return "멍멍!";
    }

    @Override
    public String getType() {   // 메서드 오버라이딩
        return type;             // 여기서 type은 Dog 자신의 필드
    }
}

public class FieldVsMethodTest {
    public static void main(String[] args) {
        Animal animal = new Dog();   // 업캐스팅! 실제 객체는 Dog, 참조 타입은 Animal

        System.out.println("1. animal.type       = " + animal.type);
        System.out.println("2. animal.sound()     = " + animal.sound());
        System.out.println("3. animal.getType()   = " + animal.getType());
    }
}
```
>[!check]+
>1. animal.type       = 동물
>2. animal.sound()     = 멍멍!
>3. animal.getType()   = 개


알아야 하는 Object 메소드들
toString (재정의 연습)
```java
// Object의 toString
public String toString(){
	return getClass().getName()+"@"+Integer.toHexString(hashCode());
}

@Override
public String toString(){
	return "SPiderMan [isSpier="+isSpider + ", name=" + name + "]";
}
```

equals (재정의 연습)
```java
class Phone{
	String number = "전화번호";
	public Phone(String number){
		this.number = number;
	}
	public boolean equals(Object obj){
		if(obj != null && obj instanceof Phone casted){
			return number.equals(casted.number);
		}
		return false
	}
}
```

hashCode(재정의 연습)
```java
class Product{
	String sn;
	(... 중략)
	@Override
	public int hashCode(){
		return sn.hashCode(); // 미리 작성된 String이나 Number 등에서 재정의된 hashCode 활용 권장
	}
}
```
> equals 재정의 시, hashCode도  반드시 재정의해라
> If two objects are qual according to the equals(Object) equals method, then calling the hashcode method on each of the two objects must produce the same integer result



(한번 읽어보고 넘어가셈) Raw타입을 사용하면 unchecked 경고 뜸 `@SuppressWarning`으로 경고 숨길 수 있음
```java
@SuppressWarnings({"rawtypes","unused"})
GenericBox box = new GenericBox();
```
당연히 @SuppressWarning을 남발하면 안좋음


1번과 2번 라인 중에 어디에서 컴파일 에러가 날까? ( SpiderMan extends Person 이라고 가정 )
```java
public void confusing(){
	GenericBox<Person> pbox = new GenericBox<>();
	pbox.setSome(new Person());
	pbox.setSome(new SpiderMan()); // (1)
	pbox = new GenericBox<SpiderMan>(); // (2)
}
```
>[!checked]+
>(2)번에서 컴파일 에러 발생
>why?
>(1)의 경우 SpiderMan이 Person으로 형변환이 되는 것. 
>(2)의 경우 GenericBox\<SpiderMan>을 GenericBox\<Person>으로 형변환 하려는 것. 그러나 자바의 제네릭은 **무공변**이기 때문에 타입이 상속관계라고 이를 사용하는 GenericBox도 자동으로 상속관계 처리를 해주지 않음


아래 코드는 허용이 될까? 안된다면 이유가 뭘까?
```java
static T itme;
```
>[!checked]+
>T가 실제로 어떤 타입이 될 지는 인스턴스를 만들때 비로소 결정이 됨. 따라서 static 멤버변수에 T를 쓸 수 없다. ( static은 모든 객체가 공유하는 것이기 때문에 )

다음 중 컴파일이 되는 항목은?
```java
T i = new T(); // (1)

GenericBox<SpiderMan> obj = new GenericBox<>();
if(obj instnceof GenericBox<String>){} ; // (2)

if(obj instanceof GenericBox gb){ // (3)
	System.out.printlnt("");
	gb.setSome("");
}

if(obj instanceof GenericBox<?>){ //(4)
	System.out.println("");
}

GenericBox<String> [] boxes1 = new GenericBox<>[3]; // (5)
```
>[!checked]+
>(3), (4)번만 컴파일 가능
>(1) : Generic은 컴파일 시점에만 타입을 체크하고 런타임시에는 타입정보가 소거되기 때문에 런타임에서 동작하는 new 키워드 사용 불가
>(2) : (1)과 같은 이유로 런타임시 타입 정보가 없기 때문에 instanceof 키워드 사용 불가
>(3) : (2)와 달리 타입 인자가 지정되지 않고 단지 GenericBox클래스인지를 물어보는 것이기 때문에 컴파일은 됨. 단지 GenericBox\<String>에 해당하는 메소드를 사용할 시 런타임 에러가남
>(4) : 와일드 카드를 사용할 경우엔 컴파일이 되. 다만 setter를 사용할 수 없으
>(5) : Generic 타입의 배열은 생성 불가능하다. (배열의 공변성 때문) List\<GenericBox\<T>> 사용 권장


다음 중 허용할 수 있는 공통요소를 고민해보자.
```java
GenericBox<Object> boxObject = new GenericBox<>();
GenericBox<Person> boxPerson = new GenericBox<>();
GenericBox<SpiderMan> boxSpiderMan = new GenericBox<>();


void useWildCardType1(GenericBox<?> box){...} // (1)
void useWildCardType2(GenericBox<? extends Person> box){...} // (2)
void useWildCardType3(GenericBox<? super Person> box){...} // (3)
```
>[!checked]+
>(1) : 3가지 모두 허용
>(2) : boxPerson과 boxSpiderMan 허용
>(3) : boxPerson과 boxObject 허용

다음 setter에 들어갈 수 있는 타입은?
```java
public void useWildCardType1(GenericBox<?> boxAll){
	boxAll.setSome(??); // 뭐만 들어갈 수 있을 까?
}
```
>[!checked]+
>null만 가능
>어떤 타입이든 들어올 수 있으니 클래스의 멤버변수를 바꾸는 setter에서 아예 받지 못하게 하는 것.


ArrayList 프레임워크는 내부적으로 어떤 타입의 배열로 관리하는가?
>[!checked]+
>내부적으로 Object[]에 저장

ArrayList의 장/단점
>[!checked]+
>배열 기반이므로 배열의 장/단점을 그대로 가져감.
>장점 : 연속된 데이터 배열로 접근 속도가 빠름
>단점 : 
>크기를 변경하기 위해서 새로운 배열을 만들고 복사해야함
>비 순차적 데이터의 추가,삭제에 많은 시간 소요됨 (그러나 System.arrayCopy() 메소드 등 최적화를 잘 해놓아서 이제는 크게 유의미하진 않다곤 함 )

LinkedList에서 요소를 삽입/삭제하는 비용은 O(1)이다?
>[!checked]+
>아니다, 삽입/삭제하려는 곳까지 이동하는 비용이 O(n)이기 때문에 최종적으로 ArrayList와 큰 차이없는 O(n)이다.

아래는 LinkedList에 n번째 요소의 값을 바꾸는 코드이다. 이 코드의 시간복잡도를 낮추도록 수정해라
```java
for(int i = 0 ; i<1000; i++){
	list.add(5000,i);
}
```
>[!checked]+
>위 코드에선 n번 요소에 찾아가는 비용 n + 삽입 비용 1을 1000번 반복한다. (n+1)\*1000
>차라리 ArrayList를 썼으면 여전히 찾아가는 비용 1 + 요소 뒤로 밀어내는 비용 n이 들었겠지만 System.arraycopy가 잘 최적화 되어있어서 LinkedList의 순회보다 더 나았을 수도 있음
>만약 LinkedList 에서 순회를 한번만 하여 위치를 찾은 후에 한번에 1000개를 추가했으면 훨씬 나았을 것.
>```java
>ListIterator<Integer> iter = list.listIterator(5000); // 딱 한번만 이동
>for(int i = 0 ; i < 1000; i++){
>	iter.add(i); // 이미 그 자리에 있으므로 순회 없이 바로 삽입
>}
>```


아래 케이스별 ArrayList vs LinkedList의 속도 승자를 골라라

| 테스트 항목           | ArrayList | LinkedList |
| ---------------- | --------- | ---------- |
| 초기화(맨 앞 추가)      |           |            |
| 처음 요소 발견         |           |            |
| 마지막 요소 발견        |           |            |
| 중간 요소 발견         |           |            |
| 단일 요소 추가 (중간)    |           |            |
| 자체 100개 추가 (중간)  |           |            |
| 반복기 100개 추가 (중간) |           |            |
>[!checked]+
>초기화(맨 앞 추가 ) : LinkedList 승, LinkedList는 head 포인터만 교체, ArrayList는 전체 이동
>처음 요소 발견 : 둘이 비슷함
>마지막 요소 발견 : 둘이 비슷함
>중간 요소 발견 : ArrayList 승, LinkedList는 노드를 하나씩 순회해야 함
>단일 요소 추가 (중간) : ArrayList 승, Big-O는 같지만, 배열의 벌크 복사가 포인터 순회보다 실전에서 빠름
>자체 100개 추가(중간) : ArrayList 승, LinkedList가 매번 처음부터 재순회, ArrayList는 매번 빠른 벌크 이동
>반복기 100개 추가(중간): LinkedList 승, LinkedList는 순회 1번 후 `O(1)` 삽입 반복, ArrayList는 매번 이동 불가피


아래 코드에서 이상인 부분을 찾고 올바르게 수정해라
```java
List<Integer> nums = new ArrayList<>();
Random rand = new Random();
for(int i =0; i<10; i++){
	nums.add(rand.nextInt(20));
}
System.out.println("전체 : "+nums);

for(int i=0; i<nums.size(); i++){
	if(nums.get(i)%2==0){
		nums.remove(i);
	}
}
System.out.println("짝수 삭제 후 : " + nums);
```
>[!checked]+
>짝수를 제거하는 for문에서 원소를 하나 제거하면 제거한 원소 뒤에 있는 원소들을 한칸씩 앞으로 당긴다. i가 1증가하면 다음 검사해야할 원소를 건너뛰기 때문에 오류가 발생
>`nums.remove(i)` 밑에 `i--;`  를 추가하여 다음 원소를 옳바르게 선택하게 하거나 아래와 같이 거꾸로 접근하도록 하면 인덱스 뒤에 있는 원소들이 앞으로 당겨지고 인덱스는 옳바르게 다음 원소를 가리킨다.
>```java
>for(int i=nums.size()-1; i>=0; i--){
>	if(nums.get(i)%2==1){
>		nums.remove(i);
>	}
>}
>```

아래 코드에서 이상인 부분을 찾고 올바르게 수정해라
```java
public class ForEachTest{
	public static void main(String[] args){
		List<Integer> nums = new ArrayList<>();
		for(int i=0; i<10; i++){
			nums.add(i);
		}
		
		for(Integer num: nums){
			if(num%2==0){
				//nums.add(num*num);
				nums.remove(num);
			}
		}
	}
}
```
>[!checked]+
>nums.add(..) 와 nums.remove(..)이 Collection의 크기를 변화시킴 그러나 forEach에서 Collection의 크기를 변화시킬 경우 ConcurrentModificationException 에러가 발생함
>방법 1 
>Iterator의 remove()를 직접 쓴다.
>```java
>Iterator<Iterator> it = nums.iterator();
>while(it.hasNext()){
>	Integer num = it.next();
>	if(num%2 == 0){
>		it.remove(); // expectedModCount도 함께 갱신해줌
>	}
>}
>```
>방법 2
>removeIf() 사용
>```java
>nums.removeIf(num -> num % 2 == 0);
>```


배열보다 제네릭이 타입 안정성이 더 좋은 이유를 설명해라
>[!checked]+
>```java
>Object[] arr1 = new Object[3];
>Object[] arr2 = new Person[3]; // 배열은 공변성이 있어서 해당 라인 컴파일 통과
>arr2[0] = 1; // 컴파일 시엔 통과했지만 런타임에서 터짐
>
>// 반면 제네릭은 무공변 자료형. 즉 원소가 상속관계라도 이를 담은 박스는 상속관계와 무관함
>List<Object> list = new ArrayList<Person>(); // 컴파일 단계에서 부터 에러. 
>```


ArrayList에서 고정 사이즈의 리스트를 생성하는 방법
>[!checked]+
>```java
>List<String> list = Arrays.asList("a","b","c");
>```
>고정 크기이기 때문에 add,remove 사용 불가
>요소의 값 수정은 가능(null도 허용)


Set에는 update가 있다 없다?
>[!checked]+
>없다. Map의 경우 key + value이기 때문에 값만 교체하는게 자연스럽지만 Set의 경우 원소 자체가 곧 데이터다. 따라서 그 값 자체를 update 한다라는게 말이 되지 않는다. 대신 그 값을 지우고 새로운 값을 넣어라 `remove(old)` 그리고 `add(new)`가 자연스럽기 때문에 `update()`가 없다.

아래 코드의 출력 값을 예상해보아라
```java
public class SetTest {
    Set<Object> hset = new HashSet<Object>();

    private void addMethod() {
        hset.add(new Integer(1));
        hset.add("Hello");
        hset.add("Hello");    // 동일한 데이터 추가 확인
        hset.add(new String("Hello"));
        hset.add(1);           // 기본형은 wrapper를 통해 추가
        System.out.println("데이터 추가 결과: " + hset);
    }

    private void retrieveMethod() {
        System.out.println("데이터 개수: " + hset.size());

        for (Object sobj : hset) {
            System.out.println("데이터 조회: " + sobj);
        }
    }

    private void removeMethod() {
        hset.remove("Hello");
        System.out.println("데이터 삭제 결과: " + hset);
    }

    public static void main(String[] args) {
        SetTest test = new SetTest();
        test.addMethod();
        test.retrieveMethod();
        test.removeMethod();
    }
}
```
>[!checked]+
>`new Integer(1)`과 `1` 그리고 `"Hello"`와 `new String("Hello")`는 Set 내부적으로 hashCode()와 equals()로 중복인지 파악한다. 따라서 동일한 것으로 처리함. (remove도 마찬가지)
>```java
>result : [1, Hello]
number of data : 2
search : 1
search : Hello
delete result : [1]
>```

아래 코드를 실행할 시 출력 값은? 의도된 대로 출력하려면?
```java
class SmartPhone{
    String number;
    public SmartPhone(String number){
        this.number = number;
    }
    public String toString(){
        return "전화 번호" + number;
        }
}

public class Main
{   
	public static void main(String[] args) {
    Set<SmartPhone> hset = new HashSet<>();
    hset.add(new SmartPhone("010-111-2222"));
    hset.add(new SmartPhone("010-111-2222"));
    hset.add(new SmartPhone("010-111-2223"));
    
    for(Object sobj : hset){
        System.out.println(sobj);
    }
	}
}
```
>[!checked]+
>```java
>전화 번호010-111-2222
전화 번호010-111-2222
전화 번호010-111-2223
>```
>Set에서 같은 원소인지 검사할 때 equals()와 hashCode()를 사용하여 모두 동일할 때 같은 원소라고 처리한다.
>기본적으로 객체는 Object를 상속 받아 hashCode()로는 메모리에 할당된 주소, equals()는 같은 주소인지 확인한다.
>따라서 비즈니스적으로 동일하게 처리하기 위해 hashCode와 equals를 오버라이드 받아 별도의 처리를 해주어야 한다.
>```java
>public int hashCode(){
>	return number.hashCode();
>}
>public boolean equals(Object obj){
>	boolean result = false;
>	if(obj != null && obj instanceof SmartPhone param){
>		result = (this.number.equals(param.number));
>	}
>	return result;
>}
>```




아래 컬렉션의 주요 메서드는 직접 사용해보면서 익히는게 제일 좋다. 아래 메서드에 대한 정석적인 예제를 생성하여 각 컬렉션 별 주요 메서드를 숙지하도록 하자.

Collection의 주요 메서드
```java
add
addAll
containsAll
equals
isEmpty
iterator
size
clear
remove
removeAll
toArray
```


List의 주요 메서드
```java
add
addAll
get
indexOf
lastIndexOf
listIterator
remove
set
subList
```


Set의 주요 메서드
작성된 List와 Map의 주요 메서드와 비슷한 결의 자주 쓰이는 메서드들로 예제 생성 필요


Map interface의 주요 메소드 (예제를 claude로 생성하여 손에 익히기)
```java
put
putAll
containsKey
containsValue
entrySet
keySet
get
values
size
isEmpty
clear
remove
put
putAll
```


Arrays.asList()로 배열을 리스트로 만들 수 있음. -> but 수정시 원본을 바꾸니 주의


언제나 정렬이 가능한 요소는?
1. String
2. Integer
3. Object


다음 SmartPhone객체가 Collections.sort를 사용할 수 있도록 코드 수정하라
```java
class SmartPhone  { 
	String number; 
	public SmartPhone(String number) { 
	this.number = number; 
	} 
	
	public String toString() { 
		return "전화 번호" + number; 
	} 
}
```

>[!checked]+
>Comparable을 바꾸거나 Comparator를 사용해야한다.
>```java
>class SmartPhone implements Comparable<SmartPhone>{
>	String number;
>	public SmartPhone(String number){
>		this.number = number;
>	}
>	@Override
>	public int compareTo(SmartPhone other){
>		return this.number.compareTo(other.number);
>	}
>	public String toString(){
>		return "전화 번호" + number;
>	}
>}
>
>List<SmartPhone> phones = new ArrayList<>(new SmartPhone("010"), new SmartPhone("011"), new SmartPhone("012")));
>Collections.sort(phones);
>```
>위 코드에서는 String 타입의 number의 기본 compareTo를 사용하였다. 이처럼 이미 사용 가능한 기준이 있다면 직접 만들지 말고 사용하는게 좋다.
>
>객체가  Comparable을 구현 할 수 없거나 사용자 정의 알고리즘으로 정렬하려는 경우 Comparator를 사용할 수도 있다.
>
>```java
>// 클래스에 implements 하는 방식
>class SmartPhoneComparator implements Comparator<SmartPhone> { 
>	@Override 
>	public int compare(SmartPhone o1, SmartPhone o2) { 
>		return o1.number.compareTo(o2.number); // String.compare()는 없음 → compareTo() 사용 but Integer.compare()는 있음
>	} 
>}
>
>Collections.sort(phones, new SmartPhoneComparator());
>```
>```java
>// 람다 표현식 이용하는 법
>Collections.sort(phones,(p1,p2)->p1.number.compareTo(p2.number));
>```
>```java
>// anonymous inner class 사용하는 법
>Collections.sort(phones, new Comparator<SmartPhone>{
>	@override
>	public int compare(SmartPhone O1, SmartPhone O2){
>		return 01.number.compareTo(o2.number);
>	}
>})
>```





Stream 단계별 주요 처리 주요 메서드는 직접 사용해보면서 익히는게 제일 좋다. 아래 메서드에 대한 정석적인 예제를 생성하여 각 컬렉션 별 주요 메서드를 숙지하도록 하자. 
distinct
filter
skip
limit
sorted
peek
flatMap
flatMapToDouble
flatMapToInt
flatMaptoLong
map
mapToDouble
mapToInt
mapToLong
asDoubleStream
asLongStream
allMatch
anyMatch
noneMatch
collect
forEach
count
findFirst
max
min
average
sum
reduce
toList
toSet
toMap

위 Stream 메소드를 이용하여 적절한 파이프라인 구축하는 것과 실행 결과를 예측하는 예제도 만들자.



Exception의 Throwable의 주요 메소드들
getMessage, getCause, printStackTrace
-> 어떤 상황에서 어떤 메소드를 써야하는지를 물어보는 예제 필요



num이 0일때 출력되는 내용은? num이 1일때 출력되는 내용은?
```java
public static void main(String[] args){
	int num = new Random().nextInt(2);
	try{
		System.out.println("cod 1, num: " + num );
		int i = 1/num;
		System.out.println("code 2 - 예외 없음");
	}catch(ArithmeticException e){
		System.out.println("code 3 - 예외 처리 완료");
	}
	System.out.println("code 4");
}
```
>[!checked]+
>num = 1일 때 
>cod 1, num : 1
>code 2 - 예외 없음
>code 4
>
>num=0일 때
>cod 1, num : 0
>code 3 - 예외 처리 완료
>code 4


다중 try 블록에서 상위 catch에서 조상 예외를 잡으면 그 아래에 있는 자식 예외는 잡히질 않는다. 주의 필요


num이 0일때 출력되는 내용은? num이 1일때 출력되는 내용은?
```java
public static void main(String[] args){
	int num = new Random().nextInt(2);
	try{
		System.out.println("cod 1, num: " + num );
		int i = 1/num;
		System.out.println("code 2 - 예외 없음");
	}catch(ArithmeticException e){
		System.out.println("code 3 - 예외 처리 완료");
	}finally{
		System.out.println("code 4 - 언제나 처리");
	}
	System.out.println("code 5");
}
```
>[!checked]+



다음 코드에서 중복되는 app.delete()를 어떻게 처리하면 좋을까?
```java
InstallApp app = new InstallApp();
try{
	app.copy();
	app.install();
	app.delete();
}catch(Exception e){
	app.delete();
	e.printStackTrace();
}
```
>[!checked]+
>```java
>finally{
>	app.delete()
>}
>로 한번에 처리
>```


checked exception과 unchecked exception의 차이는?
>[!checked]+
>checked Exception은 컴파일러가 "처리하거나 선언하라"고 강제하지만 unchecked Exception은 강제하지 않는다.
>checked exception : Exception
>unchecked exception : RuntimeException
>
>unchecked exception은 try-catch 안써도 컴파일은 됨


메서드 재 정의 시 조상 클래스의 메서드가 던지는 예외보다 부모 예외를 던질 수 없다.



다음은 부절절한 예외 처리의 예시이다.
```java
private static void endByException(){
	int sum = 0;
	try{
		for(int i=0; ; i++){
			sum+=nums[i];
		}
	}catch(ArraysIndexOutOfBoundsException e){
		e.printStackTrace();
	}
} 

// 예외 처리를 흐름 제어 용도로 사용하고 있다. 
```

```java
private static void useScannerBad(){
	Scanner scanner =.new Scanner(System.in);
	int sum = 0;
	try{
		while(true){
			int input = scanner.nextInt();
			if(input ==0){
				break;
			}else{
				sum += input;
			}
		}
	}catch(InputMismatchException e){
		e.printStackTrace();
	}
	scanner.close();
}
// scanner.nextInt()와 같은 상태 종속적 코드는 상태 확인 메서드(scanner.hasNextInt())와 함께 사용해야 한다.
```
