# 63. 그래서 GPU는 여러 Precision을 지원한다

현대 AI GPU는 단순히

```text
FP64
FP32
```

만 지원하지 않는다.

AI Workload를 위해

```text
FP16

INT8

INT4

등
```

낮은 Precision 연산도 지원한다.

강의에서는 특히 NVIDIA GPU/Tensor Core가 FP 계열뿐 아니라 INT8·INT4 같은 저정밀 Integer 연산까지 지원하는 이유를 이러한 Energy·Compute Efficiency와 연결한다.

---

# 64. 왜 낮은 Precision을 지원할까?

가능하다면

```text
FP32
```

보다

```text
INT8
```

혹은 더 낮은 Precision을 사용함으로써

```text
Memory ↓

Compute ↓

Energy ↓
```

를 얻을 수 있기 때문이다.

---

# 65. 수 체계 강의의 최종 메시지

앞 세 차시의 핵심은 결국 이것이다.

> **내 Algorithm이 정말 높은 Range와 Precision을 필요로 하는가?**

필요하지 않다면 더 간단한 Number Representation을 사용함으로써 Resource를 아낄 수 있다.

---

# 66. 이제 AI Model 전체로 확장

여기서 강의 주제가

```text
Number Representation
```

에서

```text
Model Compression
```

으로 확장된다.

---

# 67. AI는 강력하지만 공짜가 아니다

최근 AI Model의 성능은 매우 높아졌다.

하지만 높은 성능을 얻기 위해 Model은 계속 커지고 있다.

강의에서는 이를

```text
No Free Lunch
```

라는 관점으로 설명한다.

---

# 68. Scaling Law

Transformer 기반 Large Model에서는 일반적으로

```text
Model Size ↑

Data ↑

Compute ↑
↓
Capability ↑
```

라는 Scaling Trend가 관찰되어 왔다.

그래서 기업들은 Model Scale을 계속 키워왔다.

---

# 69. Model Scale 증가가 만드는 문제

Model이 커지면

```text
Training Compute ↑

Inference Compute ↑

Memory ↑

Communication ↑

Energy ↑

Cost ↑
```

가 동시에 증가한다.

---

# 70. Hardware도 계속 발전했다

GPU 역시 AI Model 증가에 맞춰 계속 발전했다.

하지만 강의에서 강조하는 문제는

> **Model Resource Requirement 증가 속도가 Hardware 발전만으로 따라가기 어려운 수준으로 가고 있다.**

는 것이다.

---

# 71. Compute Growth

강의에서는 AlexNet 이후 AI Model 개발에 요구되는 Compute가 매우 빠르게 증가해 왔다는 자료를 소개한다.

정확한 성장 배수 자체보다 핵심은

```text
Model Compute Demand
≫
Traditional Hardware Scaling
```

상황이 나타나고 있다는 점이다.

---

# 72. Moore's Law만으로 해결하기 어렵다

Hardware 자체도 계속 좋아지지만

```text
모델 규모 증가 속도
```

가 너무 빠르다면

```text
더 좋은 GPU를 기다린다.
```

만으로 문제를 해결할 수 없다.

---

# 73. Training Cost

Large Foundation Model Training에는 막대한 Compute Resource와 비용이 필요하다.

강의에서는 여러 Large Language Model의 Training Cost 사례를 들어 이를 설명한다.

정확한 금액은 강의 시점의 추정자료이므로 핵심 개념은

```text
Large Model Training
=
매우 높은 경제적 진입장벽
```

이라는 것이다.

---

# 74. 하지만 Training만 문제가 아니다

Foundation Model Training은 일부 대기업이 수행하고 다른 사용자가 그 Model을 활용할 수도 있다.

그럼 문제가 해결될까?

아니다.

---

# 75. Inference도 비싸다

Model이 너무 크면 단순히 Service하는 것만으로도

```text
Model Weight Loading

GPU Memory

Inference Compute

Energy

Serving Infrastructure
```

가 큰 비용을 요구한다.

---

# 76. Inference Economics

Service는 수많은 User Query를 처리해야 한다.

```text
Cost per Query
×
Millions of Queries
```

가 되므로 Query 하나의 작은 Compute 차이도 전체 Service Cost에서는 매우 커질 수 있다.

---

# 77. 그래서 Model Compression이 필수

강의에서는 현재의 Model Scale에서는

> **Model Compression / Model 경량화가 더 이상 선택적인 Optimization이 아니라 필수가 되고 있다.**

고 설명한다.

---

# 78. Model Compression의 궁극적 목적

단순히 Model File을 작게 만드는 것이 아니다.

궁극적으로는

> **가능한 한 비슷한 AI Quality를 더 적은 Resource와 더 낮은 Cost로 제공하는 것**

이다.

---

# 79. 공학적 관점

같은 성능을

```text
더 싸게

더 빠르게

더 적은 Energy로

더 작은 Hardware에서
```

제공할 수 있으면 더 좋은 Engineering Solution이 된다.

---

# 80. 모델 경량화는 하나의 목표가 아니다

매우 중요하다.

강의에서는 흔히

```text
모델 경량화
```

라고 하나로 묶어서 부르지만 실제 Target은 여러 가지라고 설명한다.

대표적으로

```text
1. Model Size

2. Speed / Latency

3. Energy Consumption
```

이다.

---

# 81. 목표 ① Model Size

예:

```text
GPU VRAM = 24 GB
```

인데 Model이 30 GB라면 Model을 실행할 수 없다.

이 경우 가장 중요한 목표는

```text
Model Size ↓
```

다.

속도가 그대로여도 Model이 한 GPU에 들어가는 것 자체가 큰 가치가 있다.

---

# 82. 목표 ② Speed / Latency

Model은 GPU에 들어가지만

```text
Inference가 너무 느리다.
```

면 문제다.

이때는

```text
Execution Time ↓
Throughput ↑
```

를 목표로 경량화한다.

---

# 83. 목표 ③ Energy / Power

특히

```text
Mobile

Embedded

Battery-powered Device

Large Data Center
```

에서는 Power가 중요하다.

```text
Energy ↓
```

를 중심으로 Compression을 설계할 수도 있다.

---

# 84. 같은 “경량화”라도 Strategy가 달라진다

```text
Memory 문제
```

를 해결하는 기법과

```text
Latency 문제
```

를 해결하는 기법이 반드시 같지는 않다.

또

```text
Energy 문제
```

의 최적 해법도 다를 수 있다.

---

# 85. 반드시 Target Metric을 먼저 정의한다

경량화를 시작하기 전에

```text
무엇을 줄이는가?
```

를 명확히 해야 한다.

예:

```text
Model Size?

Peak Memory?

Latency?

Throughput?

Energy?
```

이다.

---

# 86. Accuracy Trade-off

경량화에는 항상 대가가 있다.

일반적인 구조:

```text
Compression ↑
↓
Resource Cost ↓

하지만

Accuracy / Model Quality ↓ 가능성
```

이다.

---

# 87. 강의의 중요한 표현

> **모델 성능이 전혀 떨어지지 않는 경량화를 당연하게 기대하면 안 된다.**

핵심은

```text
얼마의 Accuracy Loss를
감수하면서

얼마나 많은 Resource를
절약할 것인가?
```

이다.

---

# 88. 극단적인 Compression은 의미가 없다

예를 들어 Model Size를 엄청 줄였는데

```text
Accuracy:
90%
↓
32%
```

가 되었다고 하자.

Resource는 크게 줄었지만 실제 Service에 사용할 수 없다면 좋은 Compression이라고 할 수 없다.

강의에서도 모델 경량화를 Accuracy와 효율 사이의 Trade-off 문제로 설명한다.

---

# 89. 모델 경량화의 핵심 Optimization Problem

개념적으로

```text
Resource Cost ↓
```

를 최대화하면서

```text
Model Quality Loss
```

를 허용 가능한 범위에 두는 것이다.

---

# 90. Pareto Trade-off 관점

이상적인 Compression Technique은

```text
같은 Accuracy
→ 더 작은 Model

또는

같은 Model Size
→ 더 높은 Accuracy
```

를 만든다.

실제로는 여러 Solution 중

```text
Accuracy
vs
Efficiency
```

Trade-off가 가장 좋은 지점을 선택해야 한다.

---

# 91. 단순 File Size가 줄었다고 성공이 아니다

매우 중요하다.

예:

```text
Model Size
10× 감소
```

했는데 GPU에서 실행하니

```text
Latency
10× 증가
```

할 수도 있다.

이 경우

```text
Storage 관점
→ 좋음

Speed 관점
→ 나쁨
```

이다.

---

# 92. 왜 이런 일이 생길까?

Hardware는 모든 연산을 동일하게 빠르게 처리하지 않는다.

예를 들어 특정 Compression에서 만든

```text
특이한 Sparse Pattern

특정 Bit-width

특수 Operator
```

를 GPU가 효율적으로 지원하지 않을 수 있다.

---

# 93. Hardware–Algorithm Co-design

따라서 Compression Technique은 반드시

```text
Target Hardware
```

와 함께 고려해야 한다.

이것을 넓게 보면

```text
Hardware–Algorithm Co-design
```

관점이다.

---

# 94. GPU와 궁합이 좋은 Compression

경량화 이후

```text
Model Size ↓

그리고

Latency ↓
Energy ↓
```

도 실제 GPU에서 의미 있게 개선된다면 GPU와 궁합이 좋은 기법이라고 볼 수 있다.

---

# 95. GPU와 궁합이 나쁜 Compression

Model Parameter 수나 File Size는 많이 줄었는데

```text
GPU가 해당 Operation을
효율적으로 처리하지 못함
```

으로 인해 실제 실행 속도가 개선되지 않을 수도 있다.

심하면 느려질 수도 있다.

---

# 96. 그래서 Specialized Accelerator

GPU가 잘 지원하지 않는 효율적인 Compression/Arithmetic을 위해

```text
NPU

AI Accelerator

On-device AI Accelerator
```

같은 전용 Hardware를 설계할 수 있다.

---

# 97. 현대 Mobile SoC

Mobile Device에는 흔히

```text
CPU

GPU

NPU
```

같은 여러 Compute Unit이 함께 들어간다.

각각 잘하는 Workload가 다르다.

---

# 98. NPU의 역할

NPU는 Neural Network의 특정 Operation이나 저정밀 연산을 효율적으로 수행하도록 설계할 수 있다.

즉

```text
Model Compression Technique
+
Specialized Hardware
```

를 결합하면 Resource Efficiency를 더 크게 높일 수 있다.

---

# 99. On-device AI

On-device 환경에서는 Resource Constraint가 더욱 심하다.

```text
Memory 제한

Battery 제한

Thermal 제한

Compute 제한
```

이 있다.

따라서 Model Compression의 중요성이 더욱 커진다.

---

# 100. Capsule Endoscopy 예

강의에서는 극단적인 Embedded AI 사례로 Capsule Endoscopy를 든다.

몸 안에 들어가는 작은 Capsule에는

```text
큰 GPU Server
```

를 넣을 수 없다.

매우 작은 Hardware 안에서

```text
Camera

Processor

AI Model

Power
```

를 모두 처리해야 한다.

이러한 환경에서는 본격적인 Resource-efficient AI 설계가 필요하다.

---

# 101. 모델 경량화의 목적을 세 가지 질문으로 기억

### Memory

```text
Model이 Hardware에 들어가는가?
```

### Performance

```text
필요한 속도로 실행되는가?
```

### Energy

```text
전력·열 Budget 안에서
실행할 수 있는가?
```

이다.

---

# 102. Accuracy까지 추가하면

실제 Optimization은 네 축을 가진다.

```text
Accuracy

Model Size

Latency

Energy
```

이다.

---

# 103. 이 네 개를 동시에 모두 최적화하기는 어렵다

예:

```text
Bit-width ↓
↓
Model Size ↓
Compute ↓

하지만
Precision ↓
↓
Accuracy ↓ 가능
```

이다.

---

# 104. 그래서 경량화에는 “정답”이 없다

목표 Application에 따라 최적점이 달라진다.

### Cloud LLM

```text
Throughput

GPU Memory

Serving Cost
```

가 중요할 수 있다.

### Smartphone

```text
Energy

Latency

Memory
```

가 중요하다.

### Tiny Embedded

```text
Memory

Power

Hardware Area
```

가 매우 중요할 수 있다.

---

# 105. 수 체계와 모델 경량화의 연결

앞에서 배운 Number Representation은 단순 Computer Architecture 이론이 아니다.

예를 들어

```text
FP32
↓
INT8
```

로 바꾸면 Parameter 하나당 Bit 수가

```text
32
↓
8
```

로 줄어든다.

---

# 106. Model Memory 변화

Parameter가 `N`개라면 대략

```text
FP32:
32N bit

INT8:
8N bit
```

가 된다.

즉 Weight Storage 기준으로

```text
약 1/4
```

까지 줄일 가능성이 있다.

---

# 107. 더 낮추면

```text
FP32
→ INT4
```

이면 Bit-width 자체는

```text
32
→ 4
```

이므로 Weight Storage 관점에서 최대

```text
1/8
```

수준의 표현을 생각할 수 있다.

실제 Model 전체 Memory 절감률은 추가 Metadata와 Representation 방식 등에 따라 달라질 수 있다.

---

# 108. 이것이 Quantization의 출발점

즉

> **Model이 정말 FP32 Precision을 모두 필요로 하는가?**

라는 질문이다.

필요하지 않다면 낮은 Precision으로 바꾸어 Resource를 절약한다.

뒤에서 배우게 될 **Quantization**의 핵심 문제다.

---

# 109. 하지만 Bit만 줄인다고 항상 빨라지는 것은 아니다

다시 Hardware 궁합 문제다.

```text
INT4 Model
```

을 만들었다고 해도 Target GPU가 해당 INT4 Operation을 효율적으로 지원하지 않는다면 기대한 Speed-up이 나오지 않을 수 있다.

---

# 110. 따라서 실제 Performance를 확인해야 한다

이론적 Metric:

```text
Parameter Count

Bit-width

FLOPs
```

도 중요하지만,

실제 System에서는

```text
Wall-clock Latency

Throughput

Memory Usage

Power
```

를 측정해야 한다.

---

# 111. FLOPs가 줄면 항상 빨라지는가?

강의의 전체 관점에서 보면 반드시 그렇지 않다.

실제 Speed는

```text
Hardware Support

Memory Access

Parallelism

Operator Implementation
```

등의 영향을 받는다.

따라서 Algorithm Complexity와 실제 Hardware Performance는 구분해야 한다.

---

# 112. Arithmetic Complexity와 AI가 연결되는 이유

Neural Network의 핵심은 결국 대규모

```text
Addition

Multiplication
```

의 반복이다.

특히

```text
Matrix Multiplication
```

이 핵심 연산이다.

따라서 Parameter와 Activation을 어떤 Number Format으로 표현하는지가 전체 AI Compute Cost에 직접 영향을 준다.

---

# 113. Multiply–Accumulate

Neural Network에서는 대표적으로

```text
a × b + c
```

같은

```text
MAC
= Multiply–Accumulate
```

Operation이 매우 많이 발생한다.

따라서 Multiplication과 Addition을 저비용 Precision으로 바꿀 수 있으면 전체 Model Efficiency가 크게 달라질 수 있다.

---

# 114. 하나의 Operation 차이가 큰 이유

연산 한 번의 차이는 작아 보일 수 있다.

하지만 Neural Network에서는

```text
Operation Cost 차이
×
Billions / Trillions of Operations
```

이 된다.

따라서 작은 Arithmetic Optimization도 전체 System에서는 큰 차이를 만든다.

---

# 115. Memory Access도 중요

Model Weight가 작아지면 단순 Storage만 줄어드는 것이 아니다.

GPU가 Memory에서 가져와야 하는 Data 양도 줄어든다.

```text
Bit-width ↓
↓
Memory Traffic ↓
↓
Bandwidth Pressure ↓
```

가 가능하다.

---

# 116. AI에서는 Compute만큼 Memory가 중요할 수 있다

특정 Workload에서는 Arithmetic Unit보다

```text
Memory에서 Weight를 가져오는 시간
```

이 Bottleneck일 수 있다.

따라서 Compression은

```text
Compute Optimization
+
Memory Optimization
```

둘 다 가능하다.

---

# 117. 이번 강의의 가장 중요한 사고방식

경량화를 보면서 단순히

```text
이 기법은 Model을 몇 % 줄인다.
```

만 보면 안 된다.

반드시 다음 질문을 해야 한다.

```text
무엇을 줄였는가?

Accuracy는 얼마나 떨어졌는가?

실제 Hardware에서 빨라졌는가?

Memory는 실제로 줄었는가?

Energy는 실제로 줄었는가?
```

---

# 118. Model Compression 평가 구조

```text
Original Model
↓
Compression
↓
Compressed Model
↓
다음 네 축 평가

1. Accuracy

2. Size

3. Speed

4. Energy
```

이다.

---

# 119. Optimization Target에 따른 예

### Case A

```text
GPU Memory 부족
```

→ Model Size가 최우선.

### Case B

```text
Real-time Service
```

→ Latency가 최우선.

### Case C

```text
Mobile / Battery
```

→ Energy가 최우선.

### Case D

```text
Cloud Service
```

→ Throughput / Cost per Request가 중요.

---

