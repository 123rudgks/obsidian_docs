# Java 모의고사 1회 - 문제

> 시험 범위 : 변수, 형변환, 조건문, 배열, JVM 메모리, 객체지향, 제네릭, 컬렉션(List/Set/Map), 객체 정렬, 스트림, 예외 처리
> 문항 구성 : 객관식 21문항 / 단답형 9문항 / 서술형 2문항
> 해설 : [[모의고사 1회 - 해설]]

---

## Ⅰ. 객관식 (1 ~ 21번, 보기 5개 중 1개 선택)

### 1번
다음 코드의 실행 결과는?
```java
int i1 = Integer.MAX_VALUE;
int i2 = i1 + 1;
System.out.println(i2);
```
① 2147483648
② -2147483648
③ 0
④ 컴파일 에러가 발생한다
⑤ 실행 시 ArithmeticException이 발생한다

### 2번
다음 코드의 실행 결과를 순서대로 나열한 것은?
```java
char c = '0';
System.out.println(c == 0);
System.out.println(c == 48);
System.out.println('A' + 32 == 'a');
```
① true / true / true
② false / true / true
③ false / false / true
④ true / false / false
⑤ false / true / false

### 3번
다음 중 컴파일 에러가 발생하지 **않는** 것은?
```java
byte b1 = 10; byte b2 = 20;
int i1 = 10;  long lv = 20;
```
① `byte b3 = b1 + b2;`
② `int i2 = i1 + lv;`
③ `float f1 = 10.0;`
④ `long l1 = i1 + 1;`
⑤ `float f2 = 10.0f + 20.0;`

### 4번
다음 코드의 실행 결과는?
```java
int num = 3;
switch (num) {
    case 1: System.out.println(1);
    case 2: System.out.println(2);
    case 3: System.out.println(3);
    case 4: System.out.println(4);
    case 5: break;
    default: System.out.println("default");
}
```
① `3`만 출력된다
② `3`, `4`가 출력된다
③ `3`, `4`, `default`가 출력된다
④ 아무것도 출력되지 않는다
⑤ 컴파일 에러가 발생한다

### 5번
다음 중 4x3 크기의 2차원 배열 생성 코드로 **잘못된** 것은?
① `int[][] a = new int[4][3];`
② `int[] b[] = new int[][3];`
③ `int[][] c = new int[4][];`
④ `int[][] d = new int[][]{{1,2,3},{1,2,3},{1,2,3},{1,2,3}};`
⑤ `int[][] e = {{1,2,3},{1,2,3},{1,2,3},{1,2,3}};`

### 6번
JVM 메모리 구조에 대한 설명으로 **옳지 않은** 것은?
① 객체는 Heap 영역에 저장된다
② 메서드의 지역 변수는 Stack 영역에 저장된다
③ static 변수는 Heap 영역에 저장된다
④ Stack은 스레드별로 별도 관리된다
⑤ Heap은 여러 스레드에 의해 공유된다

### 7번
다음 코드의 출력 결과를 순서대로 나열한 것은?
```java
class Animal {
    String type = "동물";
    public String sound() { return "..."; }
    public String getType() { return type; }
}
class Dog extends Animal {
    String type = "개";
    @Override public String sound() { return "멍멍!"; }
    @Override public String getType() { return type; }
}
public class Main {
    public static void main(String[] args) {
        Animal animal = new Dog();
        System.out.println(animal.type);
        System.out.println(animal.sound());
        System.out.println(animal.getType());
    }
}
```
① 동물 / ... / 동물
② 개 / 멍멍! / 개
③ 동물 / 멍멍! / 개
④ 개 / ... / 동물
⑤ 동물 / 멍멍! / 동물

### 8번
메서드 오버라이딩의 조건으로 **옳지 않은** 것은?
① 메서드 이름이 같아야 한다
② 매개변수의 개수, 타입, 순서가 같아야 한다
③ 리턴 타입이 같아야 한다
④ 접근 제한자는 부모보다 범위가 넓거나 같아야 한다
⑤ 부모 메서드가 던지는 예외보다 더 큰(상위) 예외를 던질 수 있다

### 9번
다음 중 컴파일 에러가 발생하지 **않는** 것은? (`GenericBox<T>`는 제네릭 클래스, `obj`는 `GenericBox<SpiderMan>` 타입)
① `T item = new T();`
② `if (obj instanceof GenericBox<String>) { }`
③ `GenericBox<String>[] boxes = new GenericBox<String>[3];`
④ `if (obj instanceof GenericBox<?>) { }`
⑤ `static T item;` (제네릭 클래스의 멤버 선언)

### 10번
컬렉션 프레임워크의 3대 인터페이스에 대한 설명으로 **옳지 않은** 것은?
① List는 입력 순서가 있고 중복을 허용한다
② Set은 입력 순서가 없고 중복을 허용하지 않는다
③ Map은 key의 중복은 불가하지만 value의 중복은 가능하다
④ List와 Set은 Collection 인터페이스를 상속받는다
⑤ Map은 Collection 인터페이스를 상속받는다

### 11번
ArrayList에 대한 설명으로 **옳지 않은** 것은?
① 내부적으로 `Object[]` 배열에 데이터를 저장한다
② 인덱스를 통한 접근 속도가 빠르다
③ 용량이 부족하면 기존 용량의 약 1.5배 크기의 새 배열을 만들어 복사한다
④ 맨 앞에 요소를 삽입하면 기존 요소 전체를 뒤로 밀어야 한다
⑤ 요소를 삭제하면 내부 배열의 용량(capacity)도 즉시 줄어든다

### 12번
다음 코드의 실행 결과는?
```java
List<Integer> nums = new ArrayList<>(List.of(1, 2, 2, 3, 4));
for (int i = 0; i < nums.size(); i++) {
    if (nums.get(i) % 2 == 0) {
        nums.remove(i);
    }
}
System.out.println(nums);
```
① `[1, 3]`
② `[1, 2, 3]`
③ `[1, 3, 4]`
④ ConcurrentModificationException이 발생한다
⑤ IndexOutOfBoundsException이 발생한다

### 13번
다음 코드의 실행 결과는?
```java
List<Integer> nums = new ArrayList<>(List.of(1, 2, 3, 4));
for (Integer num : nums) {
    if (num % 2 == 0) {
        nums.remove(num);
    }
}
System.out.println(nums);
```
① `[1, 3]`
② `[1, 2, 3, 4]`
③ `[2, 4]`
④ ConcurrentModificationException이 발생한다
⑤ 컴파일 에러가 발생한다

### 14번
다음 코드의 출력 값은?
```java
Set<Object> set = new HashSet<>();
set.add("Hello");
set.add(new String("Hello"));
set.add(1);
set.add(Integer.valueOf(1));
System.out.println(set.size());
```
① 1
② 2
③ 3
④ 4
⑤ 실행 시 예외가 발생한다

### 15번
다음 코드의 출력 결과를 순서대로 나열한 것은?
```java
Map<String, String> map = new HashMap<>();
System.out.println(map.put("andy", "1234"));
System.out.println(map.put("andy", "4567"));
map.putIfAbsent("andy", "9999");
System.out.println(map.get("andy"));
```
① null / 1234 / 4567
② null / null / 9999
③ 1234 / 4567 / 9999
④ null / 1234 / 9999
⑤ 1234 / 1234 / 4567

### 16번
다음 코드를 실행하면 어떻게 되는가?
```java
List<String> list = List.of("a", "b", "c");
list.add("d");
```
① `[a, b, c, d]`가 된다
② 컴파일 에러가 발생한다
③ UnsupportedOperationException이 발생한다
④ NullPointerException이 발생한다
⑤ 아무 일도 일어나지 않는다 (add가 무시된다)

### 17번
다음 코드를 컴파일/실행하면 어떻게 되는가? (SmartPhone은 어떤 인터페이스도 구현하지 않은 일반 클래스)
```java
class SmartPhone {
    String number;
    public SmartPhone(String number) { this.number = number; }
}
// ...
List<SmartPhone> phones = new ArrayList<>(List.of(
    new SmartPhone("010"), new SmartPhone("011")));
Collections.sort(phones);
```
① 번호 순으로 정렬된다
② 객체가 추가된 순서가 유지된다
③ 실행 시 ClassCastException이 발생한다
④ 컴파일 에러가 발생한다
⑤ 실행 시 UnsupportedOperationException이 발생한다

### 18번
다음 코드의 실행 결과는?
```java
List<String> names = new ArrayList<>(List.of("Java", "Hi", "World"));
Collections.sort(names, (s1, s2) -> s1.length() - s2.length());
System.out.println(names);
```
① `[Hi, Java, World]`
② `[World, Java, Hi]`
③ `[Hi, World, Java]`
④ `[Java, Hi, World]`
⑤ `[Java, World, Hi]`

### 19번
다음 코드의 출력 값은?
```java
List<String> heros = List.of("superman", "batman", "ironman", "hulk");
double avg = heros.stream()
        .mapToInt(String::length)
        .filter(len -> len > 4)
        .average()
        .getAsDouble();
System.out.println(avg);
```
① 6.25
② 6.0
③ 7.0
④ 7.5
⑤ 21.0

### 20번
다음 코드의 실행 결과는?
```java
List<Integer> nums = List.of(5, 3, 8, 1, 9, 2);
List<Integer> result = nums.stream()
        .filter(n -> n > 2)
        .sorted()
        .limit(3)
        .toList();
System.out.println(result);
```
① `[5, 3, 8]`
② `[3, 5, 8]`
③ `[1, 2, 3]`
④ `[8, 9]`
⑤ `[3, 5, 8, 9]`

### 21번
다음 코드에서 num이 0일 때 출력되는 내용은?
```java
public static void main(String[] args) {
    int num = new Random().nextInt(2);
    try {
        System.out.println("code 1, num: " + num);
        int i = 1 / num;
        System.out.println("code 2 - 예외 없음");
    } catch (ArithmeticException e) {
        System.out.println("code 3 - 예외 처리 완료");
    }
    System.out.println("code 4");
}
```
① code 1 → code 2 → code 4
② code 1 → code 3 → code 4
③ code 1 → code 3 (code 4는 출력 안 됨)
④ code 1 만 출력되고 프로그램 종료
⑤ code 1 → code 2 → code 3 → code 4

---

## Ⅱ. 단답형 (22 ~ 30번)

### 22번
ASCII 코드에서 문자 `'A'`에 해당하는 10진수 값을 쓰시오.

### 23번
객체지향 언어의 4가지 특징을 모두 쓰시오. (힌트 : OOP is A P.I.E)

### 24번
배열을 복사할 때 사용하는 대표적인 static 메소드 2가지를 쓰시오. (클래스명.메소드명 형태)

### 25번
`equals()`를 재정의할 때 반드시 함께 재정의해야 하는 Object의 메소드는?

### 26번
ArrayList가 내부적으로 데이터를 저장하는 배열의 타입은?

### 27번
HashSet이 내부적으로 데이터를 저장할 때 사용하는 컬렉션은?

### 28번
`Collections.sort(list)`로 정렬하려면 리스트 요소의 클래스가 구현해야 하는 인터페이스와, 그때 구현해야 하는 추상 메소드 이름을 쓰시오.

### 29번
스트림 파이프라인에서 중복 요소를 제거하는 중간 연산 메소드의 이름은?

### 30번
for-each 문으로 컬렉션을 순회하는 도중 해당 컬렉션의 크기를 변경(add/remove)하면 발생하는 예외의 이름은?

---

## Ⅲ. 서술형 (31 ~ 32번)

### 31번
ArrayList와 LinkedList의 내부 구조 차이를 설명하고, 아래 각 상황에서 어느 쪽이 유리한지 이유와 함께 서술하시오.
1. 맨 앞에 데이터 추가
2. 중간 요소 조회(인덱스 접근)
3. 반복자(Iterator)를 이용해 중간에 데이터 100개 연속 추가

### 32번
HashSet이 두 객체를 "같은 원소"로 판단하는 과정을 설명하시오. 또한 아래 코드에서 두 SmartPhone 객체가 중복으로 처리되지 않는 이유와, 중복으로 처리되게 만들기 위해 해야 하는 일을 코드와 함께 서술하시오.
```java
Set<SmartPhone> set = new HashSet<>();
set.add(new SmartPhone("010-111-2222"));
set.add(new SmartPhone("010-111-2222"));
System.out.println(set.size()); // 2가 출력됨
```
