# 확인문제 50제

> 통합 학습 노트(1장~6.3장) 전체 범위를 다룹니다. 먼저 풀어보고, 맨 아래 정답 및 해설로 채점하세요.

---

## 1장. 선형 회귀

**1.** 손실함수(Loss Function)의 역할로 가장 알맞은 것은? a. 입력 데이터를 무작위로 섞는다 b. 모델의 예측이 얼마나 잘못되었는지 측정한다 c. 파라미터를 업데이트하는 규칙을 제공한다 d. 학습률(learning rate)을 설정한다

**2.** 가장 널리 쓰이는 손실함수인 MSE(평균제곱오차)의 수식으로 옳은 것은? a. $\sum(y-\hat y)$ b. $\frac{1}{N}\sum|y-\hat y|$ c. $\frac{1}{N}\sum(y-\hat y)^2$ d. $\sum(y-\hat y)^2$

**3.** 단순선형회귀가 경사하강법 없이 공식으로 한 번에 구해지는 이유로 가장 알맞은 것은? a. 데이터가 적기 때문에 b. 손실함수(RSS)가 w, b에 대한 2차식이라 미분=0인 지점이 유일한 최솟값이기 때문 c. 파라미터가 2개뿐이기 때문 d. 선형회귀는 원래 반복 계산이 불가능하기 때문

**4.** 회귀 결과 해석에서 p-value가 의미하는 것은? a. 예측값과 실제값의 차이 b. 계수가 실제로는 0일 확률(=x,y 관계없음) c. 데이터의 표준편차 d. 모델의 전체 정확도

**5.** 회귀 결과를 해석할 때 가장 주의해야 할 점은? a. Coefficient 크기만 보고 영향력의 크고 작음을 판단해도 된다 b. Std.Error는 무시해도 된다 c. Coefficient 크기만으로 판단하지 말고 Std.Error, t-statistic, p-value를 함께 봐야 한다 d. t-statistic이 0이면 유의미하다는 뜻이다

---

## 2장. 다중 선형 회귀

**6.** 회귀 가중치와 상관계수의 차이에 대한 설명으로 옳은 것은? a. 둘은 완전히 같은 개념이다 b. 상관계수는 나머지 변수를 고정한 채 한 변수의 순수 영향력을 보여준다 c. 회귀 가중치는 나머지 변수를 고정한 채 그 변수만 바꿨을 때의 영향력이고, 상관계수는 단순 2변수 관계다 d. 회귀 가중치는 항상 상관계수보다 크다

**7.** 다중선형회귀의 최소제곱법 공식으로 옳은 것은? a. $\hat{\boldsymbol\beta}=\mathbf X^\top\mathbf y$ b. $\hat{\boldsymbol\beta}=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$ c. $\hat{\boldsymbol\beta}=\mathbf X(\mathbf X^\top\mathbf X)^{-1}$ d. $\hat{\boldsymbol\beta}=(\mathbf X\mathbf X^\top)^{-1}\mathbf y$

**8.** 다중선형회귀도 닫힌 형태의 공식이 있는데, 굳이 경사하강법이 필요한 이유는? a. 공식이 수학적으로 틀렸기 때문에 b. 변수(차원)가 많아지면 행렬 연산량이 폭발적으로 증가해 물리적으로 계산이 불가능해지기 때문 c. 경사하강법이 항상 더 정확하기 때문에 d. 다중선형회귀는 원래 공식이 존재하지 않기 때문에

**9.** "아이스크림 판매량과 상어 물림 사건 수가 함께 증가한다"는 예시가 보여주는 것은? a. 아이스크림이 상어를 유인한다 b. 상관관계가 곧 인과관계다 c. 상관관계 ≠ 인과관계 (공통 원인인 "여름" 때문일 뿐) d. 두 변수는 아무 관계가 없다

**10.** 다중선형회귀 해석 시 변수들이 서로 독립적이지 않으면(상관되어 있으면) 어떤 문제가 생기는가? a. 문제없이 정확한 계수가 나온다 b. 계수 추정이 불안정해지고 해석이 혼동될 수 있다 c. 손실함수가 convex가 아니게 된다 d. 학습 속도가 빨라진다

---

## 3장. 분류와 로지스틱 회귀

**11.** 분류 문제에 선형회귀를 그대로 쓰면 안 되는 이유는? a. 선형회귀는 계산이 너무 느려서 b. 선형회귀의 출력값이 $-\infty\sim+\infty$라 확률(0~1) 범위를 벗어날 수 있어서 c. 분류는 손실함수를 쓸 수 없어서 d. 선형회귀는 파라미터가 없어서

**12.** 시그모이드 함수의 출력 범위는? a. $-1\sim1$ b. $-\infty\sim+\infty$ c. $0\sim1$ d. $0\sim\infty$

**13.** 오즈(Odds)의 정의로 옳은 것은? a. $p+(1-p)$ b. $p\times(1-p)$ c. $p/(1-p)$ (성공확률/실패확률) d. $\log(p)$

**14.** 로지스틱 회귀의 파라미터(β)를 추정할 때 사용하는 지표는? a. MSE(평균제곱오차) b. MAE(평균절대오차) c. 우도(Likelihood), MLE d. 상관계수

**15.** 우도(Likelihood) 대신 로그를 취한 log-likelihood를 사용하는 이유는? a. 값을 더 크게 만들기 위해 b. 곱셈(Π)을 덧셈으로 바꿔 미분을 쉽게 하기 위해 (log 변환해도 최댓값 위치는 동일) c. 확률을 0~1 범위로 강제하기 위해 d. 계산 속도와는 아무 관련 없다

**16.** 로지스틱 회귀는 선형회귀처럼 닫힌 형태(공식)로 파라미터를 바로 구할 수 있는가? a. 그렇다, 선형회귀와 동일한 공식을 쓴다 b. 아니다, 우도식의 특성상 경사하강법 같은 반복적 최적화가 필요하다 c. 로지스틱 회귀는 파라미터가 없다 d. 로지스틱 회귀는 학습이 필요 없다

---

## 4장. 회귀 → 신경망으로의 연결

**17.** 선형회귀와 로지스틱 회귀의 공통적인 근본 한계는? a. 데이터가 많아야 한다 b. "입력들의 가중합 + 편향" 하나로만 결과를 표현해서, 직선(또는 평면) 하나만 표현 가능하다 c. 계산 속도가 느리다 d. 파라미터가 너무 많다

**18.** Shallow network의 구조 정의로 옳은 것은? a. 은닉층이 2개 이상인 신경망 b. 입력층 - 은닉층 1개 - 출력층으로 구성된 신경망 c. 은닉층이 없는 신경망 d. 출력층이 여러 개인 신경망

**19.** "표현력(expressive power)"이 의미하는 것은? a. 모델의 학습 속도 b. 모델이 표현/근사할 수 있는 함수의 복잡도·다양성 c. 모델의 파라미터 개수 그 자체 d. 데이터의 개수

**20.** 단순선형회귀(파라미터 2개)와 은닉유닛 3개짜리 Shallow network(파라미터 10개)를 비교했을 때, 옳은 설명은? a. 둘 다 표현 가능한 형태는 직선 1개로 동일하다 b. 파라미터 수가 다르므로 비교 자체가 불가능하다 c. Shallow network는 비선형 곡선까지 표현 가능해 표현력이 더 높다 d. 단순선형회귀가 항상 더 우수하다

---

## 5장. 경사하강법과 발전 과정

**21.** 경사하강법에서 (초기화 이후) 매 반복(iteration)의 첫 단계에 해당하는 것은? a. 파라미터를 무작위로 초기화한다 b. 현재 파라미터에서 손실함수의 기울기(미분값)를 계산한다 c. 데이터를 섞어 미니배치를 생성한다 d. 학습을 종료한다

**22.** 학습률(η)에 대한 설명으로 옳은 것은? a. 클수록 항상 좋다 b. 너무 크면 최솟값을 지나쳐 발산할 수 있고, 너무 작으면 수렴이 느리다 c. 학습률은 파라미터 개수를 의미한다 d. 학습률은 손실함수의 종류를 결정한다

**23.** Convex(볼록) 문제의 특징으로 옳은 것은? a. 2차 미분이 항상 0이다 b. 2차 미분이 모든 구간에서 음수이다 c. 2차 미분이 모든 구간에서 양수(또는 0)이며, 국소 최솟값이 곧 전역 최솟값이다 d. 미분값이 존재하지 않는다

**24.** 확률적 경사하강법(SGD)의 장점으로 가장 알맞은 것은? a. 항상 전역 최적해(global optimum)에 도달한다 b. 일부 데이터만으로 업데이트해 스텝당 연산 비용이 작다 c. 항상 같은 결과로 수렴한다 d. 계산 비용이 커서 정밀하다

**25.** SGD가 경사하강법(full-batch GD)보다 항상 수렴 속도가 빠른가? a. 그렇다, 항상 빠르다 b. 아니다, convex 문제에서는 오히려 GD의 수렴이 더 빠를 수 있다 (SGD가 빠른 건 스텝당 계산 비용일 뿐) c. SGD와 GD는 속도 차이가 전혀 없다 d. SGD는 애초에 수렴하지 않는다

**26.** 미니배치 경사하강법과 에포크(Epoch)에 대한 설명으로 옳은 것은? a. 에포크는 파라미터를 한 번 업데이트하는 것을 의미한다 b. 미니배치는 데이터 1개씩만 사용한다 c. 미니배치는 배치 단위 평균 기울기로 업데이트하며 병렬 연산이 가능하고, 에포크는 전체 데이터셋을 한 번 다 사용한 상태를 의미한다 d. 에포크가 많을수록 미니배치 크기가 커진다

---

## 6.1장. Shallow 네트워크

**27.** "모수적 함수(parametric function)"의 의미로 옳은 것은? a. 함수 형태 자체가 데이터에 따라 계속 바뀌는 함수 b. 함수의 형태는 고정되고, 파라미터 값에 따라 함수가 완전히 결정되는 함수 c. 파라미터가 없는 함수 d. 오직 선형회귀에만 적용되는 개념

**28.** XOR 문제가 보여주는 것은? a. 신경망은 XOR을 풀 수 없다 b. 단순 선형모델(직선 하나)로는 XOR을 분류할 수 없지만, 은닉층 하나만 추가해도 풀 수 있다 c. XOR은 로지스틱 회귀로도 절대 풀 수 없다 d. XOR은 다중선형회귀로도 절대 풀 수 없다

**29.** 다중선형회귀와 신경망의 진짜 차이점은? a. 다중선형회귀는 여러 변수를 다룰 수 없다 b. 다중선형회귀는 상호작용(교호작용) 항을 사람이 직접 설계해야 하지만, 신경망은 이를 데이터로부터 자동으로 학습한다 c. 신경망은 변수 조합을 전혀 고려하지 못한다 d. 다중선형회귀가 신경망보다 항상 표현력이 높다

**30.** Shallow network 수식 $y=\phi_0+\phi_1a[\theta_{10}+\theta_{11}x]+\cdots$에서 θ와 φ에 대한 설명으로 옳은 것은? a. θ는 출력층 가중치, φ는 입력층 가중치다 b. θ는 "입력→은닉유닛" 사이의 가중치·절편, φ는 "은닉유닛→출력" 사이의 가중치·절편이다 c. θ와 φ는 완전히 같은 값이다 d. φ만 파라미터이고 θ는 활성화 함수의 일부다

**31.** 활성화 함수가 없다면 은닉 유닛을 아무리 많이 쌓아도 어떻게 되는가? a. 표현력이 무한히 커진다 b. 계산이 불가능해진다 c. 선형함수의 합성은 여전히 선형함수라서, 결국 단순선형회귀와 동일해진다 d. 항상 XOR을 풀 수 있게 된다

**32.** ReLU 활성화 함수의 정의로 옳은 것은? a. 항상 입력값을 그대로 출력한다 b. $z<0$이면 0, $z\geq0$이면 $z$를 그대로 출력한다 c. 출력을 항상 0~1 사이로 강제한다 d. 입력의 부호를 반전시킨다

**33.** Hidden unit(은닉 유닛)의 역할로 가장 알맞은 것은? a. 손실함수의 종류를 결정한다 b. 입력 x의 특정 구간에서만 켜지는 미니 선형 조각으로, 여러 개가 조합되면 piecewise-linear 함수를 만든다 c. 학습률을 자동으로 조절한다 d. 항상 출력값을 그대로 통과시킨다

**34.** "은닉 유닛을 늘리면 표현력이 커지지만 과적합 위험은 상대적으로 낮다"고 할 때, 이 비교의 기준은? a. 단순선형모델과의 비교 b. 로지스틱 회귀와의 비교 c. Deep network(은닉층 여러 개)와의 비교 d. 아무런 비교 대상이 없다

**35.** 보편적 근사 정리(Universal Approximation)가 말하는 것은? a. 신경망은 이미지만 근사할 수 있다 b. 은닉층 1개짜리 shallow network도 hidden unit 수만 충분하면 임의의 연속함수를 원하는 정확도로 근사할 수 있다 c. Deep network만 임의의 함수를 근사할 수 있다 d. Hidden unit 개수와 근사 정확도는 무관하다

**36.** 다중 출력(예: $y_1, y_2$)을 만드는 신경망의 계산 방식에 대한 설명으로 옳은 것은? a. 조건에 따라 $y_1$ 또는 $y_2$ 중 하나만 계산되는 분기(if-else) 처리다 b. 같은 hidden unit 값들을 원재료로, 출력마다 다른 가중치를 곱해 동시에 계산하는 행렬 곱셈이다 c. 각 출력마다 완전히 다른 hidden unit을 사용한다 d. 다중 출력은 신경망에서 구현이 불가능하다

---

## 6.2장. Deep Network

**37.** Deep network를 "2개의 네트워크를 합성"하는 방식으로 만들 때의 핵심은? a. 두 네트워크가 서로 같은 파라미터를 공유한다 b. Network 1의 출력이 그대로 Network 2의 입력이 되며, 두 네트워크는 독립적인 파라미터를 가진다 c. 두 네트워크는 서로 아무 관계가 없다 d. Network 2가 먼저 계산되고 그 다음 Network 1이 계산된다

**38.** "종이접기(folding)" 비유가 설명하는 것은? a. 신경망의 파라미터 개수가 종이처럼 얇다는 것 b. 이전 층의 함수가 비단조(non-monotonic)이면, 서로 다른 입력 구간들이 같은 값으로 "접혀서" 다음 층의 처리를 공유하게 되는 현상 c. 신경망 학습이 종이 위에서 이루어진다는 것 d. 활성화 함수가 종이를 접는 것처럼 작동한다는 것

**39.** 파라미터 수가 비슷한 Shallow network(19개)와 Deep network(20개)를 비교했을 때, 노트에서 확인한 결과는? a. 둘의 조각적 선형 구역 수가 완전히 같다 b. Shallow network가 더 많은 구역을 표현한다 c. Deep network(16개 구역)가 Shallow network(7개 구역)보다 훨씬 많은 구역을 표현한다 d. 파라미터 수가 비슷하면 비교 자체가 무의미하다

**40.** Deep network의 표현력이 Shallow network보다 효율적으로 커지는 이유는? a. Deep network는 활성화 함수를 사용하지 않기 때문에 b. Shallow network는 유닛이 병렬로만 작동해 구역 수가 선형적으로 증가하지만, Deep network는 "접힘"이 반복되어 구역 수가 훨씬 빠르게(거듭제곱적으로) 증가하기 때문 c. Deep network는 파라미터가 항상 더 많기 때문에 d. Shallow network는 애초에 비선형을 표현할 수 없기 때문에

**41.** 2D 입력으로 folding 예시를 확장했을 때(등고선 그림), d) 그래프가 b) 그래프보다 훨씬 복잡한 패턴을 보이는 이유는? a. 입력 차원이 다르기 때문에 b. c)가 비단조(텐트 모양)라서, b)의 등고선들이 여러 번 접혀 반사되기 때문 c. d) 그래프는 실제로는 아무 의미가 없기 때문에 d. 활성화 함수가 2D에서는 다르게 작동하기 때문에

**42.** Deep network를 행렬·벡터로 표기할 때, $\boldsymbol\Omega_k$와 $\boldsymbol\beta_k$가 각각 의미하는 것은? a. $\boldsymbol\Omega_k$=편향 벡터, $\boldsymbol\beta_k$=가중치 행렬 b. $\boldsymbol\Omega_k$=가중치 행렬(Weight matrix), $\boldsymbol\beta_k$=편향 벡터(Bias vector) c. 둘 다 활성화 함수를 의미한다 d. 둘 다 손실함수를 의미한다

**43.** 은닉층이 K개인 Deep network의 일반화된 계산 구조로 옳은 것은? a. 모든 층을 한꺼번에 동시에 계산한다 b. $\mathbf{h}_k=a[\boldsymbol\beta_{k-1}+\boldsymbol\Omega_{k-1}\mathbf{h}_{k-1}]$ 형태의 동일한 연산을 층마다 반복 적용한다 c. 각 층마다 완전히 다른 형태의 수식을 사용한다 d. 층이 깊어질수록 활성화 함수를 사용하지 않는다

**44.** "복잡해 보이는 Deep network 수식"의 정체를 한 문장으로 정리하면? a. 매 층마다 완전히 새로운 연산 방식이 필요하다 b. (가중치 행렬 곱 → 편향 더하기 → 활성화 함수)를 레이어마다 반복하는 것 c. 딥러닝은 선형회귀와 아무 관련이 없다 d. 층이 깊어지면 파라미터가 줄어든다

---

## 6.3장. 역전파

**45.** 역전파(Backpropagation)에 대한 정의로 가장 알맞은 것은? a. 파라미터를 무작위로 초기화하는 절차 b. 출력 오차를 기준으로 계산 그래프를 거꾸로(출력층→입력층) 따라가며 연쇄법칙으로 각 층의 미분값을 구하는 절차 c. 순전파와 완전히 동일한 과정 d. 학습률을 자동으로 조정하는 알고리즘

**46.** 역전파가 기반으로 하는 핵심 수학 원리는? a. 적분 b. 연쇄법칙(chain rule) — 합성함수의 변화율 = 바깥의 변화율 × 안쪽의 변화율 c. 행렬의 역행렬 d. 삼각함수 항등식

**47.** 역전파의 계산이 시작되는 지점은 어디인가? a. 입력층, 입력값으로부터 직접 시작 b. 은닉층 중간 지점 c. 출력층 — 손실함수 정의로부터 "출력이 바뀌면 손실이 얼마나 바뀌는가"를 직접 계산 가능 d. 무작위로 정해진 층

**48.** 역전파가 효율적인 이유로 가장 알맞은 것은? a. 모든 층의 기울기를 처음부터 각각 새로 계산하기 때문 b. 각 층이 바로 다음 층에서 이미 계산해둔 기울기를 재사용하기 때문 c. 활성화 함수를 사용하지 않기 때문 d. 파라미터가 적기 때문

**49.** 역전파와 경사하강법의 관계에 대한 설명으로 옳은 것은? a. 둘은 완전히 같은 알고리즘이다 b. 역전파는 모든 층의 기울기 계산까지만 하고, 그 기울기로 파라미터를 실제 수정하는 것은 경사하강법의 몫으로 분리되어 있다 c. 경사하강법이 먼저 실행되고, 그 다음 역전파가 실행된다 d. 역전파는 파라미터를 직접 수정하는 알고리즘이다

**50.** 역전파 이후 파라미터 업데이트에 대한 설명으로 옳은 것은? a. 사람이 결과를 보고 직접 하나씩 조정한다 b. 특정 층 하나만 골라서 조정한다 c. 기본적으로 모든 층의 가중치가 자동화된 수식에 의해 동시에 조정된다 (전이학습 등 일부 층을 얼리는 특수한 경우는 예외) d. 파라미터는 한 번 초기화되면 이후 절대 바뀌지 않는다

---

## 정답 및 해설

**1.** b — 손실함수는 모델 예측값과 실제값 사이의 차이를 수치화한 지표다. 학습은 이 값을 최소화하는 방향으로 진행된다.

**2.** c — MSE $=\frac{1}{N}\sum(y-\hat y)^2$. 가장 널리 쓰이는 손실함수로, 미분 가능하고(convex) 이상치에 민감하다는 특징이 있다.

**3.** b — RSS가 w,b에 대한 2차식(볼록함수)이라 미분=0인 지점이 유일한 최솟값이고, 그 지점을 공식으로 바로 계산할 수 있다.

**4.** b — p-value는 계수가 실제로는 0(=관계없음)일 확률이며, 보통 0.05 이하면 통계적으로 유의미하다고 판단한다.

**5.** c — Coefficient 크기만으로는 판단할 수 없고, Std.Error·t-statistic·p-value를 함께 봐야 통계적 유의성을 제대로 판단할 수 있다.

**6.** c — 상관계수는 두 변수의 단순한 관계, 회귀 가중치는 다른 변수를 고정한 채 해당 변수만의 순수한 영향력을 나타낸다.

**7.** b — $\hat{\boldsymbol\beta}=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$가 최소제곱법의 닫힌 형태 해다.

**8.** b — 공식 자체는 존재하지만, 변수(차원)가 많아지면 행렬 연산량이 폭발적으로 증가해 실제로 계산하기 물리적으로 불가능해진다.

**9.** c — 두 변수 모두 "여름"이라는 공통 원인 때문에 함께 증가할 뿐, 서로 인과관계가 있는 것은 아니다. 상관관계와 인과관계는 다르다.

**10.** b — 변수들이 서로 상관되어 있으면(다중공선성) 계수 추정이 불안정해지고 해석이 혼동될 수 있다.

**11.** b — 선형회귀의 출력은 $-\infty\sim+\infty$ 범위라 확률(0~1)을 벗어날 수 있어 분류 문제에 부적절하다.

**12.** c — 시그모이드 함수는 어떤 입력이 와도 항상 0~1 사이의 값만 출력하도록 강제한다.

**13.** c — 오즈(Odds) = 성공확률/실패확률 = $p/(1-p)$.

**14.** c — 로지스틱 회귀는 MSE 대신 우도(Likelihood)를 지표로 사용하며, 우도를 최대화하는 MLE로 파라미터를 추정한다.

**15.** b — 우도식은 곱셈(Π)으로 구성되어 미분이 어려운데, log를 취하면 곱을 합으로 바꿔 미분이 쉬워진다. log 변환해도 최댓값의 위치는 동일하다.

**16.** b — 로지스틱 회귀는 우도식의 특성상 선형회귀처럼 닫힌 형태 공식으로 바로 구할 수 없어, 경사하강법 같은 반복적 최적화가 필요하다.

**17.** b — 둘 다 "가중합 + 편향" 하나로 결과를 표현하는 구조라, 직선(또는 평면) 하나만 표현 가능하다는 근본적 한계를 공유한다.

**18.** b — Shallow network는 입력층 - 은닉층 1개 - 출력층으로 구성된 신경망이다.

**19.** b — 표현력은 모델이 얼마나 다양하고 복잡한 형태의 함수(관계)를 표현/근사할 수 있는지를 뜻한다.

**20.** c — Shallow network는 은닉유닛과 활성화 함수 덕분에 비선형 곡선까지 표현 가능해, 파라미터가 더 많음에도 단순선형회귀보다 표현력이 높다.

**21.** b — (파라미터 초기화는 최초 1회의 준비 단계이고) 매 반복의 첫 단계는 현재 파라미터에서 손실함수의 기울기를 계산하는 것이다.

**22.** b — 학습률이 너무 크면 최솟값을 지나쳐 발산할 수 있고, 너무 작으면 수렴이 느려진다.

**23.** c — Convex 문제는 2차 미분이 모든 구간에서 양수(또는 0)인 형태로, 이 경우 국소 최솟값이 곧 전역 최솟값과 같다.

**24.** b — SGD는 전체 데이터가 아닌 일부(또는 1개) 샘플로 기울기를 계산해 스텝당 연산 비용이 작다.

**25.** b — SGD가 빠른 것은 스텝당 계산 비용이지, 수렴 속도 자체가 아니다. Convex 문제에서는 오히려 GD가 정확한 방향으로 이동해 더 빠르게 수렴할 수 있다.

**26.** c — 미니배치는 배치 단위 평균 기울기로 업데이트하며 병렬 연산이 가능하고, 에포크는 전체 데이터셋을 한 번 다 학습에 사용한 상태를 의미한다.

**27.** b — 모수적 함수는 함수의 형태는 고정되고, 파라미터 값에 따라 함수가 완전히 결정되는 함수다.

**28.** b — XOR은 단순 선형모델(직선 하나)로는 분류 불가능하지만, 은닉층 하나만 추가해도(비선형성 부여) 풀 수 있게 된다.

**29.** b — 다중선형회귀는 상호작용(교호작용) 항을 사람이 직접 설계(feature engineering)해야 하지만, 신경망은 이를 데이터로부터 자동으로 학습한다.

**30.** b — θ는 "입력→은닉유닛" 사이(1층), φ는 "은닉유닛→최종출력" 사이(2층)의 가중치·절편이다.

**31.** c — 활성화 함수가 없으면 선형함수의 합성은 여전히 선형함수이므로, 은닉 유닛을 아무리 쌓아도 결국 단순선형회귀와 동일한 형태로 붕괴한다.

**32.** b — ReLU는 $z<0$이면 0을, $z\geq0$이면 $z$ 값을 그대로 출력하는 함수다.

**33.** b — Hidden unit은 입력의 특정 구간에서만 활성화되는 미니 선형 조각이며, 여러 개가 조합되면 piecewise-linear 함수를 만든다.

**34.** c — "과적합 위험이 낮다"는 것은 선형모델이 아니라 Deep network(은닉층 여러 개, 표현력이 훨씬 큰 모델)와 비교했을 때의 상대적 설명이다.

**35.** b — 보편적 근사 정리는 은닉층 1개짜리 shallow network라도 hidden unit 수만 충분하면 임의의 연속함수를 원하는 정확도로 근사할 수 있다는 정리다.

**36.** b — 다중 출력은 조건부 분기가 아니라, 같은 hidden unit 값들(원재료)을 출력마다 다른 가중치로 조합하는 행렬 곱셈으로 동시에 계산된다.

**37.** b — Network 1의 출력이 그대로 Network 2의 입력이 되며, 두 네트워크는 독립적인 파라미터 세트(θ vs θ', φ vs φ')를 가진다.

**38.** b — 이전 층의 함수가 비단조(오르락내리락)이면, 서로 다른 입력 구간들이 같은 출력값으로 "접혀서" 이후 층의 처리를 공유하게 되는 현상을 설명한다.

**39.** c — 파라미터 수가 비슷해도(20 vs 19), Deep network(16개 구역)가 Shallow network(7개 구역)보다 훨씬 많은 조각적 선형 구역을 만들어낸다.

**40.** b — Shallow network는 유닛들이 병렬로만 작동해 구역 수가 선형적으로 증가하지만, Deep network는 층마다 "접힘"이 반복되어 구역 수가 훨씬 빠르게(거듭제곱적으로) 증가한다.

**41.** b — c)가 비단조(텐트 모양)이기 때문에, b)에서 같은 값을 갖는 여러 등고선 영역들이 서로 접혀 여러 번 반사된 것처럼 훨씬 촘촘한 패턴이 된다.

**42.** b — $\boldsymbol\Omega_k$는 가중치 행렬(Weight matrix), $\boldsymbol\beta_k$는 편향 벡터(Bias vector)를 의미한다.

**43.** b — $\mathbf{h}_k=a[\boldsymbol\beta_{k-1}+\boldsymbol\Omega_{k-1}\mathbf{h}_{k-1}]$처럼 동일한 형태의 연산(가중치 행렬곱+편향 → 활성화 함수)이 층마다 반복 적용된다.

**44.** b — Deep network는 결국 (가중치 행렬 곱 → 편향 더하기 → 활성화 함수)라는 동일한 연산을 레이어 수만큼 반복하는 구조다.

**45.** b — 역전파는 출력 오차를 기준으로 계산 그래프를 거꾸로 따라가며 연쇄법칙으로 각 층의 미분값을 구하는 절차다.

**46.** b — 역전파는 연쇄법칙(합성함수의 변화율 = 바깥의 변화율 × 안쪽의 변화율)을 층마다 반복 적용하는 원리에 기반한다.

**47.** c — 역전파는 출력층에서 시작한다. 손실함수 정의로부터 출력에 대한 손실의 변화율을 직접 계산할 수 있기 때문이다.

**48.** b — 각 층이 바로 다음 층에서 이미 계산해둔 기울기를 재사용하기 때문에, 처음부터 다시 계산하지 않아도 되어 효율적이다.

**49.** b — 역전파는 모든 층의 기울기를 계산하는 것으로 끝나고, 그 기울기를 이용해 파라미터를 실제로 수정하는 것은 경사하강법이라는 별도의 단계에서 이루어진다.

**50.** c — 기본적으로는 모든 층의 가중치가 경사하강법 공식에 의해 자동으로, 동시에 조정된다. (전이학습에서 특정 층을 "얼리는" 것은 예외적인 경우)