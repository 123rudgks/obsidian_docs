# 확인문제 71제

> 통합 학습 노트(1장~22.3장) 전체 범위를 다룹니다. 각 문제 아래 접힌 콜아웃을 펼쳐 정답과 해설을 확인하세요.

---

## 1장. AI, ML, DL의 정의

**1.** 다음 중 AI, ML, DL의 관계에 대한 설명으로 옳은 것은?

- a. ML은 AI과 DL의 상위 개념이다
- b. AI는 반드시 ML을 사용해야 한다
- c. ML은 AI의 한 부분으로, 데이터로부터 학습을 수행한다
- d. 규칙 기반 시스템은 ML의 한 분야이다
- e. ML은 사람이 직접 코딩한 규칙을 그대로 사용하는 방식이다

> [!success]- 정답 및 해설 **정답: c**
> 
> ML은 AI의 한 분야로, 데이터로부터 규칙을 학습해 목표 달성을 돕는다. (a: ML은 AI의 하위 개념이다 / b: AI는 규칙·휴리스틱 기반만으로도 구현 가능하다 / d: 규칙 기반 시스템은 AI의 대표적인 예시다 / e: ML은 데이터에서 추론한다)

---

## 2장. 데이터와 학습의 이해

**2.** 다음 중 Feature와 Label에 대한 설명으로 옳지 않은 것은?

- a. Feature는 모델이 예측에 사용하는 입력 정보이다
- b. Label은 모델이 예측하려는 정답, 즉 학습의 목표이다
- c. Feature와 Label의 분포와 관계가 학습 결과를 결정한다
- d. Feature는 반드시 수치형 데이터만 될 수 있다
- e. Label은 분류 문제에서는 카테고리, 회귀 문제에서는 수치값이 될 수 있다

> [!success]- 정답 및 해설 **정답: d**
> 
> Feature는 수치형뿐 아니라 범주형, 텍스트, 이미지/음성 등도 가능하다.

---

## 4장. 복수 피쳐 기반 학습

**3.** 다음 중 $f(X)$, 즉 미지의 참 함수를 학습하는 이유로 적절하지 않은 것은?

- a. Feature($X$)와 label($Y$) 사이의 관계를 알기 위해
- b. 모든 데이터의 측정오차($\epsilon$)를 제거하기 위해
- c. 새로운 입력 $X$에 대해 목표값 $Y$를 예측하기 위해
- d. 여러 feature($X$)들 중 label($Y$)와 관계를 갖고 있는 feature를 알기 위해
- e. Feature($X$) 값의 변화에 따라서 label($Y$)이 어떻게 변화하는지 알기 위해

> [!success]- 정답 및 해설 **정답: b**
> 
> 학습을 잘 한다고 해서 측정오차($\epsilon$)를 줄이거나 제거할 수 없다.

---

## 5장. 지도학습의 개념

**4.** 다음 중 지도학습(supervised learning)에 대한 설명으로 옳은 것은?

- a. 입력 데이터만 있고 정답(라벨)이 없는 데이터로 학습한다
- b. 새로운 입력에 대해 정답을 잘 맞추는 함수를 학습한다
- c. 오버피팅은 훈련데이터에서의 오류를 최대한 줄이는 것으로 학습이 목표하는 방향이다
- d. 지도학습 모델의 출력은 반드시 범주(카테고리)이다
- e. 지도학습은 측정오차를 최소화하는 것이 목적이다

> [!success]- 정답 및 해설 **정답: b**
> 
> 지도학습은 입력(특성)과 정답(라벨)이 쌍으로 있는 데이터를 학습하여 새로운 입력의 정답을 맞추는 규칙을 찾는다. (a: 입력만 있고 정답이 없는 경우는 비지도 학습이다 / c: 일반화 성능을 높이는 것이 궁극적인 목적이기에 오버피팅은 피해야 한다 / d: 지도학습 모델 출력은 수치(회귀문제)와 범주(분류문제) 모두 가능하다 / e: 지도학습에서는 예측값과 정답의 차이(오차)를 줄이는 것이 핵심이다)

---

## 6장. 회귀(Regression)

**5.** 다음 중 회귀(Regression) 문제의 특징으로 옳은 것은?

- a. 스팸/정상 분류와 같은 범주 예측 문제를 다룬다
- b. 예측값은 연속적인 숫자(예: 가격, 점수, 온도)로 나온다
- c. 대표적인 성능 지표는 정확도(Accuracy)이다
- d. 예측 결과는 이산적인 값으로 한정된다
- e. 훈련 데이터와 테스트 데이터의 분포가 다르면 반드시 발생한다

> [!success]- 정답 및 해설 **정답: b**
> 
> 회귀 문제는 연속적인 수치를 예측하는 문제다. 예를 들어 집값, 매출액, 온도 등이 있다. (a: 범주를 예측하는 것은 분류(Classification) 문제다 / c: 회귀에서는 MSE, RMSE, $R^2$ 등의 지표를 사용한다 / d: 이산 값은 분류 문제의 특징이다 / e: 훈련/테스트 분포 차이는 데이터 쉬프트 개념이지 회귀의 특징은 아니다)

---

## 7장. 분류(Classification)

**6.** 다음 중 혼동행렬에 대한 설명으로 옳지 않은 것은?

- a. TP(True Positive): 실제 양성을 양성으로 예측한 경우
- b. TN(True Negative): 실제 음성을 음성으로 예측한 경우
- c. FP(False Positive): 실제 음성인데 양성으로 잘못 예측한 경우
- d. FN(False Negative): 실제 양성인데 음성으로 잘못 예측한 경우
- e. 혼동행렬은 회귀 문제의 성능 평가에서 사용된다

> [!success]- 정답 및 해설 **정답: e**
> 
> 혼동행렬은 분류 문제의 성능 평가 지표이다. 회귀 문제에서는 혼동행렬 대신 MSE, RMSE, $R^2$ 등의 지표를 사용한다.

---

**7.** 암 진단 모델에서 실제 암 환자를 '정상'이라고 놓치면(FN) 치명적이다. 이 상황에서 더 중요하게 챙겨야 할 지표는?

- a. 정밀도(Precision)
- b. 재현율(Recall)

> [!success]- 정답 및 해설 **정답: b**
> 
> 실제 암 환자(진짜 Positive)를 놓치는 것(FN)을 최소화해야 하므로 재현율이 중요하다. 재현율의 분모(TP+FN)는 "실제 정답 전체"를 뜻한다.

**8.** 법원에서 무죄인 사람을 유죄로 잘못 판결하는 것(FP)을 최대한 피하고 싶다. 이때 중요한 지표는?

- a. 정밀도(Precision)
- b. 재현율(Recall)

> [!success]- 정답 및 해설 **정답: a**
> 
> 정밀도의 분모(TP+FP)에 FP가 포함된다. FP(무죄인데 유죄로 오판)를 줄이는 게 핵심이므로 정밀도가 중요하다. (단, "무죄"를 Positive로 정의하면 같은 상황이 재현율/Specificity 관점으로도 설명된다 — Positive를 무엇으로 잡느냐에 따라 이름만 달라질 뿐 본질은 같다.)

**9.** 유튜브가 "이 영상은 스팸/악성 콘텐츠입니다"라고 판단해서 삭제하는 모델을 만든다고 하자. 정상 영상을 스팸으로 잘못 지우면(FP) 크리에이터가 크게 항의할 것이다. 어떤 지표를 우선해야 할까?

- a. 정밀도(Precision)
- b. 재현율(Recall)

> [!success]- 정답 및 해설 **정답: a**
> 
> FP(정상인데 스팸으로 오판)의 피해가 크므로, "스팸이라고 판단한 것 중 진짜 스팸 비율"인 정밀도를 우선해야 한다.

**10.** 공항 보안 검색대에서 위험물을 놓치는 것(FN)은 절대 안 되지만, 안전한 물건을 다시 검사하는 것(FP)은 번거로울 뿐 큰 문제는 아니다. 어떤 지표를 우선해야 할까?

- a. 정밀도(Precision)
- b. 재현율(Recall)

> [!success]- 정답 및 해설 **정답: b**
> 
> FN(위험물인데 안전하다고 놓침)을 최소화해야 하는 상황이므로 재현율이 중요하다. FP는 상대적으로 감수 가능하다.

**11.** TP=40, FP=10, FN=20, TN=30일 때 정밀도(Precision)는?

- a. 40/50 = 0.8
- b. 40/60 = 0.67
- c. 40/70 = 0.57
- d. 70/100 = 0.7

> [!success]- 정답 및 해설 **정답: a**
> 
> Precision = TP/(TP+FP) = 40/(40+10) = 40/50 = 0.8

**12.** 같은 데이터(TP=40, FP=10, FN=20, TN=30)에서 재현율(Recall)은?

- a. 40/50 = 0.8
- b. 40/60 = 0.67
- c. 40/70 = 0.57
- d. 20/60 = 0.33

> [!success]- 정답 및 해설 **정답: b**
> 
> Recall = TP/(TP+FN) = 40/(40+20) = 40/60 ≈ 0.67

**13.** 모델 A: 정밀도 0.95, 재현율 0.30 / 모델 B: 정밀도 0.40, 재현율 0.90. 이 모델이 사기(fraud) 거래를 잡아내는 용도라면 어떤 모델이 더 나은 선택에 가까울까?

- a. 모델 A (예측을 매우 확신할 때만 사기라고 함)
- b. 모델 B (의심되면 폭넓게 사기라고 잡아냄)

> [!success]- 정답 및 해설 **정답: b**
> 
> 사기 거래를 놓치면(FN) 금전적 손실이 크기 때문에 대체로 재현율이 높은 모델 B가 선호된다. 다만 오탐이 너무 많으면 고객 불편이 커지므로 실제로는 F1 등 균형 지표도 함께 고려한다.

**14.** "모델이 Positive라고 예측한 것들 중 실제로 맞았던 비율"을 나타내는 지표는?

- a. 정밀도(Precision)
- b. 재현율(Recall)

> [!success]- 정답 및 해설 **정답: a**
> 
> "예측이 Positive였던 것" 중 맞은 비율 = 정밀도. 분모가 TP+FP(예측 기준)라는 게 핵심이다.

**15.** 채용 서류 필터링 AI가 있다. "실제로 뽑을 만한 지원자"를 실수로 걸러내면(FN) 좋은 인재를 놓치는 큰 손해가 발생한다. 반면 조금 부족한 지원자가 통과되는 건(FP) 다음 면접 단계에서 걸러지므로 큰 문제가 아니다. 우선해야 할 지표는?

- a. 정밀도(Precision)
- b. 재현율(Recall)

> [!success]- 정답 및 해설 **정답: b**
> 
> FN(진짜 인재인데 걸러냄)을 줄이는 게 핵심이므로 재현율이 중요하다. 이후 단계에서 FP는 걸러낼 수 있으니 감수 가능하다.

**16.** TP=15, FP=5, FN=5, TN=75일 때, 정밀도와 재현율 중 어느 쪽이 더 높을까?

- a. 정밀도가 더 높다
- b. 재현율이 더 높다
- c. 둘이 같다

> [!success]- 정답 및 해설 **정답: c**
> 
> Precision = 15/20 = 0.75, Recall = 15/20 = 0.75로 같다. FP와 FN 값이 같으면 정밀도와 재현율도 같아진다.

---

## 8장. 학습의 목적

**17.** 다음 중 언더피팅과 오버피팅의 특징을 올바르게 설명한 것은?

- a. 언더피팅은 훈련 오류와 테스트 오류 모두 크게 나타난다
- b. 언더피팅은 모델이 지나치게 복잡해 데이터 잡음까지 학습하는 현상이다
- c. 오버피팅은 모델이 단순해 중요한 패턴을 놓치는 현상이다
- d. 오버피팅은 훈련 오류가 크고 테스트 오류가 작게 나타난다
- e. 두 경우 모두 모델 복잡도를 높이면 해결된다

> [!success]- 정답 및 해설 **정답: a**
> 
> 언더피팅은 모델이 너무 단순하여 데이터 패턴을 제대로 학습하지 못해 훈련 오류와 테스트 오류 모두 크게 나타난다. (b: 오버피팅에 대한 설명이다 / c: 언더피팅이 아니라 오버피팅의 반대 현상이다 / d: 오버피팅에서는 훈련 오류↓, 테스트 오류↑이다 / e: 언더피팅은 복잡도를 높이면 개선 가능하지만, 오버피팅은 오히려 더 심해진다)

---

## 9장. 테스트 성능 평가

**18.** 훈련 오류와 테스트 오류에 대한 설명으로 옳은 것은?

- a. 훈련 오류는 새로운 데이터에 대한 예측 정확도를 측정한다
- b. 테스트 오류는 학습에 사용된 데이터에 대해 계산한 평균 오차이다
- c. 궁극적으로 테스트 오류를 최소화하는 것이 학습의 목적이다
- d. 일반적으로 훈련 오류는 테스트 오류보다 크게 나타난다
- e. 훈련 오류는 항상 0에 수렴해야 좋은 모델이다

> [!success]- 정답 및 해설 **정답: c**
> 
> 학습의 목적은 일반화 성능을 높이는 것이며, 이는 테스트 오류를 최소화하는 것이다. (a: 새로운 데이터는 테스트 오류가 담당한다 / b: 이는 훈련 오류에 대한 설명이다 / d: 일반적으로 훈련 오류 < 테스트 오류이다 / e: 훈련 오류가 0에 가까우면 오버피팅일 가능성이 높다)

---

## 11장. K-겹 교차검증 (K-fold Cross-Validation)

**19.** K-겹 교차검증(K-fold CV)에 대한 설명으로 옳지 않은 것은?

- a. 데이터를 K개 그룹(폴드)으로 나누어, 각 그룹을 한 번씩 검증셋으로 사용한다
- b. K번 반복한 후 각 폴드별 평균 오차를 계산해 일반화 성능을 추정한다
- c. K=n일 경우 Leave-One-Out 교차검증(LOOCV)이다
- d. 교차검증은 데이터 활용성을 높이면서 테스트 오류에 대한 추정이 가능하다
- e. 교차검증시, 각 폴드에 속한 데이터가 겹쳐도 무방하다

> [!success]- 정답 및 해설 **정답: e**
> 
> 교차검증에서는 각 폴드별 데이터가 겹치지 않도록 명확히 분리해야 한다.

---

## 12장. 비지도 학습

**20.** 다음 중 비지도 학습의 예로 해당하는 것은?

- a. 스팸/이메일 분류
- b. 집값 변화 예측
- c. (라벨이 없는 데이터에서) 구매 성향별 고객 묶음
- d. 주간 날씨 예측

> [!success]- 정답 및 해설 **정답: c**
> 
> 정답이 없는 묶음 문제로 전형적인 클러스터링 문제

---

## 13장. 클러스터링(Clustering)

**21.** 다음은 클러스터링 기법에 대한 설명이다. 각 설명이 K-means의 특징인지, 계층적 군집의 특징인지 분류하시오. (단, 두 방법 모두에 해당하지 않을 수 있다.)

- A. 클러스터 수 K를 미리 지정해야 한다.
- B. 덴드로그램(Dendrogram)을 통해 전체 구조를 시각적으로 제공한다.
- C. 데이터 간 병합(상향식) 과정을 통해 클러스터를 형성한다.
- D. 클러스터 내부 변동(Within-cluster variation)의 합이 최소가 되도록 분할한다.
- E. 매우 큰 데이터셋에도 계산 복잡도가 크게 늘지 않아 효율적이다.

> [!success]- 정답 및 해설 **정답: K-means: A, D, E / 계층적 군집: B, C**
> 
> K-means는 데이터를 K개의 그룹으로 나누는 대표적 기법으로, 군집 내 분산을 최소화하도록 반복 계산한다. 빠르고 대규모 데이터에 효율적이지만 클러스터 수를 미리 지정해야 하고 복잡한 모양의 데이터에는 한계가 있다. 계층적 군집은 데이터를 병합·분할하여 트리(덴드로그램) 구조를 만드는 방식으로, 클러스터 수를 미리 정하지 않아도 되고 데이터 구조를 시각적으로 파악하기 쉽다. 그러나 데이터가 많을수록 계산 비용이 커져 대규모 데이터에는 비효율적이다.

---

## 17장. 선형 회귀

**22.** 손실함수(Loss Function)의 역할로 가장 알맞은 것은?

- a. 입력 데이터를 무작위로 섞는다
- b. 모델의 예측이 얼마나 잘못되었는지 측정한다
- c. 파라미터를 업데이트하는 규칙을 제공한다
- d. 학습률(learning rate)을 설정한다

> [!success]- 정답 및 해설 **정답: b**
> 
> 손실함수는 모델 예측값과 실제값 사이의 차이를 수치화한 지표다. 학습은 이 값을 최소화하는 방향으로 진행된다.

**23.** 가장 널리 쓰이는 손실함수인 MSE(평균제곱오차)의 수식으로 옳은 것은?

- a. $\sum(y-\hat y)$
- b. $\frac{1}{N}\sum|y-\hat y|$
- c. $\frac{1}{N}\sum(y-\hat y)^2$
- d. $\sum(y-\hat y)^2$

> [!success]- 정답 및 해설 **정답: c**
> 
> MSE $=\frac{1}{N}\sum(y-\hat y)^2$. 가장 널리 쓰이는 손실함수로, 미분 가능하고(convex) 이상치에 민감하다는 특징이 있다.

**24.** 단순선형회귀가 경사하강법 없이 공식으로 한 번에 구해지는 이유로 가장 알맞은 것은?

- a. 데이터가 적기 때문에
- b. 손실함수(RSS)가 w, b에 대한 2차식이라 미분=0인 지점이 유일한 최솟값이기 때문
- c. 파라미터가 2개뿐이기 때문
- d. 선형회귀는 원래 반복 계산이 불가능하기 때문

> [!success]- 정답 및 해설 **정답: b**
> 
> RSS가 w,b에 대한 2차식(볼록함수)이라 미분=0인 지점이 유일한 최솟값이고, 그 지점을 공식으로 바로 계산할 수 있다.

**25.** 회귀 결과 해석에서 p-value가 의미하는 것은?

- a. 예측값과 실제값의 차이
- b. 계수가 실제로는 0일 확률(=x,y 관계없음)
- c. 데이터의 표준편차
- d. 모델의 전체 정확도

> [!success]- 정답 및 해설 **정답: b**
> 
> p-value는 계수가 실제로는 0(=관계없음)일 확률이며, 보통 0.05 이하면 통계적으로 유의미하다고 판단한다.

**26.** 회귀 결과를 해석할 때 가장 주의해야 할 점은?

- a. Coefficient 크기만 보고 영향력의 크고 작음을 판단해도 된다
- b. Std.Error는 무시해도 된다
- c. Coefficient 크기만으로 판단하지 말고 Std.Error, t-statistic, p-value를 함께 봐야 한다
- d. t-statistic이 0이면 유의미하다는 뜻이다

> [!success]- 정답 및 해설 **정답: c**
> 
> Coefficient 크기만으로는 판단할 수 없고, Std.Error·t-statistic·p-value를 함께 봐야 통계적 유의성을 제대로 판단할 수 있다.

---

## 18장. 다중 선형 회귀

**27.** 회귀 가중치와 상관계수의 차이에 대한 설명으로 옳은 것은?

- a. 둘은 완전히 같은 개념이다
- b. 상관계수는 나머지 변수를 고정한 채 한 변수의 순수 영향력을 보여준다
- c. 회귀 가중치는 나머지 변수를 고정한 채 그 변수만 바꿨을 때의 영향력이고, 상관계수는 단순 2변수 관계다
- d. 회귀 가중치는 항상 상관계수보다 크다

> [!success]- 정답 및 해설 **정답: c**
> 
> 상관계수는 두 변수의 단순한 관계, 회귀 가중치는 다른 변수를 고정한 채 해당 변수만의 순수한 영향력을 나타낸다.

**28.** 다중선형회귀의 최소제곱법 공식으로 옳은 것은?

- a. $\hat{\boldsymbol\beta}=\mathbf X^\top\mathbf y$
- b. $\hat{\boldsymbol\beta}=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$
- c. $\hat{\boldsymbol\beta}=\mathbf X(\mathbf X^\top\mathbf X)^{-1}$
- d. $\hat{\boldsymbol\beta}=(\mathbf X\mathbf X^\top)^{-1}\mathbf y$

> [!success]- 정답 및 해설 **정답: b**
> 
> $\hat{\boldsymbol\beta}=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$가 최소제곱법의 닫힌 형태 해다.

**29.** 다중선형회귀도 닫힌 형태의 공식이 있는데, 굳이 경사하강법이 필요한 이유는?

- a. 공식이 수학적으로 틀렸기 때문에
- b. 변수(차원)가 많아지면 행렬 연산량이 폭발적으로 증가해 물리적으로 계산이 불가능해지기 때문
- c. 경사하강법이 항상 더 정확하기 때문에
- d. 다중선형회귀는 원래 공식이 존재하지 않기 때문에

> [!success]- 정답 및 해설 **정답: b**
> 
> 공식 자체는 존재하지만, 변수(차원)가 많아지면 행렬 연산량이 폭발적으로 증가해 실제로 계산하기 물리적으로 불가능해진다.

**30.** "아이스크림 판매량과 상어 물림 사건 수가 함께 증가한다"는 예시가 보여주는 것은?

- a. 아이스크림이 상어를 유인한다
- b. 상관관계가 곧 인과관계다
- c. 상관관계 ≠ 인과관계 (공통 원인인 "여름" 때문일 뿐)
- d. 두 변수는 아무 관계가 없다

> [!success]- 정답 및 해설 **정답: c**
> 
> 두 변수 모두 "여름"이라는 공통 원인 때문에 함께 증가할 뿐, 서로 인과관계가 있는 것은 아니다. 상관관계와 인과관계는 다르다.

**31.** 다중선형회귀 해석 시 변수들이 서로 독립적이지 않으면(상관되어 있으면) 어떤 문제가 생기는가?

- a. 문제없이 정확한 계수가 나온다
- b. 계수 추정이 불안정해지고 해석이 혼동될 수 있다
- c. 손실함수가 convex가 아니게 된다
- d. 학습 속도가 빨라진다

> [!success]- 정답 및 해설 **정답: b**
> 
> 변수들이 서로 상관되어 있으면(다중공선성) 계수 추정이 불안정해지고 해석이 혼동될 수 있다.

---

## 19장. 분류와 로지스틱 회귀

**32.** 분류 문제에 선형회귀를 그대로 쓰면 안 되는 이유는?

- a. 선형회귀는 계산이 너무 느려서
- b. 선형회귀의 출력값이 $-\infty\sim+\infty$라 확률(0~1) 범위를 벗어날 수 있어서
- c. 분류는 손실함수를 쓸 수 없어서
- d. 선형회귀는 파라미터가 없어서

> [!success]- 정답 및 해설 **정답: b**
> 
> 선형회귀의 출력은 $-\infty\sim+\infty$ 범위라 확률(0~1)을 벗어날 수 있어 분류 문제에 부적절하다.

**33.** 시그모이드 함수의 출력 범위는?

- a. $-1\sim1$
- b. $-\infty\sim+\infty$
- c. $0\sim1$
- d. $0\sim\infty$

> [!success]- 정답 및 해설 **정답: c**
> 
> 시그모이드 함수는 어떤 입력이 와도 항상 0~1 사이의 값만 출력하도록 강제한다.

**34.** 오즈(Odds)의 정의로 옳은 것은?

- a. $p+(1-p)$
- b. $p\times(1-p)$
- c. $p/(1-p)$ (성공확률/실패확률)
- d. $\log(p)$

> [!success]- 정답 및 해설 **정답: c**
> 
> 오즈(Odds) = 성공확률/실패확률 = $p/(1-p)$.

**35.** 로지스틱 회귀의 파라미터(β)를 추정할 때 사용하는 지표는?

- a. MSE(평균제곱오차)
- b. MAE(평균절대오차)
- c. 우도(Likelihood), MLE
- d. 상관계수

> [!success]- 정답 및 해설 **정답: c**
> 
> 로지스틱 회귀는 MSE 대신 우도(Likelihood)를 지표로 사용하며, 우도를 최대화하는 MLE로 파라미터를 추정한다.

**36.** 우도(Likelihood) 대신 로그를 취한 log-likelihood를 사용하는 이유는?

- a. 값을 더 크게 만들기 위해
- b. 곱셈(Π)을 덧셈으로 바꿔 미분을 쉽게 하기 위해 (log 변환해도 최댓값 위치는 동일)
- c. 확률을 0~1 범위로 강제하기 위해
- d. 계산 속도와는 아무 관련 없다

> [!success]- 정답 및 해설 **정답: b**
> 
> 우도식은 곱셈(Π)으로 구성되어 미분이 어려운데, log를 취하면 곱을 합으로 바꿔 미분이 쉬워진다. log 변환해도 최댓값의 위치는 동일하다.

**37.** 로지스틱 회귀는 선형회귀처럼 닫힌 형태(공식)로 파라미터를 바로 구할 수 있는가?

- a. 그렇다, 선형회귀와 동일한 공식을 쓴다
- b. 아니다, 우도식의 특성상 경사하강법 같은 반복적 최적화가 필요하다
- c. 로지스틱 회귀는 파라미터가 없다
- d. 로지스틱 회귀는 학습이 필요 없다

> [!success]- 정답 및 해설 **정답: b**
> 
> 로지스틱 회귀는 우도식의 특성상 선형회귀처럼 닫힌 형태 공식으로 바로 구할 수 없어, 경사하강법 같은 반복적 최적화가 필요하다.

---

## 20장. 회귀 → 신경망으로의 연결

**38.** 선형회귀와 로지스틱 회귀의 공통적인 근본 한계는?

- a. 데이터가 많아야 한다
- b. "입력들의 가중합 + 편향" 하나로만 결과를 표현해서, 직선(또는 평면) 하나만 표현 가능하다
- c. 계산 속도가 느리다
- d. 파라미터가 너무 많다

> [!success]- 정답 및 해설 **정답: b**
> 
> 둘 다 "가중합 + 편향" 하나로 결과를 표현하는 구조라, 직선(또는 평면) 하나만 표현 가능하다는 근본적 한계를 공유한다.

**39.** Shallow network의 구조 정의로 옳은 것은?

- a. 은닉층이 2개 이상인 신경망
- b. 입력층 - 은닉층 1개 - 출력층으로 구성된 신경망
- c. 은닉층이 없는 신경망
- d. 출력층이 여러 개인 신경망

> [!success]- 정답 및 해설 **정답: b**
> 
> Shallow network는 입력층 - 은닉층 1개 - 출력층으로 구성된 신경망이다.

**40.** "표현력(expressive power)"이 의미하는 것은?

- a. 모델의 학습 속도
- b. 모델이 표현/근사할 수 있는 함수의 복잡도·다양성
- c. 모델의 파라미터 개수 그 자체
- d. 데이터의 개수

> [!success]- 정답 및 해설 **정답: b**
> 
> 표현력은 모델이 얼마나 다양하고 복잡한 형태의 함수(관계)를 표현/근사할 수 있는지를 뜻한다.

**41.** 단순선형회귀(파라미터 2개)와 은닉유닛 3개짜리 Shallow network(파라미터 10개)를 비교했을 때, 옳은 설명은?

- a. 둘 다 표현 가능한 형태는 직선 1개로 동일하다
- b. 파라미터 수가 다르므로 비교 자체가 불가능하다
- c. Shallow network는 비선형 곡선까지 표현 가능해 표현력이 더 높다
- d. 단순선형회귀가 항상 더 우수하다

> [!success]- 정답 및 해설 **정답: c**
> 
> Shallow network는 은닉유닛과 활성화 함수 덕분에 비선형 곡선까지 표현 가능해, 파라미터가 더 많음에도 단순선형회귀보다 표현력이 높다.

---

## 21장. 경사하강법과 발전 과정

**42.** 경사하강법에서 (초기화 이후) 매 반복(iteration)의 첫 단계에 해당하는 것은?

- a. 파라미터를 무작위로 초기화한다
- b. 현재 파라미터에서 손실함수의 기울기(미분값)를 계산한다
- c. 데이터를 섞어 미니배치를 생성한다
- d. 학습을 종료한다

> [!success]- 정답 및 해설 **정답: b**
> 
> (파라미터 초기화는 최초 1회의 준비 단계이고) 매 반복의 첫 단계는 현재 파라미터에서 손실함수의 기울기를 계산하는 것이다.

**43.** 학습률(η)에 대한 설명으로 옳은 것은?

- a. 클수록 항상 좋다
- b. 너무 크면 최솟값을 지나쳐 발산할 수 있고, 너무 작으면 수렴이 느리다
- c. 학습률은 파라미터 개수를 의미한다
- d. 학습률은 손실함수의 종류를 결정한다

> [!success]- 정답 및 해설 **정답: b**
> 
> 학습률이 너무 크면 최솟값을 지나쳐 발산할 수 있고, 너무 작으면 수렴이 느려진다.

**44.** Convex(볼록) 문제의 특징으로 옳은 것은?

- a. 2차 미분이 항상 0이다
- b. 2차 미분이 모든 구간에서 음수이다
- c. 2차 미분이 모든 구간에서 양수(또는 0)이며, 국소 최솟값이 곧 전역 최솟값이다
- d. 미분값이 존재하지 않는다

> [!success]- 정답 및 해설 **정답: c**
> 
> Convex 문제는 2차 미분이 모든 구간에서 양수(또는 0)인 형태로, 이 경우 국소 최솟값이 곧 전역 최솟값과 같다.

**45.** 확률적 경사하강법(SGD)의 장점으로 가장 알맞은 것은?

- a. 항상 전역 최적해(global optimum)에 도달한다
- b. 일부 데이터만으로 업데이트해 스텝당 연산 비용이 작다
- c. 항상 같은 결과로 수렴한다
- d. 계산 비용이 커서 정밀하다

> [!success]- 정답 및 해설 **정답: b**
> 
> SGD는 전체 데이터가 아닌 일부(또는 1개) 샘플로 기울기를 계산해 스텝당 연산 비용이 작다.

**46.** SGD가 경사하강법(full-batch GD)보다 항상 수렴 속도가 빠른가?

- a. 그렇다, 항상 빠르다
- b. 아니다, convex 문제에서는 오히려 GD의 수렴이 더 빠를 수 있다 (SGD가 빠른 건 스텝당 계산 비용일 뿐)
- c. SGD와 GD는 속도 차이가 전혀 없다
- d. SGD는 애초에 수렴하지 않는다

> [!success]- 정답 및 해설 **정답: b**
> 
> SGD가 빠른 것은 스텝당 계산 비용이지, 수렴 속도 자체가 아니다. Convex 문제에서는 오히려 GD가 정확한 방향으로 이동해 더 빠르게 수렴할 수 있다.

**47.** 미니배치 경사하강법과 에포크(Epoch)에 대한 설명으로 옳은 것은?

- a. 에포크는 파라미터를 한 번 업데이트하는 것을 의미한다
- b. 미니배치는 데이터 1개씩만 사용한다
- c. 미니배치는 배치 단위 평균 기울기로 업데이트하며 병렬 연산이 가능하고, 에포크는 전체 데이터셋을 한 번 다 사용한 상태를 의미한다
- d. 에포크가 많을수록 미니배치 크기가 커진다

> [!success]- 정답 및 해설 **정답: c**
> 
> 미니배치는 배치 단위 평균 기울기로 업데이트하며 병렬 연산이 가능하고, 에포크는 전체 데이터셋을 한 번 다 학습에 사용한 상태를 의미한다.

---

## 22.1장. Shallow 네트워크

**48.** "모수적 함수(parametric function)"의 의미로 옳은 것은?

- a. 함수 형태 자체가 데이터에 따라 계속 바뀌는 함수
- b. 함수의 형태는 고정되고, 파라미터 값에 따라 함수가 완전히 결정되는 함수
- c. 파라미터가 없는 함수
- d. 오직 선형회귀에만 적용되는 개념

> [!success]- 정답 및 해설 **정답: b**
> 
> 모수적 함수는 함수의 형태는 고정되고, 파라미터 값에 따라 함수가 완전히 결정되는 함수다.

**49.** XOR 문제가 보여주는 것은?

- a. 신경망은 XOR을 풀 수 없다
- b. 단순 선형모델(직선 하나)로는 XOR을 분류할 수 없지만, 은닉층 하나만 추가해도 풀 수 있다
- c. XOR은 로지스틱 회귀로도 절대 풀 수 없다
- d. XOR은 다중선형회귀로도 절대 풀 수 없다

> [!success]- 정답 및 해설 **정답: b**
> 
> XOR은 단순 선형모델(직선 하나)로는 분류 불가능하지만, 은닉층 하나만 추가해도(비선형성 부여) 풀 수 있게 된다.

**50.** 다중선형회귀와 신경망의 진짜 차이점은?

- a. 다중선형회귀는 여러 변수를 다룰 수 없다
- b. 다중선형회귀는 상호작용(교호작용) 항을 사람이 직접 설계해야 하지만, 신경망은 이를 데이터로부터 자동으로 학습한다
- c. 신경망은 변수 조합을 전혀 고려하지 못한다
- d. 다중선형회귀가 신경망보다 항상 표현력이 높다

> [!success]- 정답 및 해설 **정답: b**
> 
> 다중선형회귀는 상호작용(교호작용) 항을 사람이 직접 설계(feature engineering)해야 하지만, 신경망은 이를 데이터로부터 자동으로 학습한다.

**51.** Shallow network 수식 $y=\phi_0+\phi_1a[\theta_{10}+\theta_{11}x]+\cdots$에서 θ와 φ에 대한 설명으로 옳은 것은?

- a. θ는 출력층 가중치, φ는 입력층 가중치다
- b. θ는 "입력→은닉유닛" 사이의 가중치·절편, φ는 "은닉유닛→출력" 사이의 가중치·절편이다
- c. θ와 φ는 완전히 같은 값이다
- d. φ만 파라미터이고 θ는 활성화 함수의 일부다

> [!success]- 정답 및 해설 **정답: b**
> 
> θ는 "입력→은닉유닛" 사이(1층), φ는 "은닉유닛→최종출력" 사이(2층)의 가중치·절편이다.

**52.** 활성화 함수가 없다면 은닉 유닛을 아무리 많이 쌓아도 어떻게 되는가?

- a. 표현력이 무한히 커진다
- b. 계산이 불가능해진다
- c. 선형함수의 합성은 여전히 선형함수라서, 결국 단순선형회귀와 동일해진다
- d. 항상 XOR을 풀 수 있게 된다

> [!success]- 정답 및 해설 **정답: c**
> 
> 활성화 함수가 없으면 선형함수의 합성은 여전히 선형함수이므로, 은닉 유닛을 아무리 쌓아도 결국 단순선형회귀와 동일한 형태로 붕괴한다.

**53.** ReLU 활성화 함수의 정의로 옳은 것은?

- a. 항상 입력값을 그대로 출력한다
- b. $z<0$이면 0, $z\geq0$이면 $z$를 그대로 출력한다
- c. 출력을 항상 0~1 사이로 강제한다
- d. 입력의 부호를 반전시킨다

> [!success]- 정답 및 해설 **정답: b**
> 
> ReLU는 $z<0$이면 0을, $z\geq0$이면 $z$ 값을 그대로 출력하는 함수다.

**54.** Hidden unit(은닉 유닛)의 역할로 가장 알맞은 것은?

- a. 손실함수의 종류를 결정한다
- b. 입력 x의 특정 구간에서만 켜지는 미니 선형 조각으로, 여러 개가 조합되면 piecewise-linear 함수를 만든다
- c. 학습률을 자동으로 조절한다
- d. 항상 출력값을 그대로 통과시킨다

> [!success]- 정답 및 해설 **정답: b**
> 
> Hidden unit은 입력의 특정 구간에서만 활성화되는 미니 선형 조각이며, 여러 개가 조합되면 piecewise-linear 함수를 만든다.

**55.** "은닉 유닛을 늘리면 표현력이 커지지만 과적합 위험은 상대적으로 낮다"고 할 때, 이 비교의 기준은?

- a. 단순선형모델과의 비교
- b. 로지스틱 회귀와의 비교
- c. Deep network(은닉층 여러 개)와의 비교
- d. 아무런 비교 대상이 없다

> [!success]- 정답 및 해설 **정답: c**
> 
> "과적합 위험이 낮다"는 것은 선형모델이 아니라 Deep network(은닉층 여러 개, 표현력이 훨씬 큰 모델)와 비교했을 때의 상대적 설명이다.

**56.** 보편적 근사 정리(Universal Approximation)가 말하는 것은?

- a. 신경망은 이미지만 근사할 수 있다
- b. 은닉층 1개짜리 shallow network도 hidden unit 수만 충분하면 임의의 연속함수를 원하는 정확도로 근사할 수 있다
- c. Deep network만 임의의 함수를 근사할 수 있다
- d. Hidden unit 개수와 근사 정확도는 무관하다

> [!success]- 정답 및 해설 **정답: b**
> 
> 보편적 근사 정리는 은닉층 1개짜리 shallow network라도 hidden unit 수만 충분하면 임의의 연속함수를 원하는 정확도로 근사할 수 있다는 정리다.

**57.** 다중 출력(예: $y_1, y_2$)을 만드는 신경망의 계산 방식에 대한 설명으로 옳은 것은?

- a. 조건에 따라 $y_1$ 또는 $y_2$ 중 하나만 계산되는 분기(if-else) 처리다
- b. 같은 hidden unit 값들을 원재료로, 출력마다 다른 가중치를 곱해 동시에 계산하는 행렬 곱셈이다
- c. 각 출력마다 완전히 다른 hidden unit을 사용한다
- d. 다중 출력은 신경망에서 구현이 불가능하다

> [!success]- 정답 및 해설 **정답: b**
> 
> 다중 출력은 조건부 분기가 아니라, 같은 hidden unit 값들(원재료)을 출력마다 다른 가중치로 조합하는 행렬 곱셈으로 동시에 계산된다.

---

## 22.2장. Deep Network

**58.** Deep network를 "2개의 네트워크를 합성"하는 방식으로 만들 때의 핵심은?

- a. 두 네트워크가 서로 같은 파라미터를 공유한다
- b. Network 1의 출력이 그대로 Network 2의 입력이 되며, 두 네트워크는 독립적인 파라미터를 가진다
- c. 두 네트워크는 서로 아무 관계가 없다
- d. Network 2가 먼저 계산되고 그 다음 Network 1이 계산된다

> [!success]- 정답 및 해설 **정답: b**
> 
> Network 1의 출력이 그대로 Network 2의 입력이 되며, 두 네트워크는 독립적인 파라미터 세트(θ vs θ', φ vs φ')를 가진다.

**59.** "종이접기(folding)" 비유가 설명하는 것은?

- a. 신경망의 파라미터 개수가 종이처럼 얇다는 것
- b. 이전 층의 함수가 비단조(non-monotonic)이면, 서로 다른 입력 구간들이 같은 값으로 "접혀서" 다음 층의 처리를 공유하게 되는 현상
- c. 신경망 학습이 종이 위에서 이루어진다는 것
- d. 활성화 함수가 종이를 접는 것처럼 작동한다는 것

> [!success]- 정답 및 해설 **정답: b**
> 
> 이전 층의 함수가 비단조(오르락내리락)이면, 서로 다른 입력 구간들이 같은 출력값으로 "접혀서" 이후 층의 처리를 공유하게 되는 현상을 설명한다.

**60.** 파라미터 수가 비슷한 Shallow network(19개)와 Deep network(20개)를 비교했을 때, 노트에서 확인한 결과는?

- a. 둘의 조각적 선형 구역 수가 완전히 같다
- b. Shallow network가 더 많은 구역을 표현한다
- c. Deep network(16개 구역)가 Shallow network(7개 구역)보다 훨씬 많은 구역을 표현한다
- d. 파라미터 수가 비슷하면 비교 자체가 무의미하다

> [!success]- 정답 및 해설 **정답: c**
> 
> 파라미터 수가 비슷해도(20 vs 19), Deep network(16개 구역)가 Shallow network(7개 구역)보다 훨씬 많은 조각적 선형 구역을 만들어낸다.

**61.** Deep network의 표현력이 Shallow network보다 효율적으로 커지는 이유는?

- a. Deep network는 활성화 함수를 사용하지 않기 때문에
- b. Shallow network는 유닛이 병렬로만 작동해 구역 수가 선형적으로 증가하지만, Deep network는 "접힘"이 반복되어 구역 수가 훨씬 빠르게(거듭제곱적으로) 증가하기 때문
- c. Deep network는 파라미터가 항상 더 많기 때문에
- d. Shallow network는 애초에 비선형을 표현할 수 없기 때문에

> [!success]- 정답 및 해설 **정답: b**
> 
> Shallow network는 유닛들이 병렬로만 작동해 구역 수가 선형적으로 증가하지만, Deep network는 층마다 "접힘"이 반복되어 구역 수가 훨씬 빠르게(거듭제곱적으로) 증가한다.

**62.** 2D 입력으로 folding 예시를 확장했을 때(등고선 그림), d) 그래프가 b) 그래프보다 훨씬 복잡한 패턴을 보이는 이유는?

- a. 입력 차원이 다르기 때문에
- b. c)가 비단조(텐트 모양)라서, b)의 등고선들이 여러 번 접혀 반사되기 때문
- c. d) 그래프는 실제로는 아무 의미가 없기 때문에
- d. 활성화 함수가 2D에서는 다르게 작동하기 때문에

> [!success]- 정답 및 해설 **정답: b**
> 
> c)가 비단조(텐트 모양)이기 때문에, b)에서 같은 값을 갖는 여러 등고선 영역들이 서로 접혀 여러 번 반사된 것처럼 훨씬 촘촘한 패턴이 된다.

**63.** Deep network를 행렬·벡터로 표기할 때, $\boldsymbol\Omega_k$와 $\boldsymbol\beta_k$가 각각 의미하는 것은?

- a. $\boldsymbol\Omega_k$=편향 벡터, $\boldsymbol\beta_k$=가중치 행렬
- b. $\boldsymbol\Omega_k$=가중치 행렬(Weight matrix), $\boldsymbol\beta_k$=편향 벡터(Bias vector)
- c. 둘 다 활성화 함수를 의미한다
- d. 둘 다 손실함수를 의미한다

> [!success]- 정답 및 해설 **정답: b**
> 
> $\boldsymbol\Omega_k$는 가중치 행렬(Weight matrix), $\boldsymbol\beta_k$는 편향 벡터(Bias vector)를 의미한다.

**64.** 은닉층이 K개인 Deep network의 일반화된 계산 구조로 옳은 것은?

- a. 모든 층을 한꺼번에 동시에 계산한다
- b. $\mathbf{h}_k=a[\boldsymbol\beta_{k-1}+\boldsymbol\Omega_{k-1}\mathbf{h}_{k-1}]$ 형태의 동일한 연산을 층마다 반복 적용한다
- c. 각 층마다 완전히 다른 형태의 수식을 사용한다
- d. 층이 깊어질수록 활성화 함수를 사용하지 않는다

> [!success]- 정답 및 해설 **정답: b**
> 
> $\mathbf{h}_k=a[\boldsymbol\beta_{k-1}+\boldsymbol\Omega_{k-1}\mathbf{h}_{k-1}]$처럼 동일한 형태의 연산(가중치 행렬곱+편향 → 활성화 함수)이 층마다 반복 적용된다.

**65.** "복잡해 보이는 Deep network 수식"의 정체를 한 문장으로 정리하면?

- a. 매 층마다 완전히 새로운 연산 방식이 필요하다
- b. (가중치 행렬 곱 → 편향 더하기 → 활성화 함수)를 레이어마다 반복하는 것
- c. 딥러닝은 선형회귀와 아무 관련이 없다
- d. 층이 깊어지면 파라미터가 줄어든다

> [!success]- 정답 및 해설 **정답: b**
> 
> Deep network는 결국 (가중치 행렬 곱 → 편향 더하기 → 활성화 함수)라는 동일한 연산을 레이어 수만큼 반복하는 구조다.

---

## 22.3장. 역전파

**66.** 역전파(Backpropagation)에 대한 정의로 가장 알맞은 것은?

- a. 파라미터를 무작위로 초기화하는 절차
- b. 출력 오차를 기준으로 계산 그래프를 거꾸로(출력층→입력층) 따라가며 연쇄법칙으로 각 층의 미분값을 구하는 절차
- c. 순전파와 완전히 동일한 과정
- d. 학습률을 자동으로 조정하는 알고리즘

> [!success]- 정답 및 해설 **정답: b**
> 
> 역전파는 출력 오차를 기준으로 계산 그래프를 거꾸로 따라가며 연쇄법칙으로 각 층의 미분값을 구하는 절차다.

**67.** 역전파가 기반으로 하는 핵심 수학 원리는?

- a. 적분
- b. 연쇄법칙(chain rule) — 합성함수의 변화율 = 바깥의 변화율 × 안쪽의 변화율
- c. 행렬의 역행렬
- d. 삼각함수 항등식

> [!success]- 정답 및 해설 **정답: b**
> 
> 역전파는 연쇄법칙(합성함수의 변화율 = 바깥의 변화율 × 안쪽의 변화율)을 층마다 반복 적용하는 원리에 기반한다.

**68.** 역전파의 계산이 시작되는 지점은 어디인가?

- a. 입력층, 입력값으로부터 직접 시작
- b. 은닉층 중간 지점
- c. 출력층 — 손실함수 정의로부터 "출력이 바뀌면 손실이 얼마나 바뀌는가"를 직접 계산 가능
- d. 무작위로 정해진 층

> [!success]- 정답 및 해설 **정답: c**
> 
> 역전파는 출력층에서 시작한다. 손실함수 정의로부터 출력에 대한 손실의 변화율을 직접 계산할 수 있기 때문이다.

**69.** 역전파가 효율적인 이유로 가장 알맞은 것은?

- a. 모든 층의 기울기를 처음부터 각각 새로 계산하기 때문
- b. 각 층이 바로 다음 층에서 이미 계산해둔 기울기를 재사용하기 때문
- c. 활성화 함수를 사용하지 않기 때문
- d. 파라미터가 적기 때문

> [!success]- 정답 및 해설 **정답: b**
> 
> 각 층이 바로 다음 층에서 이미 계산해둔 기울기를 재사용하기 때문에, 처음부터 다시 계산하지 않아도 되어 효율적이다.

**70.** 역전파와 경사하강법의 관계에 대한 설명으로 옳은 것은?

- a. 둘은 완전히 같은 알고리즘이다
- b. 역전파는 모든 층의 기울기 계산까지만 하고, 그 기울기로 파라미터를 실제 수정하는 것은 경사하강법의 몫으로 분리되어 있다
- c. 경사하강법이 먼저 실행되고, 그 다음 역전파가 실행된다
- d. 역전파는 파라미터를 직접 수정하는 알고리즘이다

> [!success]- 정답 및 해설 **정답: b**
> 
> 역전파는 모든 층의 기울기를 계산하는 것으로 끝나고, 그 기울기를 이용해 파라미터를 실제로 수정하는 것은 경사하강법이라는 별도의 단계에서 이루어진다.

**71.** 역전파 이후 파라미터 업데이트에 대한 설명으로 옳은 것은?

- a. 사람이 결과를 보고 직접 하나씩 조정한다
- b. 특정 층 하나만 골라서 조정한다
- c. 기본적으로 모든 층의 가중치가 자동화된 수식에 의해 동시에 조정된다 (전이학습 등 일부 층을 얼리는 특수한 경우는 예외)
- d. 파라미터는 한 번 초기화되면 이후 절대 바뀌지 않는다

> [!success]- 정답 및 해설 **정답: c**
> 
> 기본적으로는 모든 층의 가중치가 경사하강법 공식에 의해 자동으로, 동시에 조정된다. (전이학습에서 특정 층을 "얼리는" 것은 예외적인 경우)