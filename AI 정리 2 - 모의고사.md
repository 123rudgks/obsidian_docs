# 자연어 처리(NLP) 확인문제

> - 본 노트(`AI_정리_2.md`)의 1~4장(워드 임베딩·RNN·LSTM, 언어모델·Seq2Seq·Attention, Self-Attention·Transformer, 사전학습·Encoder/Encoder-Decoder/Decoder 모델·In-Context Learning) 내용을 스스로 점검하기 위한 확인문제 모음
> - 정답 콜아웃은 접혀 있음 → 먼저 풀어본 후 클릭해서 펼쳐 확인할 것

---

## 1.1 워드 임베딩

### 문제 1. 워드 임베딩과 원-핫 인코딩의 차이점은 무엇인가?

A. 원-핫 인코딩은 단어 수만큼의 차원을 갖는 희소 벡터이고, 워드 임베딩은 저차원의 밀집 벡터이다. B. 원-핫 인코딩은 단어 간 의미 유사성을 잘 반영하지만, 워드 임베딩은 반영하지 못한다. C. 워드 임베딩은 항상 단어 개수와 동일한 차원을 가진다. D. 원-핫 인코딩은 반드시 학습을 통해서만 생성되지만, 워드 임베딩은 학습이 필요 없다. E. 워드 임베딩은 단어를 구분하기 위한 용도로만 쓰이고, 원-핫 인코딩은 단어 간 관계 분석에도 활용된다.

> [!question]- 정답 및 해설 **정답: A**
> 
> - 원-핫 인코딩은 단어 집합의 크기(어휘 수, Vocabulary size)만큼 차원을 만들고, 해당 단어 위치에만 `1`, 나머지는 `0`으로 채운다.
> - 워드 임베딩은 학습을 통해 단어를 100차원, 300차원 등 비교적 낮은 차원으로 압축하면서, 단어 간의 의미적 유사성을 반영한다.
> 
> → 따라서 **차원의 크기**와 **표현 방식(희소 vs 밀집)**이 이 두 방법의 가장 큰 차이점이다.
> 
> **오답 노트**
> 
> - B: 반대로 서술됨 — 의미 유사성 반영은 워드 임베딩의 특징(원-핫은 직교 문제로 불가)
> - C: 워드 임베딩 차원은 단어 개수와 무관하게 별도로 설정(100/300차원 등)
> - D: 반대로 서술됨 — 워드 임베딩이 학습을 통해 생성됨(원-핫은 규칙 기반으로 생성, 학습 불필요)
> - E: 반대로 서술됨

---

### 문제 2. Skip-gram과 CBOW의 차이점은 무엇인가?

A. CBOW는 항상 더 높은 정확도를 보장하고, Skip-gram은 속도만 빠르다. B. Skip-gram은 입력 차원이 항상 1이고, CBOW는 입력 차원이 단어 수와 같다. C. CBOW는 주변 단어(Context)로 중심 단어(Target)를 예측하고, Skip-gram은 중심 단어로 주변 단어를 예측한다. D. CBOW는 희소 벡터를 사용하고, Skip-gram은 밀집 벡터만 사용한다. E. Skip-gram은 지도학습이고, CBOW는 비지도학습이다.

> [!question]- 정답 및 해설 **정답: C**
> 
> **CBOW (Continuous Bag of Words)**
> 
> - 장점: 학습 속도가 빠르고 데이터가 적을 때 효과적
> - 예: `"The ___ is barking"` → 주변 단어 `["The", "is", "barking"]`로 중심 단어 `"dog"` 예측
> 
> **Skip-gram**
> 
> - 장점: 드문 단어(rare words) 학습에 더 효과적
> - 예: `"dog"` → 주변 단어 `["The", "is", "barking"]` 예측
> 
> → 가장 큰 차이점은 **예측 방향**에 있다 (CBOW: 주변→중심 / Skip-gram: 중심→주변)
> 
> **오답 노트**
> 
> - A: 반대 — 속도는 CBOW가 빠르고, 정확도가 항상 보장되는 쪽은 없음(상황에 따라 다름)
> - B·D: 둘 다 밀집 벡터(임베딩)를 사용하며, 입력 차원 관련 서술도 부정확
> - E: 둘 다 지도학습(주변 단어 예측이라는 정답이 있는 문제로 학습)

---

## 1.3 RNN

### 문제 3. RNN이 기존 신경망(FNN, CNN)과 다른 점은 무엇인가?

A. RNN은 은닉층이 전혀 존재하지 않고, 입력층과 출력층만으로 구성된다. B. RNN은 입력 데이터를 항상 고정된 길이의 벡터로만 처리할 수 있다. C. RNN은 이미지를 인식하기 위해 컨볼루션 필터를 사용한다. D. RNN은 순환 구조를 가지고 있어 시퀀스(Sequence) 데이터의 시간적 의존성을 학습할 수 있다. E. RNN은 순차 데이터 대신 오직 정적인 데이터(예: 고정된 이미지)만 처리할 수 있다.

> [!question]- 정답 및 해설 **정답: D**
> 
> - FNN, CNN은 입력 → 출력 방향으로만 흐르는 구조로, 입력 간 순서나 시간적 관계를 고려하지 못한다.
> - RNN은 은닉 상태를 다음 단계로 넘기는 순환 구조를 가지며, 이전 시점의 정보를 기억해 현재 출력에 반영할 수 있다.
> 
> → 따라서 자연어 처리(NLP), 음성 인식, 시계열 데이터 예측처럼 **순서(temporal order)**가 중요한 데이터에 적합하다.
> 
> **오답 노트**
> 
> - A: RNN도 은닉층(hidden state)이 핵심 구성 요소
> - B·E: 정반대 — RNN의 핵심 강점이 바로 **가변 길이**의 순차 데이터 처리
> - C: 컨볼루션 필터는 CNN의 특징

---

## 2.1 언어 모델 (N-gram)

### 문제 4. 다음 말뭉치 정보를 기반으로 4-gram 언어모델을 적용했을 때, 아래 문장에서 빈칸(______)에 올 단어의 확률이 가장 높은 것은 무엇인가?

> 문장: "SSAFY students study ______"
> 
> 말뭉치 정보
> 
> - count(SSAFY students study) = 900
> - count(SSAFY students study algorithm) = 270
> - count(SSAFY students study AI) = 450
> - count(SSAFY students study python) = 180

① algorithm ② AI ③ python ④ database

> [!question]- 정답 및 해설 **정답: ②**
> 
> 4-gram 모델에서는 직전 3개 단어("SSAFY students study")를 조건으로 다음 단어를 예측한다.
> 
> $$P(w \mid \text{SSAFY students study}) = \frac{\text{count(SSAFY students study } w)}{\text{count(SSAFY students study)}}$$
> 
> - ① $P(\text{algorithm}\mid\ldots) = 270/900 = 0.3$
> - ② $P(\text{AI}\mid\ldots) = 450/900 = 0.5$
> - ③ $P(\text{python}\mid\ldots) = 180/900 = 0.2$
> - ④ 말뭉치 정보에 `database`는 주어지지 않음 (확률 계산 불가)
> 
> → 네 후보 중 확률이 가장 높은 것은 **AI (0.5)**

---

## 2.2 Seq2Seq

### 문제 5. Seq2Seq 모델의 구조와 작동 방식에 대한 설명으로 가장 적절한 것은?

① 인코더는 출력 시퀀스를 생성하고, 디코더는 입력 시퀀스를 처리한다. ② 인코더와 디코더는 서로 독립적으로 학습되며, 따로 최적화된다. ③ 인코더는 입력 시퀀스를 고정 길이 벡터로 변환하고, 디코더는 이 벡터를 바탕으로 출력 시퀀스를 생성한다. ④ Seq2Seq는 입력과 출력의 길이가 반드시 동일해야 하는 모델이다.

> [!question]- 정답 및 해설 **정답: ③**
> 
> Seq2Seq 모델은 인코더-디코더 구조로 이루어져 있다.
> 
> - **인코더**: 입력 시퀀스를 한 타임스텝씩 읽어 고정된 차원의 벡터 표현(CONTEXT)으로 변환
> - **디코더**: 인코더에서 생성된 벡터를 조건으로 하여 출력 시퀀스를 생성
> - **End-to-End 학습**: 역전파를 통해 인코더와 디코더가 통합적으로 최적화됨
> - **가변 길이 처리**: 입력과 출력의 길이가 달라도 처리 가능 (예: 4단어 → 3단어)
> 
> **오답 노트**
> 
> - ①: 인코더·디코더 역할이 반대로 서술됨
> - ②: 반대 — 인코더·디코더는 하나의 통합 네트워크로 함께(End-to-End) 최적화됨
> - ④: 반대 — Seq2Seq의 핵심 강점이 바로 **길이가 다른 시퀀스 간의 매핑**을 처리할 수 있다는 것

---

## 2.3 Attention

### 문제 6. Attention 메커니즘의 가장 핵심적인 아이디어는 무엇인가?

① 인코더를 더 크게 만들어서 더 많은 정보를 저장한다. ② 번역할 때마다 입력 문장의 어느 부분이 중요한지 선택적으로 집중한다. ③ 디코더를 여러 개 사용해서 병렬로 번역한다. ④ 입력 문장을 더 작은 단위로 나누어서 처리한다.

> [!question]- 정답 및 해설 **정답: ②**
> 
> Attention은 문장을 번역할 때 "어느 부분이 중요한지" 스스로 판단한다. 매번(매 타임스텝마다) 선택적으로 집중하는 것이 Attention의 핵심 아이디어이다.
> 
> **오답 노트**
> 
> - ①: 인코더 크기를 키우는 것은 bottleneck problem에 대한 근본적 해결책이 아님(Attention의 핵심 아이디어와 무관)
> - ③: 디코더를 여러 개 병렬로 쓰는 것이 아니라, 하나의 디코더가 매 시점 인코더 전체를 다시 참조하는 방식
> - ④: 입력을 더 작은 단위로 나누는 것이 아니라, 이미 인코딩된 hidden state 전체 중 필요한 부분에 가중치를 주는 방식

---

## 3.1 Self-Attention

### 문제 7. Self-Attention의 설명에 해당하는 것을 고르시오.

① 시퀀스를 시간 순서대로 처리하며, 이전 시점의 은닉 상태를 다음 시점으로 전달해 의존성을 학습한다. ② 동일한 시퀀스로부터 쿼리(Q), 키(K), 값(V)를 생성하고, 토큰 간 유사도에 기반한 가중합으로 각 토큰을 문맥화한다. ③ Positional Encoding, Residual connection, Layer Normalization, Feed-Forward Network를 쌓아 시퀀스를 병렬 처리하는 아키텍처이다. ④ Word2Vec의 한 방식으로, 주변 단어(컨텍스트)로 중심 단어를 예측하여 단어 임베딩을 학습한다.

> [!question]- 정답 및 해설 **정답: ②**
> 
> **오답 노트**
> 
> - ①번은 **RNN**에 대한 설명이다.
> - ③번은 **Transformer**에 대한 설명이다.
> - ④번은 **CBOW**에 대한 설명이다.

---

## 3.1 Self-Attention의 한계와 해결법

### 문제 8. Attention 한계와 해결법을 짝지어 보라

**한계** ① 순서 정보 부재 ② 비선형성 부족 ③ 미래 참조 문제

**해결법** A. Feed-Forward Network 추가 B. Masked Self-Attention C. Positional Encoding

> [!question]- 정답 및 해설 **정답: ①-C, ②-A, ③-B**
> 
> - **①-C**: Self-Attention은 입력 토큰의 순서를 직접 활용하지 않는다. 따라서 위치 정보가 없으면 토큰 순서를 바꿔도 같은 결과가 나올 수 있다. 이를 보완하기 위해 Positional Encoding/Embedding을 입력에 더하거나 결합해 위치 정보를 주입한다.
> - **②-A**: 가중합은 선형 결합이므로 복잡한 비선형 변환 능력이 제한된다. 이를 해결하기 위해 어텐션 뒤에 각 토큰 위치에 독립적으로 적용되는 비선형 FFN을 배치한다.
> - **③-B**: 왼쪽에서 오른쪽으로 예측할 때, 일반 Self-Attention은 미래 토큰까지 볼 수 있어 정보 누수(look-ahead)가 발생한다. 현재 위치 이후의 점수에 −∞-\infty −∞를 부여해 softmax 가중치가 0이 되도록 마스킹하여 이를 차단한다.

---

## 3.2 Transformer

### 문제 9. Transformer 모델의 Attention Head의 개수가 늘어날수록 성능이 무한히 향상되는가?

O / X

> [!question]- 정답 및 해설 **정답: X**
> 
> 멀티헤드는 다양한 관계를 병렬로 포착하지만, 헤드 수가 일정 수준을 넘으면 유사한 패턴만 반복 학습하는 '중복 헤드'가 늘어나기 쉽다. 또한 모델 성능은 학습 데이터 규모, 컨텍스트 길이, 최적화 설정 등 여러 요인의 영향을 받으므로, 성능이 가장 잘 나오는 최적의 헤드 수가 따로 존재한다. 그 임계를 넘어가면 오히려 역효과가 날 수 있다.

---

## 4.1 사전학습(Pretraining)

### 문제 10. 네트워크를 무작위로 초기화한 후, 언어 패턴을 학습하는 과정이며 이후 특정 태스크에 최적화(파인튜닝)하기 전에 거치는 첫 번째 학습 단계를 무엇이라고 하는가?

> [!question]- 정답 및 해설 **정답: 사전학습(Pretraining)**
> 
> - 사전학습은 네트워크를 무작위로 초기화한 뒤, 대규모 텍스트 데이터를 통해 단어의 의미와 언어 패턴을 학습하는 단계이다.
> - 이후 파인튜닝(fine-tuning)을 통해 이렇게 학습된 파라미터를 출발점 삼아, 실제로 풀고 싶은 특정 태스크(분류, 번역 등)에 맞춰 추가로 적응시킨다.
> 
> → 즉 "일반적인 언어 패턴 학습(사전학습) → 특정 태스크 최적화(파인튜닝)"의 2단계 패러다임에서, 질문이 가리키는 것은 **첫 번째 단계인 사전학습**이다.

---

## 4.2 Encoder 모델 (BERT)

### 문제 11. BERT 모델의 양방향 문맥 이해를 위한 주요 학습 방법 중 하나로, 입력 토큰의 일부를 가리고(마스킹하고) 예측하도록 하는 기법은 무엇인가?

> [!question]- 정답 및 해설 **정답: MLM (Masked Language Model)**
> 
> - BERT는 입력 토큰의 15%를 무작위로 선택해, 이 중 일부를 [MASK] 토큰으로 치환(80%)하거나 랜덤 토큰으로 치환(10%), 혹은 그대로 두는(10%) 방식으로 처리한다.
> - 마스킹된 위치의 Transformer Encoder 출력을 FFNN + Softmax에 통과시켜, 그 자리에 원래 있었던 단어를 예측하도록 학습한다.
> - 이 방식은 Encoder가 **양방향 문맥**(앞·뒤 단어를 모두 참조)을 활용해 마스킹된 자리를 예측하게 만든다는 점에서, 전통적인 왼→오 순차 예측 언어모델과 구분된다.

---

### 문제 12. BERT 모델에서 두 개의 문장 세그먼트를 구분하기 위해 사용하는 스페셜 토큰은?

a. [CLS] (시작 토큰) b. [MASK] (가려진 토큰) c. [SEP] (세그먼트 구분 토큰) d. [END] (종료 토큰)

> [!question]- 정답 및 해설 **정답: c. [SEP] (세그먼트 구분 토큰)**
> 
> BERT의 입력 구조는 `[CLS] 문장A [SEP] 문장B [SEP]` 형태이며, **[SEP]**가 첫 번째 문장과 두 번째 문장의 경계를 표시한다. 여기에 Segment Embeddings($E_A$/$E_B$)이 더해져 각 토큰이 어느 문장에 속하는지 구분된다.
> 
> **오답 노트**
> 
> - a: [CLS]는 문장(쌍) 전체를 대표하는 토큰으로, NSP(Next Sentence Prediction) 태스크에 사용됨 — 세그먼트 구분 용도가 아님
> - b: [MASK]는 MLM 학습을 위해 단어를 가리는 토큰으로, 세그먼트 구분과 무관
> - d: BERT에는 [END]라는 특수 토큰이 존재하지 않음

---

## 4.3 Encoder-Decoder 모델 (T5)

### 문제 13. T5(Encoder-Decoder 모델)가 모든 NLP 문제를 Text-to-Text 형식으로 변환하여 학습하는 주요 방법은 무엇인가?

> [!question]- 정답 및 해설 **정답: Span Corruption**
> 
> - T5는 입력 문장에서 연속된 토큰(span)을 무작위로 선택해 제거하고, 그 자리를 `<X>`, `<Y>` 같은 특수 placeholder 토큰으로 치환한다.
> - Decoder는 각 placeholder에 해당하는 원래 span을 복원하도록 학습되며, 이를 통해 Encoder-Decoder 구조를 언어 모델링처럼 훈련할 수 있다.
> - BERT의 MLM이 개별 토큰 하나를 마스킹하는 것과 달리, T5의 Span Corruption은 **연속된 여러 토큰(구간)**을 통째로 가리고 **자기회귀적으로 순차 생성**해 복원한다는 점이 핵심 차이다.

---

### 문제 14. 다음 중 T5 모델의 주요 특징이 아닌 것은?

a. 모든 NLP 태스크를 Text-to-Text로 변환한다. b. Span Corruption 방식으로 학습한다. c. Encoder와 Decoder를 모두 사용한다. d. Next Sentence Prediction (NSP) 방식으로 두 문장 세그먼트의 연결 관계를 예측한다.

> [!question]- 정답 및 해설 **정답: d. Next Sentence Prediction (NSP) 방식으로 두 문장 세그먼트의 연결 관계를 예측한다.**
> 
> NSP는 **BERT**의 학습 방법 중 하나로, 두 문장이 실제로 이어지는 문장인지 여부를 예측하는 태스크이다. T5는 NSP가 아니라 **Span Corruption** 방식으로 학습한다.
> 
> **오답 노트**
> 
> - a: T5의 핵심 특징 — 번역, 분류, 요약 등 모든 태스크를 입출력이 모두 텍스트인 통일된 포맷으로 변환
> - b: T5의 사전학습 방법이 맞음(연속된 span을 제거하고 복원)
> - c: T5는 Transformer Encoder-Decoder 구조 기반 모델이 맞음

---

## 4.4 Decoder 모델

### 문제 15. 다음 중 단방향(Autoregressive) 구조를 가지며 강력한 텍스트 생성 능력에 강점을 보이는 모델 유형은?

a. Encoder 모델 b. Encoder-Decoder 모델 c. Decoder 모델 d. Word Embedding 모델

> [!question]- 정답 및 해설 **정답: c. Decoder 모델**
> 
> Decoder 모델(예: GPT 시리즈)은 왼쪽→오른쪽으로 다음 단어를 예측하는 **Autoregressive LM** 방식으로 사전학습되며, 미래 단어를 참조할 수 없는 단방향 구조를 가진다. 이 구조 덕분에 대화, 요약 등 **출력이 시퀀스인 생성 태스크**에 특히 강점을 보인다.
> 
> **오답 노트**
> 
> - a: Encoder 모델(예: BERT)은 양방향 문맥을 활용하는 구조로, 이해 중심 태스크(분류, NER 등)에 강함 — 생성에는 부적합
> - b: Encoder-Decoder 모델(예: T5)은 Encoder와 Decoder를 결합한 구조로, 단방향 구조만을 가진다고 볼 수 없음
> - d: Word Embedding 모델(예: Word2Vec)은 단어 벡터 학습이 목적이며, 시퀀스 생성 능력 자체가 없음

---

## 4.5 In-Context Learning

### 문제 16. GPT-3에서 등장한 개념으로, 모델을 파인튜닝하지 않고 자연어 설명과 예시를 입력으로 제공하여 예측을 수행하게 만드는 학습 방식은?

> [!question]- 정답 및 해설 **정답: In-Context Learning (ICL)**
> 
> - GPT-3는 별도의 fine-tuning(gradient 업데이트) 없이, 컨텍스트(문맥) 안에 자연어로 된 태스크 설명과 예시(0개/1개/여러 개)를 주는 것만으로 새로운 태스크를 수행할 수 있음을 보였다.
> - 이를 Zero-shot / One-shot / Few-shot으로 구분하며, 예시 개수가 많을수록(Zero-shot < One-shot < Few-shot), 그리고 모델 파라미터 크기가 클수록 성능이 향상되었다.
> - 핵심은 **파라미터가 실제로 갱신되는 것이 아니라**, 사전학습으로 이미 갖춰진 패턴 인식·완성 능력을 문맥에 맞게 불러와 적용하는 것이라는 점이다.

---

### 문제 17. Chain-of-Thought (CoT) Prompting의 핵심적인 역할은 무엇인가?

a. 모델의 학습 속도를 획기적으로 높이기 b. 파인튜닝 과정에서 사용되는 데이터의 양 줄이기 c. 논리적인 사고 유도하여 답을 도출하기 d. 사전학습 시 단어 임베딩의 품질을 높이는 것

> [!question]- 정답 및 해설 **정답: c. 논리적인 사고 유도하여 답을 도출하기**
> 
> - 단순한 Few-shot prompting만으로는 여러 단계를 거쳐야 하는(multi-step) 문제를 풀기 어려운 한계가 있었다.
> - CoT prompting은 예시의 답에 **풀이 과정(중간 추론 단계)**을 포함시켜, 모델이 이 패턴을 따라 새로운 문제도 논리적인 사고 단계를 거쳐 최종 답을 도출하도록 유도하는 기법이다.
> - 예시가 없어도 질문 뒤에 "Let's think step by step"이라는 문장만 추가해 같은 효과를 유도하는 **Zero-Shot CoT**도 등장했다.
> 
> **오답 노트**
> 
> - a: CoT는 학습 속도와 무관한 **prompting(추론 시점) 기법**이며, 사전학습·파인튜닝 자체를 가속화하지 않음
> - b: CoT는 파인튜닝을 아예 거치지 않는 in-context learning의 연장선에 있는 기법
> - d: 단어 임베딩 품질과는 무관하며, CoT는 워드 임베딩보다 훨씬 상위 단계인 "추론 과정 유도"에 관한 기법

---

## 오답 체크리스트

- [ ] 문제 1
- [ ] 문제 2
- [ ] 문제 3
- [ ] 문제 4
- [ ] 문제 5
- [ ] 문제 6
- [ ] 문제 7
- [ ] 문제 8
- [ ] 문제 9
- [ ] 문제 10
- [ ] 문제 11
- [ ] 문제 12
- [ ] 문제 13
- [ ] 문제 14
- [ ] 문제 15
- [ ] 문제 16
- [ ] 문제 17