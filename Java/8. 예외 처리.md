try 블록에서 예외 발생시 JVM이 해당 Exception 클래스의 객체 생성 후 던짐 (throw)


throws 키워드를 통한 처리 위임
- method에서 처리해야 할 하나 이상의 예외를 호출한 곳으로 전달
```java
// checked exception일 경우
void exceptionMethod() throws Exception1, Exception2{
	// 예외 발생 코드
	// 여기선 try-catch 안해도 됨
}

void methodCaller(){
	try{ // 이 메소드 쓰는 곳에서 에러 처리 해줘야 함. 아니면 얘도 throws로 넘기든가
		exceptionMethod();  
	}catch(Exception e){}
}
```
> unchecked exception이었다면 throws하지 않아도 전달 됨. 그래도 결국 try-catch는 해줘야함


사용자 정의 예외
```java
class FruitNotFoundException extends Exception{
	public FruitNotFoundException(String name){
		super(name + "에 해당하는 과일은 없습니다");
	}
}

public clss CustomExceptionTest{
	private static String[] fruits = {"사과","오렌지", "토마토"};
	public static void main(String[] args){
		try{
			getFruit2("오렌지");
			getFruit2("오렌지");	
		}catch(FruitNotFoundException e){
			e.printStackTrace();
		}
	}
}

private static void getFruit2(String name) throws FruitNotFoundException{
	for(int i = 0; i< fruits.length; i++){
		if(fruits[i] != null && frutis[i].equals(name)){
			fruits[i] = null;
			return;
		}
	}
	throw new FruitNotFoundException(name);
}
```

