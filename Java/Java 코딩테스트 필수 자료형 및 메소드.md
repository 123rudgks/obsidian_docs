
## 기본형

- `int`
- `long`
- `double`
- `boolean`
- `char`
- `+`, `-`, `*`, `/`, `%`
- `&`, `|`, `^`, `~`
- `<<`, `>>`

## String

- `length()`
- `charAt()`
- `equals()`
- `substring()` 1
- `contains()` 1
- `indexOf()` 1
- `split()` 1 
- `replace()` 1
- `toCharArray()`
- `Integer.parseInt()`
- `Long.parseLong()`
- `String.valueOf()` 1

## StringBuilder 1

- `append()`
- `insert()`
- `delete()`
- `deleteCharAt()`
- `reverse()`
- `charAt()`
- `length()`
- `toString()`

## 배열

- `int[]`
- `long[]`
- `char[]`
- `boolean[]`
- `int[][]`
- `arr.length`
- `clone()` 1

## Arrays

- `Arrays.sort()` 1
- `Arrays.fill()` 1
- `Arrays.copyOf()` 1
- `Arrays.copyOfRange()` 1
- `Arrays.equals()` 1
- `Arrays.toString()` 1
- `Arrays.deepToString()` 1
- `Arrays.binarySearch()` 1

## List / ArrayList 1

- `List`
- `ArrayList`
- `add()`
- `get()`
- `set()`
- `remove()`
- `size()`
- `isEmpty()`
- `contains()`
- `clear()`
- `sort()`

## Queue 1

- `Queue`
- `ArrayDeque`
- `offer()`
- `poll()`
- `peek()`
- `size()`
- `isEmpty()`

## Deque 1

- `Deque`
- `ArrayDeque`
- `offerFirst()`
- `offerLast()`
- `pollFirst()`
- `pollLast()`
- `peekFirst()`
- `peekLast()`
- `push()`
- `pop()`
- `peek()`

## Set / HashSet 1

- `Set`
- `HashSet`
- `add()`
- `remove()`
- `contains()`
- `size()`
- `isEmpty()`
- `clear()`

## TreeSet 1

- `add()`
- `remove()`
- `contains()`
- `first()`
- `last()`
- `lower()`
- `floor()`
- `higher()`
- `ceiling()`

## Map / HashMap 1

- `Map`
- `HashMap`
- `put()`
- `get()`
- `getOrDefault()`
- `containsKey()`
- `containsValue()`
- `remove()`
- `size()`
- `isEmpty()`
- `keySet()`
- `entrySet()`

## TreeMap 1

- `put()`
- `get()`
- `remove()`
- `firstKey()`
- `lastKey()`
- `lowerKey()`
- `floorKey()`
- `higherKey()`
- `ceilingKey()`

## PriorityQueue 1

- `PriorityQueue`
- `offer()`
- `poll()`
- `peek()`
- `size()`
- `isEmpty()`
- `Collections.reverseOrder()`

## BigInteger 1

- `add()`
- `subtract()`
- `multiply()`
- `divide()`
- `mod()`
- `compareTo()`

## Collections 1

- `Collections.sort()`
- `Collections.reverseOrder()`
- `Collections.min()`
- `Collections.max()`

## Comparator 1

- `Comparator`
- `Comparator.naturalOrder()`
- `Comparator.reverseOrder()`
- `Integer.compare()`
- `Long.compare()`

## Math 1

- `Math.max()`
- `Math.min()`
- `Math.abs()`
- `Math.sqrt()`
- `Math.pow()`
- `Math.ceil()`
- `Math.floor()`
- `Math.round()`

## 입력 1

- `BufferedReader`
- `InputStreamReader`
- `readLine()`
- `StringTokenizer`
- `nextToken()`
- `hasMoreTokens()`

## 출력 1

- `StringBuilder`
- `System.out.print()`
- `System.out.println()`
    

## 자주 쓰는 변환 1

- `Integer.parseInt()`
- `Long.parseLong()`
- `String.valueOf()`
- `toCharArray()`
- `c - '0'`
- `(char)(n + '0')`

## 추가로 숙지

- 오버플로우
    
- `int` → `long` 형변환
    
- 문자열 `equals()`
    
- 정수 나눗셈
    
- 나머지 연산 `%`
    
- 비트 연산
    
- 람다식 정렬
    
- 2차원 배열
    
- 사용자 정의 객체 정렬