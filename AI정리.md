

>[!note]
>내 표현으로 재 정리 한 후 AI 돌려 팩트 체크하기

- [ ] 강사님 블로그 보고 정리
- [ ] 강의 보고 PPT 내용으로 2차 정리
- [ ] 최종적으로 전체 내용 다듬기
## 선형 회귀
x,y 축에 있는 데이터 포인트들과 가장 근접한 직선을 찾는 것.
찾으면, 미래 데이터도 직선 근처에 있을 가능성이 큼

## 손실 함수
선형 회귀는 방정식을 단번에 푸는 것이 아닌 일단 선을 그어보고 점차 개선해 나가는 방식
이때 손실 함수를 사용하여 예측선과 실제 값이 얼마나 차이가 나는지 확인하면서 찾는다.
> 계속 무작위적으로 기울기와 절편을 찾는건 아니고 미분을 통해 기울기를 확인하며 찾아간다.

손실 함수 종류

단순 오차 합
$Loss = \sum_{i=1}^{N}(y_i-\hat{y_i})$

**평균 제곱 오차(mean squared error)**
$MSE=\frac{1}{N}\sum_{i=1}^N(y_i - \hat{y_i})^2$

평균 절대 오차(mean absolute error)
$MAE=\frac{1}{N}\sum_{i=1}^{N}\left\vert(y_i-\hat{y_i}\right\vert)$


## 다중 선형 회귀
단순하게 입력값 x 하나만 y에 영향을 미치는 경우에는 위 MSE식 처럼 간단하게 손실 함수 계산도 되고 계산이 어렵지 않다.
그러나 세상에 대부분은 다양한 변수에 영향을 받는다.

결과값(y)에 예를 들어 3개의 변수가 영향을 미친다고 할 때 각 변수마다 또 y에 영향을 미치는 정도(회귀 가중치)가 다를 거다.

>[!note]
>변수가 y에 미치는 영향력은 상관계수가 아니다. 상관계수는 두 변수가 얼마나 함께 움직이는 지를 나타내는 값
> 가중치는 나머지 변수들의 값을 고정해놓고 상관계수를 확인하려는 변수 하나만 값을 조정하면서 결과값에 미치는 영향을 본다. (ex : 평수와 ,방개수, 집값. 방개수와 집값의 상관계수는 높게 나오지만 이는 평수와 방개수의 상관 계수가 높아서 그럼. 평수를 고정하고 방개수와 집값의 상관계수를 확인하면 다르게 나온다.)


따라서 결과값에 대한 여러 변수들에 대한 식을 다시 작성하면 아래와 같다. 
$\hat{y} = w_1x_1 + w_2x_2 + \cdots + w_nx_n + b$

또 다중 선형 회귀의 Loss 함수는 아래와 같다.
$Loss = \frac{1}{N}\sum_{i=1}^{N}\left(y_i - (w_1x_{i1} + w_2x_{i2} + \cdots + w_nx_{in} + b)\right)^2$

이렇게 되면 연산을 할게 너무 많아져서 물리적으로 컴퓨터가 감당을 할 수가 없다.
게다가 AI한테 넣을 값은 변수가 수천만개이상임 ㅎㄷㄷ...

## 경사 하강법

위와 같은 이유로 경사 하강법을 사용하여 예측선과 실제값 차이의 오차를 줄여나간다.

Loss를 최소화 하는 최적의 가중치를 반복적으로 조금씩 조정하면서 업데이트를 하는데 이것에 바로 AI가 최적선을 **학습**하는 것이다.

그럼 경사 하강법은 어떻게 이걸 가능하게 하지? 단순 선형회귀때 처럼 기울기와 절편에 ...
-> 현재 오차 기울기를 감지해서 조금씩 이동하면서 찾음

