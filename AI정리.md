

>[!note]
>내 표현으로 재 정리 한 후 AI 돌려 팩트 체크하기
## 선형 회귀
x, y축에 있는 데이터 포인트들과 가장 근접한 직선을 찾는 것. 찾으면, 미래 데이터도 직선 근처에 있을 가능성이 큼
$$y = wx + b$$
## 손실 함수
선형 회귀에서 최적의 선을 찾는 방법 중 하나는, 일단 선을 그어보고 점차 개선해 나가는 방식이다. 이때 손실 함수를 사용하여 예측선과 실제 값이 얼마나 차이가 나는지 확인하면서 찾는다.
>[!questions]+
> 계속 무작위적으로 기울기와 절편을 찾는건 아니고 미분을 통해 기울기를 확인하며 찾아간다.
> 엥? 이거 그냥 경사하강법이랑 같은거 아닌가? 
> 맞다 그러나 단순 선형회귀의 경우 아래에서 보듯 미분값이 0이 되는 지점을 공식으로 바로 계산할 수 있어서 단순 선형 회귀에서는 사실 반복적으로 개선할 필요는 없다.


#### 손실 함수 종류

단순 오차 합
$$Loss = \sum_{i=1}^{N}(y_i-\hat{y_i})$$

**평균 제곱 오차(mean squared error)**
$$MSE=\frac{1}{N}\sum_{i=1}^N(y_i - \hat{y_i})^2$$

평균 절대 오차(mean absolute error)
$$MAE=\frac{1}{N}\sum_{i=1}^{N}\left\vert(y_i-\hat{y_i}\right\vert)$$


### 단순 선형회귀에서 기울기와 절편을 찾는 방법

#### 핵심
- 단순 선형회귀 $y = wx + b$에서 최적의 기울기 $w$와 절편 $b$는
  **반복 계산 없이 공식으로 한 번에** 구할 수 있다.
#### 공식이 나오는 흐름 (직관)
1. 오차를 재는 손실함수로 **잔차제곱합(RSS)**을 사용한다.
   $RSS(w, b) = \sum_{i=1}^n \left(y_i - (wx_i + b)\right)^2$
2. 이 식은 $w, b$에 대한 **이차식**이다.
   → 그래프로 그리면 아래로 볼록한 그릇 모양이라 최솟값이 딱 하나뿐이다. 그리고 그 지점은 미분(기울기)가 0이 되는 지점이다.
3. 그 지점에서의 $w, b$를 직접적으로 구할 수 있다.

## 한 줄 요약
> 잔차제곱합(RSS)을 세우고 → 미분해서 기울기가 0인 지점을 찾으면 → 
> 그게 바로 단순 선형회귀의 최적 w, b다.

### 단순선형회귀 결과 해석

보고 해석만 할 줄 알면 됨

|     | Coefficient        | Std. Error | t-statistic | p-value |
| --- | ------------------ | ---------- | ----------- | ------- |
| 절편  | $\hat{B_0}=7.0325$ | 0.4578     | 15.36       | 0.0001  |
| 기울기 | $\hat{B_1}=0.0475$ | 0.0027     | 17.67       | 0.0001  |
Coefficient : 기울기, 절편의 추정치
Std.Error : 새로운 데이터가 들어왔을 때 Coefficient 값이 바뀔 확률. 추정치의 불확실성(정밀도)을 뜻한다.
t-statistic : $\frac{Coefficient}{Std.Error}$ , 0에 가까울 수록 계수가 통계적으로 유의하지 않을 가능성이 크다.
p-value : 기울기가 0일 확률, 즉 결과값과 변수가 관계가 없을 확률. 높을수록 x와 y의 관계가 없다는 뜻. 또는 낮을 수록 x와 y의 관계가 유의미하다. t-statistic의 절댓값이 작을 수록 p-value가 커진다. 

보통 p-value$\le 0.05$ 이면 통계적으로 매우 유의미함
Coefficient 값의 크기만 보고 선형성이 있다 없다를 판다하면 안됨 (Std.error, t-statistic , p-value 등을 봐야함)

## 다중 선형 회귀
단순하게 입력값 x 하나만 y에 영향을 미치는 경우에는 위 MSE식 처럼 간단하게 손실 함수 계산도 되고 계산이 어렵지 않다. 그러나 세상에 대부분은 다양한 변수에 영향을 받는다.

결과값(y)에 예를 들어 3개의 변수가 영향을 미친다고 할 때 각 변수마다 또 y에 영향을 미치는 정도(회귀 가중치)가 다를 거다.

>[!note]
>변수가 y에 미치는 영향력은 상관계수가 아니다. 상관계수는 두 변수가 얼마나 함께 움직이는 지를 나타내는 값
> 가중치는 나머지 변수들의 값을 고정해놓고 영향력을 확인하려는 변수 하나만 값을 조정하면서 결과값에 미치는 영향을 본다. (ex : 평수와 ,방개수, 집값. 방개수와 집값의 상관계수는 높게 나오지만 이는 평수와 방개수의 상관 계수가 높아서 그럼. 평수를 고정하고 방개수와 집값의 상관계수를 확인하면 다르게 나온다.)

따라서 결과값에 대한 여러 변수들에 대한 식을 다시 작성하면 아래와 같다. 
$$\hat{y} = w_1x_1 + w_2x_2 + \cdots + w_nx_n + b$$

또 다중 선형 회귀의 Loss 함수는 아래와 같다.
$Loss = \frac{1}{N}\sum_{i=1}^{N}\left(y_i - (w_1x_{i1} + w_2x_{i2} + \cdots + w_nx_{in} + b)\right)^2$

단순 선형 회귀와 마찬가지로 RSS가 최소일 떄 얻어지는 계수 $w_1,w_2,w_3,w_4...w_p$가 최적 추정치이다.

그럼 이 식을 어떻게 푸느냐? 단순 선형회귀떄는 공식으로 기울기와 절편을 직접 구할 수 있었는데 이건 변수가 너무 많잖아

행렬  표현
가지고 있는 데이터들을 행렬로 표현하면 아래와 같다. 그러면 행렬로 이전 단순 선형 회귀때 참함수와 같은 모습으로 표현할 수 있다.
$$ \mathbf{y} = \begin{bmatrix} y_1 \\ y_2 \\ \vdots \\ y_n \end{bmatrix} = \begin{bmatrix} 1 & x_{11} & \cdots & x_{1p} \\ 1 & x_{21} & \cdots & x_{2p} \\ \vdots & \vdots & \vdots & \vdots \\ 1 & x_{n1} & \cdots & x_{np} \end{bmatrix} \begin{bmatrix} \beta_0 \\ \beta_1 \\ \vdots \\ \beta_p \end{bmatrix} + \begin{bmatrix} \epsilon_1 \\ \epsilon_2 \\ \vdots \\ \epsilon_n \end{bmatrix} = \mathbf{X}\boldsymbol{\beta} + \boldsymbol{\epsilon} $$

이를 이용하여 최소 제곱법 공식으로 표현하면 아래와 같다. 아래 T는 전치(transpose)를 뜻하며, 벡터에 자기 자신의 전치를 곱하면 ($V^TV$)각 원소를 제곱해서 더한 값이 된다. 즉 $(y-X\hat\beta)^T(y-X\hat\beta)$는 $∑(yi​−\hat{y_i}​​)2$ 과 같은 것이라서 결국 모양이 단순 선형 회귀때의 RSS와 동일해진다.
$$ \begin{aligned} RSS &= (\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\beta}})^\top (\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\beta}}) \\ &= \mathbf{y}^\top\mathbf{y} - \hat{\boldsymbol{\beta}}^\top\mathbf{X}^\top\mathbf{y} - \mathbf{y}^\top\mathbf{X}\hat{\boldsymbol{\beta}} + \hat{\boldsymbol{\beta}}^\top\mathbf{X}^\top\mathbf{X}\boldsymbol{\beta} \end{aligned} $$
그리하여 이 또한 결국 2차식이기 때문에 RSS를 최소화 하는 값은 기울기가 0이 되는 지점이다. 따라서 미분을 하면 아래와 같은 공식으로 표현할 수 있다.
$$ \frac{\partial RSS}{\partial \hat{\boldsymbol{\beta}}} = -2\mathbf{X}^\top\mathbf{y} + 2\mathbf{X}^\top\mathbf{X}\hat{\boldsymbol{\beta}} = \mathbf{0} $$
위 미분 공식을 잘 정리하면 우리가 찾고 싶은 $\hat{\beta}$의 해를 아래와 같이 구할 수 있다.
$$ \hat{\boldsymbol{\beta}} = [\hat{\beta}_0, \hat{\beta}_1, \dots, \hat{\beta}_p]^\top = (\mathbf{X}^\top\mathbf{X})^{-1} \mathbf{X}^\top\mathbf{y} $$

그러나 다중 선형 회귀때에도 단순 선형 회귀와 같이 미분하여 직접적으로 $w,b$를 찾을 수는 있지만 다중 선형 회귀는 변수가 많아 행렬을 사용해서 연산이 필요한데 이게 연산량이 매우 많다. 게다가 AI 학습시 사용할 변수는 수천만개가 되는 경우도 있어서 컴퓨터가 물리적으로 답을 찾을 수가 없다.

>[!note]
>위 행렬 식을 계산할 줄 아는건 조금만 투자하면 누구나 할 수 있는 것인데, 이걸 할 줄 아는 사람과 그냥 이해하길 포기한 사람의 차이는 꽤 크다.
>여유가 되면 한번 공부해보는것도 좋다. 
>이젠 수학은 시험보기 위한게 아니라 지식을 더 깊이 이해하기 위한 수단이다.

#### 다중 선형 회귀 결과

다중 선형 회귀의 그래프는 더 이상 직선이 아니다. 평면이 된다.

|       | Coefficient         | Std. Error | t-statistic | p-value |
| ----- | ------------------- | ---------- | ----------- | ------- |
| 절편    | $\hat{B_0}=7.0325$  | 0.4578     | 15.36       | 0.0001  |
| 파라미터1 | $\hat{B_1}=0.0475$  | 0.0014     | 32.81       | 0.0001  |
| 파라미터2 | $\hat{B_2}=0.189$   | 0.0086     | 21.89       | 0.0001  |
| 파라미터3 | $\hat{B_3}=-0.0001$ | 0.0059     | -0.18       | 0.8599  |
위 결과에서 파라미터3은 p-value가 굉장히 크다. 그래서 파라미터3은 결과값에 대해 유의미하지 않을 가능성이 크다라고 해석할 수 있다.

### 다중선형회귀시 회귀계수 해석의 주의점
- 이상적인 상황 : 변수들이 서로 연관되지 않고, 독립적일 때에는 계수 해석이 명확함
- 만약 변수들이 서로 연관되어 있다면 계수 추정이 불안정해지고 해석에 혼동이 발생할 수 있음
- 주의 : 관찰 데이터의 상관관계로 인과관계를 주장하면 안됨 (ex : 아이스크림 소비량 vs 상어에 물리는 사건)

## 분류
y값이 숫자가 아닌 범주를 다루는 문제

분류 함수
입력 : X, 특성 벡터
출력 : y, 범주가 가질 수 있는 값 (ex : color범주 안의 black, red .., 순서가 없다.)
목표 : 
- 분류 함수 f(x)를 학습하여 입력 X가 속할 범주를 예측
- 그러나 범주를 직접 예측하는것 보다 범주에 속할 확률을 구하는게 더 유용할 때가 많음

로지스틱 회귀(Logistic Regression)은 분류를 다루는 모델

분류 문제에 선형회귀 쓰면 안되나?
No, 이진 분류 문제가 발생함
ex : 
응급실 환자 진단 (0: 비응급, 1: 응급)
선형 회귀는 확률 범위(0~1)을 벗어나는 값을 내놓을 수 있어서 문제가 발생함.
선형이니까 값이 1보다 클 수도 있고 0보다 작을 수도 있음.. 말이 안됨 그래서 부적절

그럼 뭘 쓰냐?
**로지스틱 회귀**
- 시그모이드(Sigmoid)함수를 씀, 입력 변수가 얼마나 크든지 작든지 이건 0~1 범위내 확률 값 예측 보장함. 그래서 확률값으로 사용하기 적절함
- 그냥 선형함수가 0~1을 벗어나지 못하도록 강제한 느낌

# 로지스틱 회귀

| 출력 범위가 0~1인 시그모이드 함수
$$ y = \frac{e^z}{1+e^z} = \frac{1}{1+e^{-z}} $$ (오일러 수 $e \approx 2.71828$)
- z라는 인풋이 커질 수록 1에 가까워지고 0에 가까워 질 수록 0.5, 더 작아질 수록 0에 가까워 진다.
모든 실수 $z$에 대한 $y$값의 범위는 $0 \le y \le 1$ 이다. 
- $z \to +\infty$ 일 때, $y$는 $1$로 수렴 - $z \to -\infty$ 일 때, $y$는 $0$으로 수렴


## 로지스틱 회귀 (Logistic Regression)의 모형식
### 시그모이드 함수에 선형회귀 식 대입 
시그모이드(Sigmoid) 함수 $y = \dfrac{e^z}{1+e^z}$ 의 $z$에 (Linear Regression) 식 $z = \beta_0 + \beta_1 x$ 를 대입하면, $$ y = \frac{e^{\beta_0 + \beta_1 x}}{1 + e^{\beta_0 + \beta_1 x}} $$ | 그냥 선형식을 0~1 범위로 제한해주기 위해서 변형한것 같은데...

### 확률 표기로 나타낸 모형식 확률 표기 $p(x)$를 활용한다면, 로지스틱 회귀의 모형식이 된다. 

$p(X) = P(Y=1 \mid X)$ 로 두고, (X가 주어졌을 때, 결과 Y가 1일 확률) $$ p(X;\boldsymbol{\beta}) = \frac{e^{\beta_0+\beta_1x}}{1+e^{\beta_0+\beta_1x}} = \frac{1}{1+e^{-(\beta_0+\beta_1x)}}, \qquad \boldsymbol{\beta} = [\beta_0, \beta_1]^\top $$
$p(X;\boldsymbol{\beta})$ : $X$에 대해서 Y=1일 확률을 $\beta$라는 파라미터를 이용해서 표현한 식이란 뜻

결국 학습해야 하는 값은  $\beta$로 선형 회귀와 달라진건 없음. 그냥 함수 꼴만 달라진 것


## 로지스틱 회귀 모형 vs 선형 모형

#### 로지스틱 함수와 선형회귀의 관계

- **로지스틱 함수** $p(x;\boldsymbol{\beta}) = \dfrac{e^{\beta_0+\beta_1x}}{1+e^{\beta_0+\beta_1x}}$ 는 선형 함수 $y = \beta_0 + \beta_1 x$ 를 내포하고 있다.
- 이 두 함수 간의 관계를 해석하기 위해 오즈(Odds)와 **로짓 변환**(logit)에 대해 알아보자

#### 오즈 (Odds)

오즈(Odds)란, 성공($y=1$)확률이 실패($y=0$)확률에 비해 몇 배 더 높은가를 나타낸다.

$$
\text{odds} = \frac{p(y=1|x)}{p(y=0|x)} = \frac{p(y=1|x)}{1-p(y=1|x)} = \frac{\text{성공확률}}{\text{실패확률}}
$$

>  $p(y=1|x)$ : $x$라는 변수가 주어졌을 때 $y=1$일 확률
>  $p(y=0|x)$ : $x$라는 변수가 주어졌을 때 $y=0$일 확률

#### 로짓 변환 (logit) = Log odds

로짓변환은 오즈(odds)에 log를 취한 함수 형태

$$
\text{logit}(p) = \log(\text{odds}) = \log\frac{p(y=1|x)}{1-p(y=1|x)} = \log\frac{\text{성공확률}}{\text{실패확률}}
$$

근데 신기한게 이렇게 odds를 로짓변환해주면 우리가 알던 선형 모형식이 나온다.
$$
\text{logit}(p) = \log(\text{odds}) = \log\frac{p(y=1|x)}{1-p(y=1|x)} = \log\frac{p(x;\boldsymbol{\beta})}{1-p(x;\boldsymbol{\beta})} = \beta_0 + \beta_1 x
$$
정리하자면
로지스틱 모형식은 선형 모형식과 시그모이드(sigmoid)함수의 결합이며, 이를 로짓 변환 하면 선형 회귀 모형식으로 다시 표현이 가능하다.

로짓 변환을 왜 했는가? -> 해석하기 쉬우라고
위에서 도출된 식 $\beta_0 + \beta_1x$의 값이 커질 수록 odds가 증가 즉, 성공 확률이 증가한다는 뜻이고, 반대로 값이 작아질 수록 실패 확률이 증가한다는 뜻

#### 우도(Likelihood)
: 현재 확률 함수가 데이터를 얼마나 잘 설명하는지 나타내는 지표
- 확률을 추정하는 모델을 결정했으니, 모수$(\beta_0, \beta_1)$를 추정하는 방법에 대해 알아보자
- 선형 회귀에선 MSE(평균 제곱 오차)를 지표로 삼았었으나 확률을 계산하는 함수를 평가하기 위해선 우도(Likelihood)를 지표로 삼는다.
- 모델의 학습은 우도 값을 높여 최대화가 되도록 하는 것이 목표. 이걸 MLE(Maximum Likelihood Estimation)이라고 한다.


# 로지스틱 회귀의 우도 최대화

로지스틱 회귀와 같은 이진 분류 문제에서 우도를 최대화한다.

$$
\underset{\boldsymbol{\beta}}{\text{maximize}} \; \mathcal{L}(\boldsymbol{\beta}) = \prod_{i:y_i=1} p(x_i;\boldsymbol{\beta}) \prod_{i:y_i=0} \left(1-p(x_j;\boldsymbol{\beta})\right)
$$

참고 $\; p(x_i;\beta) = \dfrac{e^{\beta_0+\beta_1x_i}}{1+e^{\beta_0+\beta_1x_i}}$

$\prod_{i:y_i=1} p(x_i;\boldsymbol{\beta})$ : 데이터에 y가 1인 애들을 모아서 걔네들을 예측했을때 1로 예측이 될 확률들을 곱한거. 이게 높아야 잘 맞춘것

그래서 로지스틱 회귀에서 사용할 지표에 대한 아이디어는 이렇게 된다. 
그러나 결국 위 우도를 최대화 하는 $\beta$를 찾는게 목표이기 때문에 선형 회귀때 처럼 미분을 하여 찾아야 하는데 위 방식은 덧셈이 아니라 곱셈으로 되어있게 때문에 미분이 어렵다. 

그래서 트릭을 쓰는것
양변에 log를 취해 곱셈을 더하기로 변환시킨 뒤 log-likelihood를 만들어 최대화 한다. (곱을 로그 취하면 더하기로 바뀜)
$$
\underset{\boldsymbol{\beta}}{\text{maximize}} \; \log \mathcal{L}(\boldsymbol{\beta}) = \sum_{i=1}^{n} \left[y_i \log p(x_i;\beta) + (1-y_i)\log(1-p(x_i;\beta))\right]
$$
로그를 씌워서 max를 찾는게 과연 정당한가? 실제로 해보면 Log변환 한 함수의 max의 위치와 log변환 하지 않은 함수의 max의 위치와 동일함 -> 믿고 써도 됨

그래서 log 변환 시킨 뒤 미분하여 기울기 0 찾는 방식으로 찾는다.
note!. 그러나 이 경우에는 선형 회귀와 달리 수식적으로 $\beta$값을 찾을 수가 없다. 그래서 근접하도록 반복 최적화 하여 찾아야 함

#### 로지스틱 회귀 결과

위와 같은 방식으로 나온 결과

|       | Coefficient            | Std. Error | t-statistic | p-value |
| ----- | ---------------------- | ---------- | ----------- | ------- |
| 절편    | -10.6513               | 0.3612     | -29.5       | 0.0001  |
| 파라미터1 | $\hat\beta_1 = 0.0055$ | 0.0002     | 24.9        | 0.0001  |
이게 무슨 뜻이냐?
추정 결과
$\hat\beta_1=0.0055$
즉, 신용카드 사용량이 1 증가할 때마다 연체의 로짓(log-odss)가 0.0055 증가한다.
note. 확률이 0.0055만큼 증가한다는게 아니다. 로짓이 증가한다는 거다. 주의!

오케이 그런데 이건 1개의 변수일 때만 해당하는 이야기고 역시 다중 변수일 때 어떻게 할지도 해결해야하잖아.

그 경우에도 다중 선형회귀때와 마찬가지다.

$$
\log\frac{p(X;\beta)}{1-p(X;\beta)} = \beta_0 + \beta_1X_1 + \cdots + \beta_pX_p
$$

$$
p(X;\beta) = \frac{e^{\beta_0+\beta_1X_1+\cdots+\beta_pX_p}}{1+e^{\beta_0+\beta_1X_1+\cdots+\beta_pX_p}}
$$

아래는 다중 변수일 때의 결과값 (선형 회귀 때의 결과값과 비슷함.)

|       | Coefficient          | Std. Error | t-statistic | p-value |
| ----- | -------------------- | ---------- | ----------- | ------- |
| 절편    | $\hat{B_0}=-10.8690$ | 0.4923     | -22.08      | 0.0001  |
| 파라미터1 | $\hat{B_1}=0.0057$   | 0.0002     | 24.74       | 0.0001  |
| 파라미터2 | $\hat{B_2}=0.0030$   | 0.0082     | 0.37        | 0.7115  |
| 파라미터3 | $\hat{B_3}=-0.6468$  | 0.2362     | -2.74       | 0.0062  |

여기서 부터 이해가 안됨, 대충 느낌만 파악한 상태
선형 회귀를 얘기하다가 갑자기 shallow 네트워크 , 신경망 이런 얘기를 하는데 그 중간에 어떻게 넘어가진건지가 이해가 안됨.
# Shallow 네트워크 vs 1D 선형회귀

## 단순(1D) 선형회귀

$$y = f[x,\boldsymbol\phi] = \phi_0 + \phi_1 x$$

- 파라미터(parameter): $\phi_0$ (절편), $\phi_1$ (기울기) — 총 **2개**

## Shallow 네트워크의 예

$$y = f[x,\boldsymbol\phi] = \phi_0 + \phi_1\, a[\theta_{10}+\theta_{11}x] + \phi_2\, a[\theta_{20}+\theta_{21}x] + \phi_3\, a[\theta_{30}+\theta_{31}x]$$

### 구조 분해
- $\theta_{j0}+\theta_{j1}x$ ($j=1,2,3$) : 은닉 유닛(hidden unit) 3개가 각각 $x$에 대해 수행하는 **미니 선형회귀** — 앞서 본 "뉴런의 가중합($w \cdot x + b$)" 부분과 동일한 역할
- $a[\cdot]$ : **활성화 함수**(activation function) — 비선형성을 부여하는 함수. 지금까지 배운 시그모이드 $\sigma$도 이 $a$의 한 예시
- $\phi_1, \phi_2, \phi_3$ : 각 은닉 유닛의 활성화 결과를 최종 출력으로 합칠 때 곱해지는 가중치
- $\phi_0$ : 출력층의 절편(bias)

### 파라미터 개수

| 구분 | 파라미터 | 개수 |
|---|---|---|
| 은닉층 ($\theta$) | $\theta_{10},\theta_{11},\theta_{20},\theta_{21},\theta_{30},\theta_{31}$ | 6개 |
| 출력층 ($\phi$) | $\phi_0,\phi_1,\phi_2,\phi_3$ | 4개 |
| **합계** | | **10개** |

→ 은닉 유닛 3개짜리 shallow network

### 단순선형회귀와 비교

| | 단순선형회귀 | Shallow network |
|---|---|---|
| 은닉 유닛 수 | 없음 | 3개 |
| 활성화 함수 | 없음 (선형 그대로) | $a[\cdot]$ 적용 |
| 파라미터 수 | 2개 | 10개 |
| 표현 가능한 함수 형태 | 직선 하나 | 꺾인 구간을 여러 개 가진 비선형 곡선 |

> 참고: 이 예시는 출력이 확률(0~1)이 아니라 연속값 $y$를 예측하는 **회귀** 문제라서, 출력층에는 활성화 함수가 따로 붙지 않고 $\phi_i$로 가중합만 한다. 반면 로지스틱 회귀/분류에서는 마지막에 시그모이드 $\sigma$를 한 번 더 씌워 확률로 변환했다는 차이가 있다.
## 신경망(Neural Network)란 무엇일까?

#### Shallow 네트워크
| 정의
입력층(input) - 은직층(hidden layer) 1개 - 출력층(output) 으로 구성된 1개의 은닉층만 가진 신경망 구조
| 장점
- 복잡한 비선형 패턴까지 학습 가능 (단순 선형 모델 대비 표현력 향상)
- 구조가 간단해 학습 속도가 빠르고 구현이 쉬움
- 데이터가 충분하지 않아도 과적합 위험이 비교적 낮음
| 사용 예시
- 간단한 분류 및 회귀 문제

#### Shallow 네트워크 vs 1차원 선형회귀


Shallow 네트워크의 표현력에서 표현력이 뭔 말이야?
: 

## 경사 하강법

위와 같은 이유로 경사 하강법을 사용하여 예측선과 실제값 차이의 오차를 줄여나간다.

Loss를 최소화 하는 최적의 가중치를 반복적으로 조금씩 조정하면서 업데이트를 하는데 이것이 바로 AI가 최적선을 **학습**하는 것이다.

그럼 경사 하강법은 어떻게 이걸 가능하게 하지?
-> 현재 오차 기울기를 감지해서 기울기가 0에 가까운 방향으로 조금씩 이동하면서 찾음
$w ← w − η · ∂L/∂w$
"업데이트된 새 위치" ← "현재 위치" - "학습률 × 기울기"
>[!note]
>η (에타)
>학습률 - 경사 하강 시의 보폭 크기
>현재 기울기를 확인한 후 얼마 만큼 이동할지 설정하는 **보폭의 크기**
>η가 너무 크면 최솟값을 지나칠 수도 있다. 반대로 η가 너무 작으면 수렴 속도가 느려진다.
>
>∂L/∂w (편미분)
>기울기 - 현재 위치에서의 경사도와 방향
>손실 함수 L을 가중치 w로 미분한 결과값.
>이 편미분값의 반대 방향으로 이동해야 손실이 감소하는 방향으로 간다.




## 확률적 경사하강법
단순 선형 회귀에서 사용하는 방식이 변수가 늘어남에 따라 물리적으로 연산이 불가능하기에 경사 하강법을 사용하기로 했다.
위 경사 하강법 설명을 할 때 사용된 공식은 사실 변수가 1개일 경우에 해당하는 공식이고 다중 선형 회귀에서는 각 변수마다 각자만의 가중치(기울기)를 가진다.
따라서 각 변수들의 가중치의 변경 값들의 평균을 다음 연산에 넣어줘야 하기에 경사하강법 공식은 아래와 같아진다.
$w ← w − η · (1/N) · Σᵢ₌₁ᴺ ∂Lᵢ/∂w$
> [!note]
> 위 설명에서 가중치를 기울기라고 표현했지만 사실 다중 회귀에서는 더 이상 직선이 아니라 평면이기 때문에 편미분 값이라고 표현하는게 정확하다.

공식을 보면 알곘지만 경사 하강법도 데이터의 갯수가 많아 짐에 비례하여 연산의 횟수가 늘어나는건 매 한가지이다.
ex : 데이터 100만개일 경우 기울기 1번 계산 하려면 100만번 연산을 해야함. 많이 느리고 연산시 필요한 내용을 메모리에 올리는 것도 힘듬

그래서 고안 된 것이 **확률적 경사하강법(Stochastic Gradient Descent)**이다.
확률적 경사 하강법은 여러개의 데이터 중에서 무작위로 1개를 선택하여 그 변수를 기준으로한 기울기를 계산해서 나온 결과 값을 사용하여 손실이 적은 방향으로 이동한다.
데이터를 1개만 사용하기 때문에 공식은 다시 $w ← w − η · ∂Lᵢ/∂w$ 가 된다.

SGD는 계속 무작위 데이터를 선택하여 값을 조정하기 때문에 기울기의 값이 요동치는 노이즈가 발생한다.
그러나 이것이 나쁘기만 한 것은 아닌게 지역 최솟값 장벽을 뛰어 넘게 하는 요인이 될수도 있기 때문이다.

## 미니 배치 경사 하강법
많은 한계를 극복해가면서 확률적 경사 하강법까지 도달했지만 이 마저도 완벽하질 않으니 바로 노이즈가 있기 때문이다. 그래서 최적화 과정이 극도로 불안하다. (계속 이리저리 튀는 현상). 
또한 SGD에서는 데이터를 1개씩 루프 돌면서 처리하기 때문에 GPU의 병렬 하드웨어 활용률이 떨어진다. 

이를 극복하기 위하여 미니 배치 경사 하강법이 고안되었다.
이는 데이터를 여러개로 나누고 나눠진 값들의 평균을 사용하여 학습을 한다. 그렇기에 SGD에 비해 수렴 안정성이 증가하였다. 또 이렇게 나눠진 배치들을 병렬로 학습을 수행하면서 학습 속도를 향상 시켰다.

이를 이해하기 위해 사용되는 용어를 하나 알고 가자.
### 에포크
1 에포크(Epoch)는 전체 데이터 세트를 학습에 모두 활용을 마친 상태를 뜻함. 
매 에포크마다 편향을 방지하기 위해 데이터를 셔플하고 설정된 배치크기로 쪼개어 순차 학습한다.



$w \leftarrow w - \eta \cdot \frac{1}{B} \sum_{i \in batch} \frac{\partial L_i}{\partial w}$


## 모델 학습 방법의 발전 흐름

1. 단순 선형 회귀
	공식으로 최적의 기울기와 절편을 구할 수 있었다. 그러나 변수의 수가 늘어난 다중 선형 회귀에선 물리적으로 최적의 기울기와 절편을 구하는 연산을 수행할 수가 없다.
2. 경사 하강법
	따라서 그때 그때의 기울기를 구하여 가장 손실이 적은 방향으로 이동해가면서 값을 찾는 경사 하강법이 고안되었다. 그러나 여전히 변수의 수가 늘어남에 따라 연산 횟수도 비례하여 늘어나 부담이 된다.
3. 확률적 경사하강법
	그래서 모든 변수에 대해서 기울기를 구하지 말고 무작위로 하나의 변수를 선택해 그 변수의 기울기를 따라 값을 조정하는 방식인 확률적 경사하강법이 고안되었다. 그러나 이 방식은 매번 무작위로 변수가 선택되기 때문에 기울기가 계속 달라지며 최적값으로 향하는 과정에 이리저리 튀는 노이즈가 발생하였다. 또한 변수를 순차적으로 돌기 때문에 병렬처리가 가능한 GPU의 이점을 살릴 수가 없었다.
4. 미니 배치 경사하강법
	 그래서 데이터 셋을 여러개의 배치로 나누고 병렬 연산을 하는 방식으로 수렴 안정성과 학습 속도를 향상 시키는 미니 배치 경사하강법까지 도달하게 되었다.



다음 할 것

모델 학습 전체 파이프라인에 대해 정리

활성화 함수 이해

최종적으로 모델 학습 과정을 한눈에 볼 수 있게 정리