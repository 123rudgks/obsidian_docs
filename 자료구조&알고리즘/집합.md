집합은 중복 허용 X. 즉 중복 데이터가 없어야 할 때 활용하면 좋다.

배열 기반 집합과 일반적인 [[배열]] 간의 차이는 집합은 중복 값의 삽입을 허용하지 않는 다는 것 뿐이다.
>[!note]
>집합은 배열 기반 외에도 여러 종류가 있다.


집합의 삽입은 검색이 우선이다.
- 삽입 전 값이 집합 내에 존재하는지 확인해야 하기 때문에 검색이 우선되어야 함

