
[[재귀]]를 사용할 때 불필요한 재귀 호출을 피하는 것이 중요하다. 어떤 떄에는 동일한 재귀 함수를 여러차례 호출하는 경우도 있을 수 있다. (하위 문제 중첩)
**동적 프로그래밍**은 하위 문제가 중첩되는 재귀 문제를 최적화하는 절차다.
>[!note]
>"동적"이라는 말에 너무 의미를 두지 말자, 로직과 큰 연관이 없이 지어진 이름이다.


### 동적 프로그래밍 기법

#### 메모이제이션(memoization)
한번 연산한 함수 결과값을 저장해놓고 이후 호출 부턴 추가 연산 없이 찾아서 사용
```java
public class MemoizationExample {

    static long[] memo;

    public static long fibonacci(int n) {
        // 이미 계산한 값이면 바로 반환
        if (memo[n] != -1) {
            return memo[n];
        }

        // 계산한 결과를 저장
        memo[n] = fibonacci(n - 1) + fibonacci(n - 2);
        return memo[n];
    }

    public static void main(String[] args) {
        int n = 50;

        memo = new long[n + 1];

        // 아직 계산하지 않았다는 의미로 -1 설정
        for (int i = 0; i <= n; i++) {
            memo[i] = -1;
        }

        // 초기값
        memo[0] = 0;
        memo[1] = 1;

        System.out.println(fibonacci(n));
    }
}
```

#### 상향식
별건 아니고 그저 같은 문제를 재귀 대신 (루프 같은) 다른 방식으로 해결하는 것
```java
public static long fibonacci(int n) {
    if (n <= 1) {
        return n;
    }

    long prev2 = 0;
    long prev1 = 1;

    for (int i = 2; i <= n; i++) {
        long current = prev1 + prev2;
        prev2 = prev1;
        prev1 = current;
    }

    return prev1;
}
```


재귀가 매우 직관적이지 않은 이상 일반적으로 상향식을 택하는 편이 더 낫다. 재귀가 더 직관적이면 재귀를 사용하되 메모이제이션으로 빠르게 만들어야 한다.

